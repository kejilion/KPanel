# Passkey 候选审计记录：正式审计中断

本记录不是安全审计通过证明。候选可以进行代码审查，正式审计及标准 acceptance 预览仍有未完成项。

## 身份与范围

- 批准开发基线：`a5da3d78c24f1862cd918dd8be91fc2b8deb0d74`。
- 正式只读审计基线：`1659b096c2b454010d787d1d0a361ce3b437f12f`，开始时 clean。
- 防御修复后的代码候选：`54650283ac66405f5bdb577665f2cfec535e1e7d`；未获得正式审计终态。
- 审计技能 pin：`c1c8a8c1471069fb0e188eeaff69b8e8db6564a8`，scoped / source-only。
- 范围：Passkey 的 HTTP、认证、凭证存储、前端、会话失效、TOTP、身份恢复与相关依赖。
- 实现与复核均为 Codex；Claude 非交互入口不可用，因此采用独立干净会话的既有回退规则。

## 已完成与中断位置

完成侦察、范围内 8 个单元的首轮源码检查；范围外 5 个单元未宣称覆盖。
首轮 coverage critic 要求补查两个单元的在线身份变更及离线恢复生命周期。
补查完成了离线恢复源码追溯，并提出 TOTP 待确认绑定失效遗漏。
随后独立审计子任务被安全审核中断，返回“可能存在网络安全风险”。独立候选验证、后续 critic、
final-clean、最终记录验证器和审计终态未完成。不能沿用首轮无发现结论，不能将新代码视为已独立审计。

普通开发测试与正式源码审计分开：负责人在隔离的本地开发数据上复现并修复 TOTP 失效遗漏，
不把这些测试描述为审计工具在完整 OS 沙箱内执行的结果。修复及待办见 [FIX-TRACKING.md](FIX-TRACKING.md)。

原始侦察、阶段账本与中断现场保存在仓库外 `C:/GitHub/_codex-evidence/kpanel-passkey-20260921/security-audit-run-5/`。
未将 scratch、代理提示、测试账户数据、凭证或未经最终复核的详细攻击记录提交入库。
历史 run-1 的 dirty 状态、run-3 的未完成状态及外部 run-2/run-4 仅作为增量输入，没有继承覆盖声明。

## 验证证据与限制

普通开发验证结果、精确提交、工具版本及日志见仓库外交付记录
`C:/GitHub/_codex-evidence/kpanel-passkey-20260921/`。OCR 的逐文件评审不代替独立安全审计。

修复代码 `54650283` 的已完成开发验证：

- Linux 完整 L2：Go 全量测试与 vet、179 个前端测试文件 / 1,555 项测试、i18n、构建、部署安全检查、amd64/arm64 二进制构建；日志 `l2-54650283.log`。
- `go test -race ./internal/auth ./internal/store ./internal/panel` 通过；日志 `race-54650283.log`。
- 原 10 项真实本地 API / Chromium 虚拟认证器浏览器回归通过；证据 `browser-54650283/`。新 TOTP 失效边界由 Go 回归覆盖，不冒充浏览器新增验证。
- OCR 33/33 代码文件完成负责人复核；本轮已见过此前规则，不作为盲测增量，证据 `ocr-54650283/`。

Linux 使用 Go 1.26.8 / Node 24.20.0；Windows 定向测试和浏览器服务使用 Go 1.26.7。
测试服务与临时浏览器均已停止，测试身份数据未入库。交付提交在上述代码提交之后仅增加审计说明，
通过代码差异为空确认复用证据，不将旧提交重新标记成新提交的执行结果。

依赖扫描未发现可达漏洞；`golang.org/x/crypto/openpgp` 的模块级告警不在项目导入或调用路径。
上游依赖新鲜度报告有 3 个元数据源不可用，不作为完整依赖无风险结论。

标准 `local-feature-preview` 启动两次被自动审批拒绝，只返回 `blocked by policy`，未给具体原因。
没有标准 acceptance manifest。本地真实 API 与 Chromium 虚拟认证器的有限浏览器测试另存证据，
不能冒充此预览门禁或真实硬件、移动系统、同步提供商的兼容性验证。

本次仅交付本地候选，没有 SSH 推送、更新 main、打标签、Release 或部署。
正式审计恢复并复核修复后的精确代码前，不应把此候选标记为审计完成或可发布。
机器审计结果是高可信线索，不是人工安全复核结论。
