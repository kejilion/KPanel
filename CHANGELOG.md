# Changelog

## [0.86.1] - 2026-08-19

### Fixed

- 原生拖拽下载在保留 `DownloadURL` 的同时补充标准 `text/uri-list` 回退，兼容 Windows Explorer、Chrome/Edge 的不同拖拽接收路径。
- `DownloadURL` 与 `text/uri-list` 共用同源、HTTP(S)、无凭据 URL 校验；跨域、非 HTTP(S) 或含用户名/密码的地址不会写入拖拽数据。

### Upgrade Notes

- 本补丁不改变后端/API、下载权限、端口、Agent 权限、Compose 或应用市场契约；文件夹/多选 ZIP 和页面内拖拽保持不变。
- 拖拽仍受浏览器企业下载策略、会话有效期和 Windows Explorer 行为影响；本版本不宣称绕过浏览器策略，需在目标 Windows 环境实测。
- 回滚到 v0.86.0 会恢复仅 `DownloadURL` 的拖拽格式，不删除用户文件或配置。

## [0.86.0] - 2026-08-18

### Added

- 文件预览支持受限的流式媒体读取，图片、音视频继续复用现有会话与权限边界，并支持真实 Range/HEAD 响应。
- Docker Compose 编组支持按组内最早容器创建时间排序，排序方向与容器列表排序保持一致。

### Changed

- AI 移动端模型选择菜单在窄屏下居中显示，避免弹层偏移或超出视口。
- 历史监控初始状态隐藏空闲缩放提示，进入缩放或精细数据加载时仍保留状态条、返回和重置操作。

### Upgrade Notes

- 本版本不迁移数据库、不改变 KPanel 端口、Agent 权限、`kejilion.sh` 或应用市场安装契约；现有文件、容器、Compose 配置、网络和卷不会被自动改写。
- 媒体预览继续受会话、路径、资源版本、Range 和响应大小限制；无法提供有效流式响应时仍回退到原有下载/打开入口。
- Docker 创建时间排序读取 Docker 返回的真实时间；缺失或无效时间的编组保持稳定名称顺序，不影响容器操作。
- 回滚到 v0.85.0 会移除本版本的媒体流式预览、Docker 编组创建时间排序、AI 菜单居中和历史监控提示精简，但不会删除用户数据或运行中的资源。

## [0.85.0] - 2026-08-18

### Added

- Docker 容器列表支持按真实创建时间排序，排序方向可切换并保持容器状态与 Compose 编组信息不变。

### Changed

- 优化 Docker 管理页面在桌面、中等宽度和窄屏下的操作列布局，Compose 管理操作保持可见且不被裁切。
- Compose 与独立容器编组的标题、收起/展开、强调色和轻量过渡保持清晰一致，容器操作按钮在非全屏窗口中仍可达。

### Upgrade Notes

- 本版本不迁移数据库、不改变 KPanel 端口、Agent 权限、`kejilion.sh` 或应用市场安装契约；现有容器、Compose 配置、网络和卷不会被自动改写。
- 创建时间排序读取 Docker 返回的真实创建时间；无法提供有效时间的条目保持稳定顺序，不影响容器管理操作。
- 回滚到 v0.84.0 会移除新的排序和本轮布局表现，但不会删除容器、Compose 文件、网络、卷或项目变量。

## [0.84.0] - 2026-08-18

### Added

- 文件和目录现在可以从 KPanel 文件窗口或桌面快捷方式直接拖出到操作系统；单文件使用原生下载拖出，目录和多选实时生成受限 ZIP，不在服务器持久化归档。
- 拖出下载继续复用会话、路径、符号链接、资源版本、并发和归档容量校验；不支持原生 `DownloadURL` 的浏览器保留显式下载/压缩入口。

### Fixed

- 作业详情中的阶段标记固定在详情弹窗内部，不再因全局样式选择器导致标记溢出或出现在弹窗外。

### Upgrade Notes

- 本版本不迁移数据库、不改变 KPanel 端口、Agent 权限、`kejilion.sh` 或应用市场安装契约；现有文件、目录和桌面入口不会被自动改写。
- 拖出目录或多选时，Agent 只流式生成临时 ZIP 响应，不在源目录或 Panel 中创建持久归档；来源变化、权限不足或超出预算会拒绝/中止传输，不产生可误认的完整文件。
- 回滚到 v0.83.0 会移除原生窗口外拖出入口，但不会删除任何真实文件、目录、桌面快捷方式或既有配置；用户仍可使用页面内下载与压缩操作。

## [0.83.0] - 2026-08-18

### Added

- Compose 部署会自动识别 `${VAR}` 变量，并在部署与项目管理中提供紧凑的变量编辑；变量保存为项目标准 `.env` 文件并参与 `docker compose config` 校验。
- Compose 与独立容器编组支持收起和展开；Compose 编组使用稳定的低饱和强调色，键盘操作和“减少动态效果”偏好均受支持。
- 标准 `/home/docker/<project>` 与 `/home/web/<project>` 目录中的 Compose 项目即使暂时没有容器也会保留在管理列表，可直接重新部署恢复容器。

### Changed

- Compose 配置与 `.env` 使用同一资源版本、原子替换和失败回滚事务；`.env` 权限固定为 `0600`，运行状态和容器数量不再造成虚假的版本冲突。
- Docker 部署语法错误会自动短暂标记对应行，随后保留浅色错误提示；精确诊断存在时不再重复显示通用错误。
- 优化中等宽度下的容器操作列，并为编组收起/展开使用轻量 `120ms` 合成层过渡，避免按钮裁切和表格缩放卡顿。

### Upgrade Notes

- 本版本不迁移数据库，不改变 KPanel 端口、Agent 权限、`kejilion.sh` 或应用市场安装契约；现有运行中的 Compose 项目不会被自动重部署。
- 现有 `.env` 继续作为项目变量真源；KPanel 仅在管理员明确保存或重新部署时写入，敏感值不会出现在日志或回执中。
- 零容器项目发现仅覆盖 KPanel 标准目录 `/home/docker/<project>` 与 `/home/web/<project>`，不会扫描任意宿主机路径。
- 回滚到 v0.82.0 不会删除 Compose 配置、`.env`、容器、网络或卷，但会移除变量管理和零容器项目发现；零容器项目需在 KPanel 外执行一次 `docker compose up -d` 后才会重新出现在旧版列表。

## [0.82.0] - 2026-08-17

### Added

- Docker Run 与 Compose 粘贴部署新增轻量语法、语义诊断和精确行列定位，错误项可点击聚焦；提交前仍由 Agent 使用 `docker compose config` 做最终校验。
- Docker 容器按标准 `com.docker.compose.*` 标签编组，并新增基于真实 `working_dir`、`config_files` 的 Compose 项目启动、停止、重启、编辑与重新部署管理。

### Changed

- Compose 配置重新部署加入资源版本并发保护、同目录暂存、权限与属主保留及原子替换；启动失败时自动恢复原配置并重新拉起原项目。
- 优化 Docker 页面桌面与窄屏布局、部署诊断提示和管理入口可达性，独立容器继续单独展示。

### Upgrade Notes

- 本版本不迁移数据库、不改变 KPanel 自身端口、Agent 权限、`kejilion.sh` 或应用市场安装契约。Compose 项目状态继续以 Docker 标准标签和实际配置文件为真源。
- 升级不会自动改写现有业务 Compose 文件；只有管理员明确保存并重新部署时才进入带备份与失败回滚的写事务。
- 回滚到 v0.81.2 会移除 Compose 编组、诊断和项目管理界面，不删除现有容器、网络、卷或 Compose 配置；如回滚前正在执行重部署，应先等待任务结束并核对项目实际状态。

## [0.81.2] - 2026-08-17

### Changed

- 统一登录、初始化和密码恢复流程的产品定位与操作提示，并优化概览、网站、Docker、应用、AI、集群、终端、文件、审计、任务、诊断、监控和设置等页面的现有可见文案。
- 同步优化简体中文、繁体中文和英文词条，使关键动作、状态和错误提示更简洁一致；本次没有新增文案槽位或改变页面功能。

### Upgrade Notes

- 本补丁仅调整前端可见文案及对应精确测试，不改变数据库、API、依赖、端口、Compose、Agent 权限、`kejilion.sh` 或应用安装/更新契约，无需数据迁移。
- 回滚到 v0.81.1 会恢复旧文案，不会修改用户数据、应用配置或 KPanel 的既有本地定制。

## [0.81.1] - 2026-08-17

### Fixed

- 应用市场全量列表现在始终优先展示已安装应用，同时在已安装与未安装分组内继续按 60 个 UTC 自然日规则展示和排序新品。
- 更新 Arena Brawl、Bomb Party、Ice Climber Arena 和 Neon Arena 四个游戏的 128×128 WebP 图标，并将目录摘要与实际资源保持一致。

### Upgrade Notes

- 本补丁不改变数据库、API、端口、Compose、Agent 权限、`kejilion.sh` 或应用安装/更新契约，无需数据迁移。
- 回滚到 v0.81.0 会恢复新品优先于已安装状态的全量列表排序和旧游戏图标，不会卸载应用或修改应用配置。

## [0.81.0] - 2026-08-17

### Added

- 应用市场支持可选的首次收录日期：新应用在 60 个 UTC 自然日内显示“新品”，并按收录日期倒序优先展示；日期缺失的历史目录保持原有排序。
- 离线回退目录同步到 153 个应用，新增 DeepSeek Harness、AIradio 和 4 个多人网页游戏，并补齐简体、繁体、英文元数据及本地图标。

### Changed

- 应用目录同步会保留 KPanel 已审核的既有图标、繁体文案、首次收录日期及其他本地元数据，只为真正新增的应用下载图标，避免上游刷新覆盖产品内定制。
- 远程目录刷新会冻结同 id/token 应用的首次收录日期；应用更新、改名或换图标不会重新获得“新品”标记，非法或未来日期不会被展示。

### Upgrade Notes

- 本版本不改变数据库、端口、Compose、Agent 权限或 `kejilion.sh` 固定运行时，无需数据迁移；`addedAt` 是可选展示字段，旧版会安全忽略。
- 新增应用仍通过现有脚本 selector 与第三方配置安全检查决定安装能力；未通过兼容性校验时只展示目录信息，不会执行未知命令。
- 回滚到 v0.80.1 会移除新品排序和本版离线目录快照，但不会卸载应用、删除配置或覆盖 KPanel 已有图标；在线动态目录仍按旧版规则加载。

## [0.80.1] - 2026-08-16

### Fixed

- 修复桌面文件或目录快捷方式快速松手时，浏览器未触发原生 `drop` 事件而导致本地落点未保存的问题；同时避免跨面板复制结束后被误判为本地移动，并在拖出桌面期间隐藏本地原位图标。

### Upgrade Notes

- 本补丁仅调整桌面快捷方式拖放状态，不改变数据库、API、端口、Compose、Agent、`kejilion.sh` 或应用市场契约，无需数据迁移。
- 回滚到 v0.80.0 会恢复旧拖放行为；已有文件、目录、桌面入口及其已保存位置不会被删除。

## [0.80.0] - 2026-08-16

### Added

- 已配对的 KPanel 在双方支持安全入口时，会通过现有集群摘要同步入口路径；从集群页面打开远程面板会直接进入该安全入口，再由目标面板完成独立登录。

### Fixed

- 修复桌面图标连续快速拖动时，较早的位置保存请求完成或失败后可能覆盖最新落点的问题；本地拖放完成后也会立即清除拖动状态。
- 修复从另一个 KPanel 跨站打开安全入口时，浏览器因 `SameSite=Strict` Cookie 尚未进入目标站点上下文而落到 404 的问题；现在先加载无脚本、禁止联网的同源过渡页，再进入登录页，不降低 Cookie 策略。

### Upgrade Notes

- 本版本不改变数据库、端口、Compose、Agent 权限、`kejilion.sh` 或应用市场契约，无需数据迁移。
- 安全入口路径只返回给已认证且明确声明兼容能力的配对控制端；旧版控制端、旧版目标端、轻量节点或未启用安全入口时继续打开面板根地址，不需要重新配对。
- 回滚到 v0.79.0 会恢复从集群页面打开根地址的行为，并移除桌面落点竞态修复；现有配对、安全入口配置和桌面位置数据不会被删除。

## [0.79.0] - 2026-08-16

### Added

- 桌面新增壁纸选择器，在经典壁纸之外提供 4 张内置壁纸；选择结果仅保存在当前浏览器，不改变服务器数据或其他用户的桌面。

### Changed

- 壁纸和明暗主题切换增加平滑过渡，并在切换主题前关闭桌面菜单；浅色壁纸遮罩更轻，同时继续尊重系统的“减少动态效果”偏好。
- 集群公开分享入口调整到访问设置之前，公开页面强化信息层级、主机卡片与列表布局，并改善桌面及窄屏下的紧凑度和可读性。

### Upgrade Notes

- 本版本不改变数据库、API、端口、Compose、Agent、`kejilion.sh` 或应用市场契约。壁纸偏好使用浏览器本地存储键 `kpanel:desktop-wallpaper:v1`；旧版本会忽略该键，升级或回滚均不会影响服务器数据。
- 集群公开分享继续沿用既有随机 Token、字段白名单、缓存、限流与默认关闭策略；本版本只调整入口与呈现，不扩大匿名访问范围。
- 回滚到 v0.78.0 会移除新增壁纸入口和分享页布局优化，但会保留浏览器本地壁纸偏好及已有分享状态；重新升级后可继续使用。

## [0.78.0] - 2026-08-16

### Added

- Cluster v2 配对支持“一次配对、双向文件互传”：中心添加目标 KPanel 后，在回连地址可达时自动建立反向文件能力，无需创建反向主机、复制私钥或授予反向摘要与终端权限。
- 文件管理器与桌面之间支持双向组合传输，覆盖文件管理器 ↔ 文件管理器、桌面 ↔ 桌面、文件管理器 ↔ 桌面；旧 v2 配对可在主机管理中显式启用并刷新连接。

### Changed

- 桌面内移动文件/目录快捷方式时不再误显示整屏接收蒙层，并补齐抓取点偏移、实时位置预览、多选整体约束、边缘自动滚动及按预览位置落格；跨窗口仍使用浏览器原生拖放通道。
- 双向文件连接使用独立 Noise 路径与 prologue、短期续租和无密钥 sidecar；直连主机认证失败不会降级到互传通道，撤销或删除关系后新的文件打开请求立即拒绝。

### Upgrade Notes

- 本版本不改变数据库、Compose、端口、Agent 权限、`kejilion.sh` 或应用市场契约。升级后会在 KPanel 数据目录按需创建 `cluster-file-peers-v2.json`；该文件不保存密钥，采用严格 JSON、`0600` 权限和原子写入，应与数据目录一并备份恢复。
- 新版中心连接旧版目标面板时，收到 `404`、`405` 或 `426` 后会停止自动重试；目标面板升级后，在主机管理中点击一次“启用双向文件互传”即可，无需重新配对。配置的 Public URL 必须可由对端访问，地址变化后可使用“刷新连接”。
- 回滚到 v0.77.0 会停止使用新增的反向文件 sidecar，但不会删除已传输文件或桌面入口；原有单向 Cluster v2 关系继续保留。需要完全撤销文件互访时，应在回滚前先删除或撤销对应配对。

## [0.77.0] - 2026-08-16

### Added

- 新增完整繁體中文（`zh-TW`）介面：覆蓋基礎文案與全部 20 個按頁載入語言包，語言選擇、重新整理後持久化、日期/時長/相對時間及動態無障礙標籤均使用同一 locale。
- 瀏覽器語言為 `zh-TW`、`zh-HK`、`zh-MO` 或 `zh-Hant` 時會自動選用繁體中文；使用者仍可在簡體中文、繁體中文和英文之間主動切換。
- 應用市場為 7 個分類與 147 個現有應用補充繁體中文名稱及描述；遠端目錄尚未提供繁體欄位時，後端按 id/token 安全合併內建翻譯。

### Changed

- 應用市場同步工具會保留已審核的 `zh_tw`、`name_zh_tw` 和 `desc_zh_tw` 欄位，後續同步上游簡體/英文資料與圖示時不會覆蓋繁體翻譯。
- i18n 完整性門禁同時校驗英文與繁體中文的來源鍵、插值占位符、空翻譯及全部 lazy catalog，避免後續頁面只更新單一語言。

### Upgrade Notes

- 本版本不改變資料庫、API 路徑、連接埠、Compose、Agent 權限、`kejilion.sh` 或應用安裝動作；既有設定和資料無需遷移。
- 遠端動態應用目錄即使暫未提供繁體欄位，也會沿用同 id/token 的內建繁體元資料；無法匹配的新應用會明確回退到既有簡體名稱與描述，不影響安裝能力。
- 回滾到 v0.76.0 時，舊版本會忽略新增的應用目錄繁體欄位；已儲存的 `zh-TW` 選擇會按舊版既有回退邏輯重新選擇支援語言，不會破壞設定或資料。

## [0.76.0] - 2026-08-15

### Added

- 桌面中的真实文件与目录快捷方式可拖到另一个已配对的 KPanel：拖入目标桌面会复制到 `/home/KPanel Desktop` 并在传输成功后创建入口，拖入目标文件管理器则复制到当前目录或指定目录；支持框选、多选、部分可用选择和明确的跳过提示。
- 集群页面新增默认关闭的公开分享页。管理员可生成、关闭或重置随机分享链接；匿名页面仅展示经过固定白名单重新组装的主机名称、在线状态、地区、系统及资源概况，不公开面板地址、IP、节点标识、指纹、版本、错误细节或管理入口。

### Fixed

- AI 思考模式的一次模型响应包含多批工具调用时，后续回放会保留该响应对应的真实推理上下文，避免兼容接口拒绝工具历史，同时继续排除猜测性推理内容。

### Changed

- 桌面跨面板文件传输沿用 Cluster v2 的 `cluster.files.read` 配对权限和目标端重新鉴权；拖拽描述符不携带密钥，应用、网站、URL 与系统入口不会伪装成可传文件。同一 KPanel 的桌面快捷方式拖回自身文件窗口会被拒绝，避免与本地复制或移动语义冲突。
- 公开分享响应使用 10 秒服务端快照缓存、15 秒可见页刷新和按来源限流；关闭分享或重置链接后，旧地址立即失效并统一返回未找到。

### Upgrade Notes

- 本版本不改变数据库、端口、Compose、Agent 特权、`kejilion.sh` 或应用市场打包契约。公开分享默认关闭；升级不会自动暴露任何集群信息，只有管理员显式开启后才生成访问地址。
- 桌面跨面板传输只复制真实文件或目录，不移动、修改或删除来源；只有复制与校验全部成功后才创建目标桌面入口。已有 Cluster v2 配对如缺少 `cluster.files.read`，需由管理员确认双方身份后重新配对。
- 回滚到 v0.75.0 不会删除已经复制的文件、桌面入口或公开分享设置；旧版本不会显示本版本入口。若曾启用公开分享，回滚前应先关闭分享，以避免旧版本无法管理仍保存在状态文件中的分享配置。
- AI 修复适用于新生成的工具调用历史；已被上游接口拒绝的旧会话可新建对话重新开始。

## [0.75.0] - 2026-08-15

### Added

- 已配对的 KPanel 主机之间支持在文件管理器和桌面中复制文件或目录；传输按项目逐项执行，提供进度、取消、同名副本、部分成功与明确失败反馈，源文件始终保留不动。
- 跨主机目录采用受限归档流传输，并对路径、资源版本、条目数量、单项与总字节数进行校验；目标端只在完整校验成功后原子提交，失败或取消会清理临时内容。
- 文件窗口与桌面支持将远端文件或目录拖入当前目录、指定目录或桌面；只有真实复制完成后才创建桌面入口，不会因快捷方式写入失败删除已复制文件。

### Changed

- 新建立的 Cluster v2 配对会显式包含 `cluster.files.read` 权限；已有配对不会静默扩大权限，需要管理员重新配对后才能使用跨主机文件复制。
- 应用市场继续使用 v0.74.x 已上线的动态目录、同源图标缓存与安全回退契约，本版本将在线目录及 DeepSeek Harness 条目纳入发布验收，不改变安装脚本或应用配置格式。

### Upgrade Notes

- 本版本不改变数据库、端口、Compose、Agent 特权、`kejilion.sh` 或应用市场打包契约，无需数据迁移。跨主机复制只创建目标副本，不移动或删除源项目；同名目标会生成可预测的编号副本。
- 出于最小权限原则，升级前已经存在的 Cluster v2 配对不会自动获得文件读取能力。需要跨主机复制时，请在确认双方身份后重新配对；旧配对的监控、终端等既有能力不受影响。
- 桌面文件入口仍默认使用 `/home/KPanel Desktop`，真实文件与 `${DataDir}/desktop-workspace/workspace.json`、`icons/` 应按既有规则成对备份。回滚到 v0.74.1 不会删除已经复制的真实文件，但旧版本不提供跨主机复制入口。

## [0.74.1] - 2026-08-15

### Fixed

- AI 对话按目标供应商能力保存兼容的历史上下文，避免 DeepSeek 等接口因不支持的推理字段或空助手消息拒绝后续请求，同时不回放模型的推测性推理内容。
- 动态应用目录图标通过 Agent 有界下载、校验并缓存到本地，由 Panel 提供同源读取入口；上游图标不可用时继续使用内置回退，不影响应用目录加载。
- 诊断终端标题输出统一使用 Unix 换行，避免浏览器终端中出现多余空行或平台相关显示差异。

### Upgrade Notes

- 本补丁不改变数据库、端口、Compose、Agent 权限、应用安装动作或 `kejilion.sh` 协议；动态图标缓存可安全重建，现有配置与数据无需迁移。

## [0.74.0] - 2026-08-15

### Changed

- 应用市场在线目录以同一套校验、缓存和安全回退机制动态更新内置与第三方应用；既有业务继续优先使用固定受管脚本，只有受管脚本不包含动态新增的内置 selector 时，才回退到通过安全、协议和 selector 校验的宿主机 `/usr/local/bin/k`。完成本次升级后，后续新增应用只需同步更新 `kejilion.sh` 与动态目录，不接受远程命令或安装器。

### Upgrade Notes

- 本版本不改变数据库、API、端口、Compose、Agent 权限或现有应用的安装行为；动态新增的内置应用要求宿主机 `kejilion.sh` 已包含对应编号。脚本尚未更新时任务会明确失败，KPanel 不会执行远程命令或自行下载替代安装器。

## [0.73.2] - 2026-08-15

### Fixed

- 经典模式侧栏在进入 AI 等分区的嵌套工作区路由后继续保持对应导航项选中，避免深层页面丢失当前位置提示。
- 桌面模式的 Agent 更新提示在浅色主题下使用更清晰的文字、背景和边框对比度，同时保留深色主题原有视觉层级。

### Upgrade Notes

- 本补丁不改变数据库、API、端口、Compose、Agent 权限、应用市场安装契约或 `kejilion.sh` 协议；现有配置与数据保持不变。

## [0.73.1] - 2026-08-15

### Fixed

- 桌面框选区域改用稳定的品牌色变量，恢复在当前主题下的可见边界和填充效果。
- 桌面应用与网站跳转确认窗重新平衡标题栏和内容区间距，使图标、标题与关闭按钮保持垂直对齐。
- 应用市场强化已安装卡片的状态边框，并约束标题与状态徽标的伸缩关系，避免长名称挤压或错位。

### Upgrade Notes

- 本补丁不改变数据库、API、端口、Compose、Agent 权限、应用市场安装契约或 `kejilion.sh` 协议；现有配置与数据保持不变。

## [0.73.0] - 2026-08-15

### Added

- 桌面右键菜单在主题切换下方新增浏览器全屏开关，可由用户主动进入或退出全屏；不支持或拒绝全屏的浏览器继续使用原有视口桌面模式。

### Fixed

- 网站页面等待 Agent capabilities 实际加载完成后才显示“网站写入当前不可用”，避免页面初次加载时短暂误报。

## [0.72.0] - 2026-08-14

### Added

- 文件管理中的普通文件和目录可通过单项菜单、批量操作或拖放添加为桌面快捷方式；文件入口打开父目录并预览目标，目录入口直接打开对应位置，不同目录可在独立文件管理窗口中并行使用，同一目录会聚焦复用已有窗口。
- 宽屏桌面支持空白区域框选、Ctrl/Command/Shift 追加选择、选中组整体拖动与一次确认批量移除；固定系统入口不会被删除，应用、网站、文件和目录的真实资源保持不变。
- 可将 Windows、macOS 或 Linux 文件管理器中的文件和文件夹直接拖入 KPanel 桌面；内容通过现有 Agent 文件流上传到真实宿主机目录，保留目录结构和空目录，并以轻量传输浮层显示总进度、取消、完成及部分失败状态。
- 多个文件管理窗口之间支持将单个或多选文件、目录直接拖到目标窗口或文件夹；默认移动，按住 Ctrl/Option 复制，并提供目标高亮、进行中状态及部分失败反馈；复制操作可安全取消。文件剪贴板同时支持跨窗口复制、剪切和粘贴。

### Changed

- 桌面工作区升级为 v2 类型化快捷方式，统一区分 URL、文件与目录目标；文件快捷方式只保存规范路径引用，从桌面移除不会移动、修改或删除宿主机真实文件。
- 桌面文件快捷方式沿用文件管理器的文件类型图形，并为代码、图片媒体、表格、压缩包等大类使用克制的分类色；目录快捷方式使用打开文件夹图形，在保持黄色一致的同时和系统“文件”入口形成区别，普通及未知文件则使用中性回退。
- 外部桌面上传默认使用 `/home/KPanel Desktop`，同名项目自动保留为编号副本；完成后的文件和目录图标不增加永久角标或额外颜色，保存位置可切换到其他已存在且可由文件管理访问的宿主机目录。
- 文件移动或重命名成功后，指向原项目或其后代的桌面快捷方式自动跟随 Agent 返回的新路径，保留图标位置与名称；仅同步成功项目，复制操作不自动创建新的桌面入口。

### Fixed

- 提升桌面框选区域在深浅色壁纸上的边界与填充对比度，使拖动范围更容易识别，同时保持图标内容清晰可见。
- 修复文件管理批量工具栏与全屏编辑器的局部样式在生产构建后错误合并、导致桌面文件窗口被隐藏的问题。
- 修复通过目录快捷方式打开文件管理后，桌面“文件”图标只聚焦其他目录、无法另开根目录窗口的问题。

### Upgrade Notes

- 既有 Workspace v1 URL 快捷方式在读取时自动兼容为 v2，并在下一次工作区写入时原子升级，无需人工迁移。仅支持 v1 的旧版本会保留但暂时不读取 v2 工作区，重新升级后可继续使用；回滚或备份仍需成对处理 `${DataDir}/desktop-workspace/workspace.json` 与 `icons/`。
- 既有文件和目录入口仍只引用 Agent 文件系统，不复制或移动源目标；只有从操作系统外部拖入的内容会执行真实上传。首次使用默认位置时会创建 `/home/KPanel Desktop`，从桌面移除入口不会删除其中内容，真实删除仍须在文件管理回收站流程中执行。

## [0.71.0] - 2026-08-14

### Added

- 桌面图标支持鼠标、宽屏触摸及手写笔直接拖拽吸附、位置交换和自动整理；已安装应用与网站可仅从桌面隐藏并恢复，自定义快捷方式支持名称、描述、HTTP(S) 地址及受限图片上传。布局与快捷方式使用独立、带资源版本的 Panel 工作区持久化，不会卸载应用、删除网站或触发目标地址抓取。

### Changed

- 桌面右键菜单按“刷新桌面、添加快捷方式、管理桌面图标”排列；图标管理窗口将自定义快捷方式置顶、自动整理置底，并在添加或编辑快捷方式时保留管理窗口，避免切换弹窗时闪屏。
- 桌面“添加快捷方式”表单采用统一的图标化输入框、聚焦反馈和图标上传卡片，并改善移动端输入可读性。
- 前端开发服务器支持通过 `VITE_DEV_API_CHANGE_ORIGIN=true` 连接 WSL 等独立 KPanel 实例，默认代理行为保持不变。
- 桌面应用与网站图标的透明区域现在使用柔和白底，避免透出桌面渐变影响识别；跳转确认窗同时采用更紧凑的宽度，并集中显示名称和完整地址，不再保留重复说明。
- 青龙面板与 Nginx Stream 四层代理更新为统一风格图标，并复用于应用市场、桌面入口和跳转确认窗。
- Go 构建工具链升级到 `1.26.6`，同步更新 CI、依赖审计、Release 与固定摘要的官方构建镜像，修复标准库网络与模板路径中的可达安全问题。
- 项目 README 重新组织产品定位、桌面/经典模式、AI、集群、核心能力、安全边界与部署入口，并补充当前界面预览。

### Upgrade Notes

- 首次读取桌面工作区时会自动创建 v1 空配置；隐藏应用或网站只影响桌面显示，不会卸载应用、删除网站或修改真实资源。
- 桌面工作区使用独立的 `${DataDir}/desktop-workspace/` 目录，旧版本会忽略并保留这些数据；备份或恢复时必须停写并成对处理 `workspace.json` 与 `icons/`，不能只恢复其中一侧。
- 本版本不改变端口、Compose、Agent 权限、应用市场安装契约或 `kejilion.sh` 协议，现有应用、网站及其他配置保持不变。

## [0.70.2] - 2026-08-13

### Changed

- 文件管理器在多选文件后打开右键菜单时，批量下载、复制、剪切、压缩、权限修改和移入回收站会继续作用于完整选择；回收站新增当前列表全选入口，并为批量权限修改补齐资源版本保护。
- 主机终端与应用交互终端在会话激活、切换和关闭后优先聚焦真实 Shell 输入区域，避免键盘输入落入隐藏或辅助输入控件。
- Docker 页面统一输入框、下拉框和复选框的尺寸、焦点及明暗主题表现；浅色主题的部署编辑器不再固定使用深色终端底色。
- 桌面应用与网站的系统浏览器跳转确认窗改为更紧凑的扁平布局，集中显示图标、名称和完整地址，减少重复说明。

### Upgrade Notes

- 本补丁不改变数据库、API、端口、Compose、Agent 权限、应用市场安装契约或 `kejilion.sh` 协议；升级过程不会自动执行文件、Docker 或终端写操作，现有配置与数据保持不变。

## [0.70.1] - 2026-08-13

### Fixed

- 修复经典 Docker 镜像存储模式下，更新后未带标签的旧版 KPanel 镜像不会出现在默认镜像列表、因而无法自动清理的问题；清理现在显式扫描全部镜像，同时继续保留当前镜像和仍被容器引用的历史镜像。

### Upgrade Notes

- 本补丁不改变数据、端口、API、Agent 权限或 `kejilion.sh` 协议；升级成功后会按原有安全条件补清理无容器引用的旧版官方 KPanel 镜像。

## [0.70.0] - 2026-08-13

### Changed

- 桌面应用与网站的跳转确认窗现在会显示对应图标；图标不可用时保留应用字母或网站标识作为回退。

### Fixed

- KPanel 应用市场更新成功后会清理没有容器引用的旧版官方 KPanel 镜像，同时保留当前镜像和仍被容器使用的历史镜像；更新失败或回滚路径不执行清理。

### Upgrade Notes

- 没有数据库、API、端口、Agent 权限或 `kejilion.sh` 协议变化。应用市场更新会在新版本健康后执行有界旧镜像清理；删除失败不会影响更新成功，已保存的停写备份和镜像归档仍作为回滚真源。

## [0.69.0] - 2026-08-13

### Removed

- 移除桌面内置浏览器的 Web 界面、Panel API、Reader/Beta 运行时、Relay 服务、部署参数、依赖例外及相关测试与规范。

### Changed

- 桌面已安装应用和已部署网站的 Web 地址现统一显示跳转确认窗；只有用户确认后才使用系统浏览器打开，脚本型应用继续进入脚本终端。

### Upgrade Notes

- 没有数据库迁移。受管应用市场更新会使用 Compose `--remove-orphans` 移除旧版已废弃服务；独立部署升级时应使用新 Compose 并清理不再需要的旧环境变量、密钥挂载和反向代理入口。

## [0.67.0] - 2026-08-12

### Added

- Docker“新建容器”支持直接粘贴单条 `docker run` 命令或完整 Compose YAML，并自动识别部署类型；常用命令参数转为既有结构化配置，Compose 项目保存至 `/home/docker/<project>/docker-compose.yml` 后由固定 `docker compose` 入口校验和启动。
- Compose 部署新增同名资源冲突检查、任务互斥、输出脱敏与有界保留、启动失败回滚；回滚失败时保留恢复文件并明确标记需要人工处理，不虚报成功。

### Fixed

- 修复手机端主机终端和应用交互终端无法稳定滑动历史输出的问题，新增原生纵向触摸回滚映射并兼容新版 xterm 滚动容器。
- 修复窄桌面窗口仍为已转为抽屉的主机列表保留空行、挤压终端区域的问题。

### Upgrade Notes

- 本版本没有数据库、端口、密钥、Agent 协议或 `kejilion.sh` 协议变化。只有用户明确提交 Compose 部署时才会新建 `/home/docker/<project>` 并启动容器，升级过程不会自动执行 Docker 写操作。
- 回滚可恢复 `v0.66.0` 不可变镜像；已由本版本创建的 Compose 项目和容器不会自动删除，仍可通过 Docker CLI、Compose、`kejilion.sh` 或恢复新版面板继续管理。

## [0.65.0] - 2026-08-12

### Added

- 手机经典模式下，主机终端和体检页面的侧边选择器改为左侧抽屉；可通过顶部按钮打开，并在完成选择、点击关闭按钮或点击遮罩后收起，避免侧栏持续挤压终端输出和输入区。

### Changed

- 经典模式的主机终端与体检终端统一工作区高度、最小高度、终端底色、边框和圆角；主机数量、空状态或错误提示不会改变终端工作区本身的尺寸规则。
- 依赖治理继续使用现有单一工作流：稳定版本候选按周检查，当前依赖安全通告按日审计；到期例外和 EOL 复核期限会进入报告并触发明确告警，不会自动合入、发布或部署。

### Upgrade Notes

- 本版本只调整 Web 终端/体检布局及项目治理脚本和 Actions，没有新增 Go API、数据库、端口、Compose、systemd 权限、`kejilion.sh` 协议或应用市场安装契约变化。
- 升级不会自动执行终端命令、体检项目或宿主机写操作；配置与数据保持不变，可直接回滚至 `v0.64.0`。

## [0.64.0] - 2026-08-11

### Added

- 新增独立“系统中心”页面和桌面应用入口，将现有系统管理能力按日常维护、基础配置、登录与安全、网络与流量、性能优化及危险操作六个稳定分区集中展示。
- 为系统更新、系统清理、虚拟内存、SSH 防御、DNS 和 BBR 六项常用工具增加“推荐”标记；推荐状态在窄屏下保持完整可读。

### Changed

- 概览页的系统管理区改为 3×2 的六个高频快捷入口，并通过“更多设置”进入完整系统中心；从系统中心返回概览时恢复原视口位置，经典模式、桌面模式和移动端共用同一真实状态与管理弹窗。
- 一条龙系统调优移入“系统中心 → 性能优化”，概览继续保留快捷入口；既有 typed API、Agent 权限、持久化任务、失败停止和 `kejilion.sh` 真源契约保持不变。

### Upgrade Notes

- 本版本仅调整 docs 与 Web 展示、导航和交互，没有新增 Go API、数据库、端口、Compose、systemd 权限或 `kejilion.sh` 协议变化。
- 升级和打开系统中心不会自动执行系统更新、清理、重启、SSH、防火墙、调优或重装操作；现有配置和面板数据保持不变，可直接回滚至 `v0.63.1`。

## [0.63.1] - 2026-08-11

### Fixed

- 修复 Release 运行时镜像契约仍核对上一版 `kejilion.sh` 提交和摘要、导致正确的 `0.63.0` 候选在公开产物生成前被门禁拦截的问题。
- 受管脚本提交、Dockerfile `ADD --checksum`、外联真源登记、镜像 OCI 标签和镜像内脚本摘要现在由同一个检查器交叉验证；本地 L3 与 Release 共用该检查器，后续更新脚本时不再维护工作流中的平行硬编码值。

### Upgrade Notes

- 本补丁包含 `0.63.0` 标记中的一条龙系统调优功能，业务代码和宿主机写入语义未变化；`v0.63.0` 未公开 Release、未推送正式镜像、未进入生产。
- 无数据库、端口、Compose 或 systemd 权限迁移；升级本身不会自动执行系统调优。可回滚至 `v0.62.2`，但回滚 KPanel 不会撤销管理员已经主动执行的宿主机调优。

## [0.63.0] - 2026-08-11

### Added

- 概览“基础系统设置”新增一条龙系统调优：默认选择脚本菜单 66 的 12 个固定项目，也可逐项取消；执行后按真实脚本顺序展示后台进度，页面关闭或连接中断后重新打开仍可恢复任务状态。
- 新增独立 typed Agent/Panel API `GET/POST /api/v1/system/system-tuning`，使用资源版本、共享写锁、持久化维护任务和危险操作审计；首个项目失败即停止，后续项目不会继续执行。

### Changed

- 发布镜像固定 `kejilion/sh@28f89c1b34df4b25e6ef9b144c328fdea75dbac9`，原始脚本 SHA-256 为 `0583f7cd5be1f0bb6ec48d92e2cf224bfabfafada5788658bda4414ba9561229`；脚本提供 `KPANEL_SYSTEM_TUNING_PROTOCOL_VERSION="1"` 非交互协议，并使菜单 66 与 KPanel 共用同一组实现。
- LinuxMirrors 与网络参数脚本使用固定提交和 SHA-256 下载；菜单 66 的“开放所有端口”改为调用带资源版本、共享锁和回执的事务化防火墙 `open-all` 动作，不再只显示成功文案。

### Upgrade Notes

- 本版本不新增数据库、端口、Compose 或 systemd 权限迁移；升级本身不会自动执行系统调优，也不会修改 Swap、SSH、Fail2Ban、防火墙、DNS、软件源、时区或内核参数。
- 系统调优包含升级系统、修改 SSH 端口、开放所有端口等高风险宿主机操作，只会在已认证管理员明确提交后执行；回滚 KPanel 不会自动撤销已经完成的宿主机调优，需根据任务审计和系统备份逐项恢复。

## [0.62.2] - 2026-08-11

### Fixed

- 修复手机浏览器把文件下载交给系统下载器后，因系统请求未携带面板 Session Cookie 而下载失败的问题；文件管理器现在先通过已认证请求取得 5 分钟内有效的随机下载 ticket，再由系统下载器使用该短时地址完成下载，并支持 `HEAD`、`Range` 和失败重试。
- 下载 ticket 仅保存在 Panel 内存中，创建仍要求登录、同源与 CSRF 校验，公开地址只接受格式有效的 256-bit 随机凭证；实际文件读取继续经过 Agent 的路径、符号链接、权限、并发与流式传输边界，响应禁止缓存。

### Upgrade Notes

- 本补丁不修改数据库、端口、Compose、Agent systemd 权限或 `kejilion.sh` 协议；升级会重启 Panel，因此尚未使用的短时下载 ticket 会失效，重新点击下载即可，可直接回滚至 `v0.62.1` 并保留现有配置和数据。

## [0.62.1] - 2026-08-11

### Fixed

- 修正 Release 运行时镜像契约，使其与已发布的 `kejilion/sh@e9c3078eb516b05f9df6d2a9294cf3b226ca02bd` 及 SHA-256 `147f624c479931c21b7d92392ff3e3a1a58b19bea4f98741f4ec114ab933546a` 完全一致；此前 `v0.62.0` 在镜像发布前被门禁拦截，未公开 Release、未推送正式镜像、未进入生产。

### Upgrade Notes

- 本补丁包含 `0.62.0` 标记中的 SSH 防御管理、空磁盘集合兼容和递归锁修复；无新增数据库、端口或 Compose 迁移，升级本身不会执行任何 Fail2Ban 写操作。

## [0.62.0] - 2026-08-11

### Added

- 概览“基础系统设置”新增轻量 SSH 防御管理：严格以 `kejilion.sh` 为真源，支持查看 Fail2Ban SSH jail 状态、启停或卸载、温和/标准/严格三档策略、封禁 IP 查询与单个/全部解封、可信 IP/CIDR 管理及最近 20 条事件。
- SSH 防御使用独立 typed Agent/Panel API、资源版本、共享写锁、固定动作、危险操作审计和持久化长任务，不扩展旧的通用系统动作接口。

### Changed

- 发布镜像固定 `kejilion/sh@e9c3078eb516b05f9df6d2a9294cf3b226ca02bd`，原始脚本 SHA-256 为 `147f624c479931c21b7d92392ff3e3a1a58b19bea4f98741f4ec114ab933546a`；脚本提供 `KPANEL_F2B_MANAGER_PROTOCOL_VERSION="1"` 非交互协议并负责配置验证、回读、备份和失败回滚。

### Fixed

- 修复无磁盘记录时 Agent 将 `disks` 序列化为 `null`、导致概览数据归一化失败的问题；空集合现在稳定返回 `[]`。
- 修复 SSH 防御 Manager 写动作在持有共享锁时再次读取带锁维护状态而递归锁死的问题；写事务改用无重复加锁的真实状态读取路径。

### Upgrade Notes

- 本版本不新增数据库、端口或 Compose 迁移；应用更新会同步 Panel、Agent 和受管 `kejilion.sh`，旧脚本缺少 Manager protocol v1 时 SSH 防御能力保持禁用。
- 发布和升级本身不会启用、停用或卸载 Fail2Ban，也不会修改封禁、信任地址或防御策略；这些宿主机写操作只在已认证管理员明确操作后执行。
- 可回滚至 `v0.61.4` 并保留面板数据；回滚 Panel/Agent 不会撤销管理员已主动完成的 Fail2Ban 配置、封禁或信任地址变更，这些宿主机状态须按审计与恢复快照单独还原。

## [0.61.4] - 2026-08-11

### Fixed

- 修复正式 systemd 沙箱会移除 `ss` 读取 socket 进程归属所需权限、导致端口占用全部降级为“未知程序”的问题；Agent 单元现在保留 `CAP_SYS_PTRACE`，端口列表可展示真实程序名和 PID。
- KPanel 自更新会在切换 Agent、镜像或配置前，用当前镜像和 Docker 网络真实验证临时端口发布；Docker 防火墙链异常时提前停止并保持当前服务不变，避免新版本与自动回滚同时无法启动。
- 发布镜像继续固定 `kejilion/sh@d82f043aa95064235b2bfe370e25e141cd75c321`，原始脚本 SHA-256 为 `40a9d77aa89d53a4e360026a6d0698622a248d01f059a1c92299dc56068d14f2`。

### Upgrade Notes

- 本补丁为 Agent systemd 单元新增 `CAP_SYS_PTRACE`，仅供固定只读 `ss` 适配器取得 socket 的程序名和 PID；没有新增通用 ptrace、进程内存或任意命令接口。
- 应用市场安装/更新契约同步变化；不修改数据库、端口、Compose 或 `kejilion.sh` 协议，可从 `0.61.2` 或 `0.61.3` 直接更新并保留现有配置和数据回滚。

## [0.61.3] - 2026-08-11

### Fixed

- 改善端口占用查看的可读性：从可信 `ss` 记录中提取干净的程序名和 PID，按程序分组展示监听端口，并合并同程序、协议、地址和端口的重复记录；原始 socket 元数据保留在折叠的技术详情中。
- 发布镜像继续固定 `kejilion/sh@d82f043aa95064235b2bfe370e25e141cd75c321`，原始脚本 SHA-256 为 `40a9d77aa89d53a4e360026a6d0698622a248d01f059a1c92299dc56068d14f2`。

### Upgrade Notes

- 本补丁不修改 `kejilion.sh` 真源、协议、数据库、端口、Compose 或权限；可从 `0.61.2` 直接更新并保留现有配置和数据回滚。

## [0.61.2] - 2026-08-11

### Fixed

- 修复无 SSH 公钥账户返回空密钥集合时，账户管理弹窗在加载完成后因历史 `null` 数据触发前端渲染错误并消失的问题；Agent 现在稳定返回数组，Web API 边界同时兼容旧响应。
- 发布镜像继续固定 `kejilion/sh@d82f043aa95064235b2bfe370e25e141cd75c321`，原始脚本 SHA-256 为 `40a9d77aa89d53a4e360026a6d0698622a248d01f059a1c92299dc56068d14f2`。

### Upgrade Notes

- 本补丁不新增数据库、端口、Compose、权限或 `kejilion.sh` 协议迁移；可从 `0.61.1` 直接更新，并保留现有配置和数据回滚。

## [0.61.1] - 2026-08-11

### Fixed

- 修复账户状态协议在 `/etc/passwd` 含有不符合协议用户名语法的历史服务账户时，`total` 仍统计该账户但记录被过滤，导致 Agent 拒绝账户快照的问题；现在总数只统计协议可表示并实际返回的账户。
- 发布镜像固定 `kejilion/sh@d82f043aa95064235b2bfe370e25e141cd75c321`，原始脚本 SHA-256 为 `40a9d77aa89d53a4e360026a6d0698622a248d01f059a1c92299dc56068d14f2`。

### Upgrade Notes

- 本补丁不新增数据库、端口、Compose 或 API 迁移；从 `0.60.2` 或 `0.61.0` 可直接更新，并可保留现有面板数据回滚。

## [0.61.0] - 2026-08-11

### Added

- 概览“网络工具”新增端口占用查看和限流自动关机；端口状态按打开弹窗时通过可信脚本执行有界 `ss` 读取，限流仅接受固定双向 GiB 阈值、月重置日及启用/停用动作。
- 概览“基础系统设置”新增账户管理，可管理人类/系统账户、密码、SSH 公钥、三种角色及 SSH 登录策略，并支持在同一事务中创建替代管理员后禁用 Root。

### Changed

- 网络与账户写入统一使用 `kejilion.sh` protocol v1、共享宿主机写锁、资源版本、写后回读、失败回滚和宿主可见恢复快照；密码和公钥正文只通过有界单行 stdin 传输，不进入脚本 argv、回执或审计。
- 发布镜像固定 `kejilion/sh@a01067d63676b36c6067275d03a4827a7cb142bd`，原始脚本 SHA-256 为 `d9d9aa70de2c440f557c4db433bf0435ced813e46cb1acbb5c96f8ff601d8181`。

### Fixed

- 修复账户资源版本把随机临时文件名纳入摘要、导致未变更状态连续读取仍产生不同版本并阻断写操作的问题，并增加稳定性回归测试。

### Upgrade Notes

- 本版本不新增数据库、端口或 Compose 迁移；升级会按既有应用生命周期更新 Panel、Agent 和受管 `kejilion.sh`，旧固定脚本缺少两个 protocol v1 标记时相关能力保持禁用。
- 限流达到任一阈值会关机，月重置日会重启；SSH 策略、Root 状态、账户删除和角色调整会直接修改宿主机。执行前应确认登录恢复路径和主机备份。
- 可回滚至 `v0.60.2` 并保留面板数据；回滚 Panel/Agent 不会撤销管理员已经主动完成的账户、SSH、限流脚本或 crontab 变更，这些宿主机状态须按审计与恢复快照单独还原。

## [0.60.2] - 2026-08-10

### Added

- 体检项目列表会标记已经成功或失败完成过的项目，方便区分已测与未测项目；移除重复的最近体检区块，让工作区集中展示当前项目和终端输出。

### Fixed

- 修复紧凑桌面窗口中体检交互终端的输出区挤压脚本输入区、导致输入区不可见的问题。

### Upgrade Notes

- 本版本不新增 API、端口、数据库、Compose、Agent 权限、`kejilion.sh` 协议或应用市场安装契约；可直接回滚至 `v0.60.1` 并保留现有数据和配置。

## [0.60.1] - 2026-08-10

### Fixed

- 修复部分 OpenAI 兼容、Anthropic 和 Gemini Provider 不接受组合式工具 Schema，导致 Docker 维护工具无法正常注册或调用的问题；Provider 侧改用可移植的扁平对象 Schema，继续保留鉴权、审批、固定动作路由、结构化输入、审计和 Agent 技术校验边界。
- 修复桌面窗口高度较小时，多主机终端和应用脚本终端的输入区可能被终端画布挤出可视范围的问题；终端区域现在会正确收缩并保持输入区可见。

### Upgrade Notes

- 本版本不新增 API、端口、数据库、Compose、Agent 权限、`kejilion.sh` 协议或应用市场安装契约；可直接回滚至 `v0.60.0` 并保留现有数据和配置。

## [0.60.0] - 2026-08-10

### Added

- 桌面右侧服务器监控在网络指标中同时展示实时速率与累计接收、累计发送流量，直接复用概览页已有的宿主机网络统计，不增加请求或轮询。

### Fixed

- 统一桌面监控三行指标的固定高度和间距，并压缩累计流量副行，避免网络指标加入累计值后挤压相邻内容。
- 修复桌面窗口中的多主机终端、应用交互终端和体检终端依赖外层滚动的问题；内容改为占满窗口剩余高度，聚焦输入框时不再带动窗口滚动。
- 修复窄桌面窗口中终端主机列表占用过多高度，导致终端画布和输入区被压缩的问题。

### Upgrade Notes

- 本版本只有桌面监控与终端布局变化，不新增 API、端口、数据库、Compose、Agent 权限、`kejilion.sh` 协议或应用市场安装契约；可直接回滚至 `v0.59.0` 并保留现有数据和配置。

## [0.59.0] - 2026-08-10

### Added

- 概览“基础系统设置”新增本地 Hosts 和 root 定时任务管理，直接读取宿主机真实内容，并支持按精确行新增、修改和删除。
- 概览“网络工具”新增网卡状态与防火墙管理；支持网卡即时启停，以及端口、IPv4/CIDR、Ping 和 DDoS 的固定防火墙动作。

### Changed

- 四类系统资源写入统一使用 `kejilion.sh` system-resource v3 协议，增加资源版本冲突检测、跨进程锁、写后回读、持久化和失败回滚；旧脚本或权限条件不足时仅展示真实状态，不开放写入。
- 发布镜像固定 `kejilion/sh@3972217d4d4a51d473b7375f5b850870e066be92`，原始脚本 SHA-256 为 `90da9f13da056b133079fa6fcb673004f377a6b2c0e69e01015040f234e51921`。

### Upgrade Notes

- 本版本没有数据库、端口或 Compose 迁移；升级会按既有应用生命周期更新受管 `kejilion.sh` 和 Agent，现有面板数据、Hosts、Crontab、网卡及防火墙状态保持不变。
- 防火墙“全部开放/关闭”会沿用脚本的高影响语义，清空 filter 表规则和自定义链后重建基础规则；其他动作也会持久化当前规则，执行前应确认云安全组和现有防火墙策略。
- 可回滚至 `v0.58.0`；回滚 Panel/Agent 不会撤销管理员已经通过本版本主动完成的 Hosts、Crontab、网卡或防火墙修改。

## [0.58.0] - 2026-08-10

### Added

- AI 输入区新增统一的权限模式、思考强度和模型选择菜单；模型较多时可搜索，并展示工具调用、图像输入和思考能力标记。
- 网站管理页新增“终端管理”入口，通过现有受控终端会话启动 `kejilion.sh` 的固定 `k web` 原生菜单，并处理会话上限、启动失败和关闭竞态。

### Changed

- AI 任务运行中，空输入的主操作按钮可直接停止本轮；输入文字或附件时继续追加到当前任务，并显示明确的运行状态和失败反馈。

### Fixed

- 修复手机全屏终端多出空白网格行、导致输入区被推离视口的问题。
- 修复经典模式顶部栏的桌面入口图标在手机宽度下未与 44 像素按钮底框居中的问题。
- 修复桌面模式体检窗口中“最近体检”区块被剩余高度推远，以及终端自动聚焦输入框时外层窗口滚到底部的问题。
- 加固 AI 选择菜单在手机宽度下的定位和可视范围，避免权限与思考菜单越出屏幕。

### Upgrade Notes

- 无数据库结构、端口、Compose、Agent 权限、`kejilion.sh` 协议或应用市场配置迁移；网站终端管理复用现有本机终端 API 和 `k web` 命令。可直接回滚至 `v0.57.0` 并保留现有数据和配置。

## [0.57.0] - 2026-08-10

### Added

- 桌面窗口支持拖到顶部最大化、拖到左右边缘半屏贴靠，并在释放前显示低成本品牌色目标预览；从贴靠状态拖离时恢复原浮动尺寸。
- 桌面模式按应用记忆用户最后一次主动调整的窗口宽高；记录有界保存在当前浏览器本地，不包含页面 URL、文件目录、会话参数或脚本应用标识，并会按当前视口安全校正。

### Fixed

- 修复手机和平板上桌面图标依赖双击、右键菜单难以调用的问题；触摸或手写笔可单击打开、长按调出原菜单，滑动、取消和多指手势不会误触。
- 修复窄屏桌面窗口超出安全区、任务栏切换受阻的问题；移动视口使用单窗口工作区，同时保留桌面鼠标的双击和右键习惯。
- 加固窗口拖拽的指针所有权、丢失捕获恢复和每帧合并更新，避免多指或高频移动造成窗口跳动和冗余状态写入。

### Upgrade Notes

- 无数据库结构、端口、Compose、Agent 权限、`kejilion.sh` 协议或应用市场配置迁移；可直接回滚至 `v0.56.0` 并保留现有数据和配置。浏览器本地的窗口尺寸记录在回滚后会被旧版安全忽略。

## [0.56.0] - 2026-08-10

### Added

- 桌面任务栏窗口项新增完整标题提示与右键关闭操作，并复用窗口原有关闭流程处理动画、焦点和状态清理。
- 新增紧凑页面标题、页面操作位置、手机竖屏和窄桌面窗口布局契约，覆盖应用、集群、体检、Docker、文件、监控、终端等核心工作区。

### Changed

- 页面可见标题统一交由经典外壳或桌面窗口承载，刷新、新建和管理操作移入对应内容工具栏，减少重复标题占用的垂直空间。
- 重排应用市场、集群、体检与多主机终端工作区；体检命令和终端主机支持折叠侧栏，应用卡片与密集控制区会随窗口及手机宽度自适应。
- 补齐 14 个核心路由的英文界面文案，并扩展 i18n 门禁对动态占位语句和运行时共享文案的识别。

### Fixed

- 修复 360–430 像素手机竖屏和窄桌面窗口中的工具栏挤压、卡片溢出、任务栏不可滚动及页面级横向溢出问题。
- 修复概览服务详情选择器依赖通用子元素结构的问题，改用显式语义类保持布局稳定。

### Upgrade Notes

- 无数据库结构、端口、Compose、Agent 权限、`kejilion.sh` 协议或应用市场配置迁移；可直接回滚至 `v0.55.5` 并保留现有数据和配置。

## [0.55.5] - 2026-08-09

### Fixed

- 统一桌面图标在深浅主题下的选中语义；浅色模式的名称底框同样使用品牌绿色，同时保留高对比深色文字。

### Upgrade Notes

- 无数据库结构、端口、Compose、Agent 权限、`kejilion.sh` 协议或应用市场配置迁移；可直接回滚至 `v0.55.4` 并保留现有数据和配置。

## [0.55.4] - 2026-08-09

### Fixed

- 修正桌面图标名称在浅色底框内因不对称内边距和字体基线产生的垂直偏移；中文与英文名称统一使用固定高度和对称行框居中。

### Upgrade Notes

- 无数据库结构、端口、Compose、Agent 权限、`kejilion.sh` 协议或应用市场配置迁移；可直接回滚至 `v0.55.3` 并保留现有数据和配置。

## [0.55.3] - 2026-08-09

### Fixed

- 修复浅色桌面中脚本终端窗口为外层滚动条预留白色槽位、导致内容区右侧出现竖向白条的问题；终端窗口改由内部滚动区域独立管理滚动。

### Upgrade Notes

- 无数据库结构、端口、Compose、Agent 权限、`kejilion.sh` 协议或应用市场配置迁移；可直接回滚至 `v0.55.2` 并保留现有数据和配置。

## [0.55.2] - 2026-08-09

### Fixed

- 提升桌面图标与名称清晰度；动态应用和网站图标不再叠加白色高光遮罩，浅色主题名称移除发白文字阴影并增强前景对比。
- 降低浅色桌面的壁纸漂白蒙版、暗角和光晕强度，恢复原始壁纸的纹理与色彩层次；深色主题处理及原始壁纸文件保持不变。

### Upgrade Notes

- 无数据库结构、端口、Compose、Agent 权限、`kejilion.sh` 协议或应用市场配置迁移；可直接回滚至 `v0.55.1` 并保留现有数据和配置。

## [0.55.1] - 2026-08-09

### Fixed

- 修复普通桌面窗口内部跳转无法使用浏览器原生前进/后退的问题；文件目录、历史监控、进程管理器与监控缩放会按窗口恢复历史，不再重复显示窗口内返回箭头。
- 桌面不再显示重复的 KPanel 自身应用及其代理站点入口；应用市场中的 KPanel 更新与管理能力保持不变，避免自身递归嵌入或安全策略报错。

### Upgrade Notes

- 无数据库结构、端口、Compose、Agent 权限、`kejilion.sh` 协议或应用市场配置迁移；可直接回滚至 `v0.55.0` 并保留现有数据和配置。

## [0.55.0] - 2026-08-09

### Added

- 新增服务器 root 本地管理员密码恢复：`paneld reset-password` 通过交互式终端隐藏输入并二次确认，只更新认证状态、撤销旧 Session，默认保留 TOTP；登录页提供可复制的安全操作指引。

### Security

- 密码恢复不新增公网 API，要求服务器本地交互式终端和已停止的 Panel 状态锁；继续使用现有 Argon2id 参数原子持久化，并记录审计事件。只有显式指定 `--disable-2fa` 才会清除 TOTP 与恢复码。

### Upgrade Notes

- 无数据库结构、端口、Compose、Agent 权限、`kejilion.sh` 协议或应用市场配置迁移；可直接回滚至 `v0.54.0` 并保留现有数据和配置。已经执行的密码恢复会继续保留新密码和已撤销的 Session，不会因代码回滚而恢复旧凭据。

## [0.54.0] - 2026-08-09

### Fixed

- 修复桌面窗口内部跳转后无法返回的问题；窗口标题栏现提供统一后退按钮并支持 `Alt + ←`，文件目录、历史监控、进程管理器及监控缩放均使用当前窗口的独立导航历史。

### Changed

- 概览中的历史监控与进程管理快捷入口调整为与对应数据卡一致的顺序。

### Upgrade Notes

- 无数据库、端口、Compose、Agent 权限、`kejilion.sh` 协议或应用市场配置迁移；可直接回滚至 `v0.53.0` 并保留现有数据和配置。

## [0.53.0] - 2026-08-09

### Added

- 网站列表在域名下方显示从首页安全抓取并缓存的网站名称；名称与 favicon 共用受限的本机 Nginx 抓取链路，缺失或失败时保持原列表展示。
- 桌面网站图标按“用户重命名、网站名称、域名”的顺序选择标签；favicon 不可用时显示按站点稳定配色的字母图标与网站角标。

### Changed

- 桌面脚本应用终端将应用图标和“应用名的脚本终端”合并到窗口标题栏，移除终端内容区重复的应用信息栏，为脚本交互释放更多空间。
- AI 助手的 Docker 维护工具按动作声明必填字段，并在修改现有容器、镜像、网络、存储卷、备份或 daemon 配置前读取最新真实状态与资源版本。

### Fixed

- 修复进程管理页面在桌面窗口中未使用标准页面容器、导致内容间距与其他管理页面不一致的问题。
- 修复 AI Docker 维护参数缺失、资源已变化或指定资源不存在时错误分类不明确的问题；模型现在会按参数错误或状态冲突重新读取并规划，不再用清理悬空镜像替代精确镜像删除。

### Security

- 网站名称只从已发现站点的本机 Nginx 首页读取，经过实体解码、空白归一化、控制字符过滤和长度限制，并继续复用 favicon 链路的并发、响应体、重定向与缓存边界。
- Docker 写操作继续经过固定动作白名单、审批、Agent 二次校验和 `resourceVersion` 冲突保护；本次没有新增任意命令、任意 URL 或 Docker Socket 浏览器入口。

### Upgrade Notes

- 无数据库、端口、Compose、Agent 权限、`kejilion.sh` 协议或应用市场配置迁移；网站外观缓存属于可重建派生数据，可直接回滚至 `v0.52.0` 并保留现有数据和配置。

## [0.52.0] - 2026-08-08

### Added

- 新增轻量进程管理器：从概览或桌面任务栏进入，可实时搜索、排序和查看宿主机进程，并在确认后发送 `SIGTERM` 或 `SIGKILL`；查询、返回量、刷新频率和信号动作均有固定边界。
- 桌面上的脚本管理型应用可在独立窗口中启动并恢复原生交互终端，不再先跳转应用市场详情。

### Fixed

- 修复桌面 AI 窗口滚动可能带动外层页面的问题，消息跟随现在只作用于窗口内部滚动区域。
- 修复应用代理网站与对应已安装应用同时生成桌面图标的问题；同一应用的代理入口只保留应用图标。

### Upgrade Notes

- 无数据库、端口、Compose、Agent 权限、`kejilion.sh` 协议或应用市场配置迁移；进程管理器复用现有 Agent 与系统动作权限，可直接回滚至 `v0.51.0` 并保留现有数据和配置。

## [0.51.0] - 2026-08-08

### Added

- 桌面应用图标支持打开对应的应用市场详情；没有 Web 地址但提供脚本管理能力的已安装应用，也可直接从桌面进入原生脚本管理任务。
- 桌面网站图标支持持久化重命名和恢复域名，并继续展示已启用但处于警告或未知状态的网站入口。

### Changed

- 桌面应用与网站入口增加短时有界缓存和失败回退，刷新暂时失败时保留最近一次成功结果，显式刷新仍会重新读取真实状态。

### Fixed

- 修复桌面模式刷新或懒加载时短暂显示经典界面的问题。

### Upgrade Notes

- 无数据库、端口、Compose、Agent 权限、`kejilion.sh` 协议或应用市场配置迁移；可直接回滚至 `v0.50.0` 并保留现有数据和配置。

## [0.50.0] - 2026-08-08

### Added

- 终端主机选择区支持收起与展开，记住本机偏好，并补充移动端布局、减少动态效果和无障碍状态支持。

### Fixed

- 修复概览时区优先读取旧 `/etc/timezone`、无法正确跟随宿主机 `/etc/localtime` 来源的问题；无法识别时不再误报为上海时区。
- 修复桌面上动态生成的已安装应用和网站图标未完整填充入口图块的问题。

### Upgrade Notes

- 无数据库、端口、Compose、Agent 权限、`kejilion.sh` 协议或应用市场配置迁移；可直接回滚至 `v0.49.0` 并保留现有数据和配置。

## [0.49.0] - 2026-08-08

### Added

- 新增桌面模式：提供桌面、任务栏、开始菜单、时钟、壁纸和可拖动、最小化、最大化及恢复的多窗口工作区，并保留原有管理视图作为窗口内容。
- 已安装应用与网站会自动生成桌面入口；概览、文件、终端、Docker、网站、应用、任务、集群、监控、诊断、环境和 AI 等管理页面可在桌面窗口中打开并保持独立路由状态。
- 新增桌面服务器监控组件，集中展示 CPU、内存、磁盘和网络状态，并补充窗口几何、路由、手势、入口、时钟、监控及视觉契约测试。

### Changed

- 加固桌面窗口生命周期：窗口隐藏或关闭时暂停页面轮询并正确释放终端等资源，恢复窗口时重新同步真实状态，避免后台重复请求和残留连接。

### Upgrade Notes

- 无数据库、端口、Compose、Agent 权限、`kejilion.sh` 协议或应用市场配置迁移；桌面模式是兼容性界面扩展，可直接回滚至 `v0.48.4` 并保留现有数据和配置。

## [0.48.4] - 2026-08-08

### Fixed

- 修复内置 AI 在 Agent 返回带安全问题码的可恢复业务拒绝时直接终止任务的问题；现在会把脱敏后的拒绝结果交给模型重新规划，并限制相同调用和单轮失败次数，避免重复执行。
- 修复部分 OpenAI-compatible Responses 接口要求历史消息包含稳定 `id` 时请求失败的问题；仅在首次请求尚未产生输出且明确返回缺少消息 ID 时执行一次兼容重试。
- 修复通用弹窗打开后焦点停留在背景页面、键盘焦点可逃出弹窗及多层弹窗错误响应 `Esc` 的问题；关闭后会恢复到原触发控件。
- 修复概览服务状态卡片中图标元素被通用详情布局选择器误匹配的问题，避免图标内部结构影响名称与版本信息排列。

### Upgrade Notes

- 无数据库、端口、Compose、Agent 权限、`kejilion.sh` 协议或应用市场配置迁移；保留现有 AI Provider、会话和主机配置，可直接回滚至 `v0.48.3` 并保留数据。

## [0.48.3] - 2026-08-06

### Fixed

- 修复概览 CPU 采样与页面其他请求并发时容易受到干扰的问题：系统摘要先独立完成区间采样，再加载 Agent、Docker、站点和应用数据。
- 修复宿主机网络累计流量重复包含 loopback、Docker、容器、VPN 等虚拟接口的问题；优先按 IPv4/IPv6 默认路由接口统计，无路由信息时排除已知虚拟接口。
- 修复磁盘已用容量错误包含文件系统保留块的问题，并按 `df` 的 `Bfree`/`Bavail` 口径计算已用容量与使用率。
- 修复概览首次网络采样显示误导性 `0 B/s` 的问题；首次及计数回绕时显示“等待下一次采样”，获得有效相邻样本后再显示实时速率。

### Upgrade Notes

- 无数据库、端口、Compose、Agent 权限、`kejilion.sh` 协议或应用市场配置迁移；升级后等待下一轮概览刷新即可获得网络速率，可直接回滚至 `v0.48.2` 并保留现有数据。

## [0.48.2] - 2026-08-06

### Changed

- 优化历史监控的容器对比区：已选容器改为紧凑横向摘要，列表与曲线支持悬停和键盘聚焦联动，长容器名称与镜像名称保持可读且不挤压指标列。

### Upgrade Notes

- 无数据库、监控数据格式、端口、Compose、Agent 权限、`kejilion.sh` 协议或应用市场配置迁移；可直接回滚至 `v0.48.1` 并保留现有数据。

## [0.48.1] - 2026-08-06

### Fixed

- 修复浏览器前进、后退无法恢复历史滚动位置，以及同一页面仅切换查询参数时意外回到顶部的问题。

## [0.48.0] - 2026-08-06

### Added

- 历史监控的框选缩放、逐级返回和重置会同步到浏览器历史，浏览器前进、后退和刷新均可恢复同一监控时间窗口。
- 历史监控支持同时选择最多 5 个容器进行 CPU、内存、网络和磁盘对比，并保留选择、颜色和指标展示偏好。

### Changed

- 优化概览、网站、任务、Docker 和应用外壳在桌面与手机端的间距、滚动边界、标签和操作区布局，并补充布局回归测试。
- 文件管理成功进入目录时会写入规范化路由，浏览器前进、后退可恢复目录；首次列表上限由 500 调整为 100，并继续使用现有分页加载更多内容。
- 概览的可选请求改为并行加载；应用、Docker、文件和监控页面缓存高频派生结果，限制时间戳解析缓存，并将历史监控默认窗口由 24 小时调整为 6 小时。
- 增加跨智能体项目管理、独立 worktree、单写入者、交付包、发布冻结和冲突恢复规范，以及对应 AI Task Issue 与可复用工作流入口。

### Fixed

- 修复 systemd 257 等版本中 `ProtectSystem=strict` 无法被 `ReadWritePaths=/` 可靠覆盖，导致文件管理在 `/etc`、`/var/local` 等宿主机目录创建内容时返回 `read-only file system` 的问题；Panel 状态目录继续通过 `ReadOnlyPaths` 独立保护。

### Security

- Agent systemd 沙箱改用 `ProtectSystem=false`，使已经通过 KPanel 鉴权、CSRF、Agent token 与文件 API 路径校验的文件管理功能可以按宿主机原有权限写入系统目录；Panel 状态目录仍通过 `ReadOnlyPaths` 保护，其他 systemd 加固项保持不变。
- 本次不新增监听端口、外联目标、数据库迁移、Compose 挂载或 `kejilion.sh` 协议。

### Upgrade Notes

- 标准应用更新会替换 Agent systemd 单元并重启服务，无需手工迁移数据库或配置；升级后应复核 `/etc`、`/var/local` 的受控写入以及 Panel 状态目录的只读保护。
- 可回滚至 `v0.47.0` 镜像和对应应用配置；数据库与数据目录格式保持兼容，但旧版文件管理会再次受到 systemd 257 根文件系统只读限制。

## [0.47.0] - 2026-08-05

### Added

- 历史监控所有趋势图支持横向框选同步放大；页面先显示当前数据的有界预览，再按所选区间读取更细数据，并可逐级返回或重置到当前时间窗口。

### Fixed

- 历史监控三网延迟移除重复图例与成功率展示，恢复紧凑的九路线路开关；悬停提示缩短同组名称与数值间距，并扩大双列间隔。

### Security

- 框选查询只接受固定时间范围内成对出现且不可重复的 RFC3339 `start/end`；服务端校验时间顺序、根窗口、原始或小时汇总保留期，并继续限制扫描字节、响应点数和并发查询槽。
- 本次不新增监听端口、外联目标、宿主机写入、Panel 挂载、Agent 权限、任意文件路径或任意 URL 入口。

### Performance

- 浏览器只保留根响应和当前响应，缩放栈最多 16 组时间参数；拖动过程不请求，松手后最多发起一次可取消查询。
- 32 容器与九线路最大夹具中，最近 6 小时、60 天前 24 小时和 180 天前 7 天框选响应分别约 `1.15 MiB`、`0.39 MiB` 和 `2.60 MiB`，均低于既有 `8 MiB` Panel→Agent 上限；Monitoring 路由约 `9.8 KiB gzip JS`。

### Upgrade Notes

- 无数据库、监控文件格式、端口、Compose、Agent 权限、`kejilion.sh` 协议或应用市场配置迁移；现有原始日分片与长期小时汇总直接兼容。
- 可直接回滚至 `v0.46.0`，数据目录保持不动；旧版会忽略仅用于查询的框选参数。

## [0.46.0] - 2026-08-05

### Added

- 历史监控新增 3 个月、6 个月和 12 个月窗口；Agent 将主机、容器与三网延迟按小时有界汇总，长期视图分别按 6、12、24 小时展示趋势。
- 长期 CPU 趋势同时显示峰值与按样本数加权的平均值，三网延迟补充桶内成功率，历史存储卡片合并展示原始数据与长期趋势占用。

### Fixed

- 小时汇总文件暂时不可写或达到单分片上限时，失败会保留在监控状态中，但累加器继续进入下一小时，避免一次写入失败永久阻断后续汇总。
- 长期容器记录为控制体积不保存重复镜像元数据；页面在列表和详情中统一显示明确的缺省提示。
- 修复低于 `1 B/s` 的网络或磁盘速率图表纵轴出现无效单位的问题。

### Security

- 长期范围仍是固定枚举，只读取 Agent 权限为 `0700/0600` 的既有监控目录；月分片限制 `16 MiB`、长期目录限制 `128 MiB`、保留期限制 365 天，并继续拒绝符号链接与非普通文件。
- 长期查询独占两个既有查询槽，单行限制 `256 KiB`，不新增监听端口、外联目标、Panel 宿主机挂载、Agent 权限或任意文件路径入口。

### Performance

- 32 容器、12 个月最大数据基准的响应约 `6.08 MiB`，低于既有 `8 MiB` Panel→Agent 响应上限；在 i5-12600KF/Windows amd64 上单次查询约 `1.01 s`，单次 32 容器小时汇总约 `36.6 μs`。
- 原始日分片继续限制 `128 MiB`，长期月分片另限制 `128 MiB`；最大年度小时汇总投影和所有返回序列、桶数量、扫描字节均由测试固定上限。

### Upgrade Notes

- 升级后在既有 `/var/lib/kejilion-panel/monitoring` 目录逐小时生成 `hourly-YYYY-MM.jsonl`；不会回填升级前已删除的历史数据，3/6/12 个月窗口会从升级后的采样开始逐步补齐。
- 无数据库、端口、Compose、Agent 权限、`kejilion.sh` 协议或应用市场配置迁移。可直接回滚至 `v0.45.2`；旧版会忽略新增小时分片，如需保留长期趋势可在回滚前备份监控目录。

## [0.45.2] - 2026-08-05

### Changed

- 主机终端将会话标签和终端操作合并为同一工具栏，网页铺满时保留标签切换、回到顶部和恢复窗口操作。
- 主机终端和应用交互终端统一使用网页视口铺满，不再请求浏览器或操作系统原生全屏，避免遮蔽浏览器导航与退出入口。

### Fixed

- 切换终端会话和网页铺满状态后主动重新适配当前终端尺寸；关闭最后一个主机终端会话时同步恢复普通页面布局。

### Security

- 本次只调整前端布局与终端尺寸刷新，不改变右键剪贴板手势、OSC 52 拦截、终端会话鉴权、Origin/CSRF、Agent 或后端命令边界。

### Upgrade Notes

- 无数据库、Compose、Agent 权限、`kejilion.sh`、应用市场配置或网络迁移；可直接回滚至 `v0.45.1` 镜像，数据目录保持不动。

## [0.45.1] - 2026-08-05

### Added

- 主机终端和应用交互终端新增右键菜单，可复制当前选区、读取用户主动选择的剪贴板内容进行粘贴，或选择全部终端缓冲区；同时保留 `Ctrl+Shift+C`、`Ctrl+V` 和普通 `Ctrl+C` 的既有终端语义。
- 两类终端统一使用“回到顶部”和全屏工具栏；全屏优先使用浏览器原生 API，不可用时回退视口全屏，并支持 `Esc` 退出和尺寸重新适配。

### Fixed

- 将全屏状态收敛到实际终端组件，避免体检、环境任务和主机终端同时出现外层弹窗与内层终端的重复全屏控制。

### Security

- 继续拦截远端 OSC 52 剪贴板写入；复制和粘贴只由明确的右键菜单或键盘手势触发，粘贴内容仍通过现有固定终端输入 API、会话鉴权、Origin/CSRF 与 Agent 边界，不新增任意后端命令入口。

### Upgrade Notes

- 无数据库、Compose、Agent 权限、`kejilion.sh`、应用市场配置或网络迁移；可直接回滚至 `v0.45.0` 镜像，数据目录保持不动。

## [0.45.0] - 2026-08-05

### Added

- 体检工作区新增全屏模式，交互终端和普通日志均支持按钮切换、`Esc` 退出及全屏尺寸自适应。
- 概览中的 SSH 端口修改改用固定的 `kejilion.sh ssh-port` 非交互协议；Agent 负责端口校验、执行前配置备份和结果回读，脚本继续复用原有单端口修改业务，并在返回成功前验证 SSH 配置语法与新端口监听。

### Fixed

- 隔离体检、应用交互终端和主机终端的滚动边界，避免滚动终端历史时带动整个页面。
- 历史监控三网延迟的悬停提示在九路同时显示时改用紧凑双列布局，避免底部线路信息被图表区域裁掉。
- 集群主机复用概要页的操作系统识别规则；已知发行版显示对应图标，无法识别时回退 Linux 企鹅图标。
- 完善初始化与登录的密码、用户名及错误提示，连接重试后按真实会话状态跳转，并补齐表单无障碍反馈。
- 修复 `kejilion.sh` 的容器访问规则只处理单一 Docker 地址的问题；多网络容器现在为每个 IPv4 同步写入、识别和清理 `DOCKER-USER` 规则。

### Security

- SSH 端口入口只接受 `1-65535` 的结构化整数，并要求镜像内固定提交与 SHA-256 校验通过的本机脚本；不新增任意命令入口。云安全组仍需由管理员单独放行新端口。
- 登录与初始化改动仅改善现有校验反馈、错误本地化和重试跳转，不改变后端认证策略、会话边界或密码存储格式。

### Upgrade Notes

- 发布镜像内置 `kejilion.sh 4.5.5`（提交 `dd26cf7eb962f985f94773c15f9b643677b4471c`，SHA-256 `cf835ec01a10955e91d49a5653870a8cab33288b32ee5cab40405bd69ef77bf4`）；生命周期升级会按既有契约同步到受管脚本路径。
- 无数据库、Compose、Agent 权限或应用市场配置迁移。可回滚至 `v0.44.4` 镜像；镜像回滚不会自动撤销管理员已经执行的 SSH 端口变更，必要时应使用操作返回的备份路径人工恢复并验证 SSH 配置。

## [0.44.4] - 2026-08-05

### Added

- 历史监控新增 30 天窗口；宿主机、容器与三网延迟按 2 小时桶聚合，每条序列最多返回 361 个包含边界的趋势点。

### Fixed

- 修复 Docker one-shot stats 缺少有效 `precpu_stats` 时，历史容器 CPU 长期显示 `0%` 或累计比值失真的问题；历史采样改用相邻轮次累计计数器差值，容器详情改用双周期 CPU stats。

### Security

- 30 天范围仍是固定枚举，只读取 Agent 私有监控目录；查询保持上下文取消、分片大小、序列数量、响应大小和并发容量限制，不新增端口、权限、外联目标或 Docker Socket 暴露。

### Performance

- 保留期扩展为完整滚动 30 天，单日继续限制 `4 MiB`、历史目录限制 `128 MiB`；最大采样投影约 `94.43 MiB`。30 天查询流式扫描日分片并独占两个查询槽，最大构造响应约 `4.99 MiB`，低于既有 `8 MiB` Panel→Agent 响应上限。

### Upgrade Notes

- 无数据库、端口、Compose、Agent 权限、`kejilion.sh` 协议或应用市场安装契约迁移；现有 JSONL 历史文件直接兼容。升级前已被 7 天保留策略删除的数据不会恢复，30 天窗口会随新采样逐日补齐。
- 可回滚至 `v0.44.3` 镜像。旧版会恢复 7 天保留与 `32 MiB` 目录上限；如需保留超过 7 天的新增历史分片，应在回滚前备份 `/var/lib/kejilion-panel/monitoring`。

## [0.44.3] - 2026-08-05

### Added

- Docker 容器、镜像、网络和存储卷列表新增右键菜单，同时保留显式“更多操作”按钮；可直接查看日志与性能、进入控制台、管理生命周期和外部访问、复制资源标识、更新镜像、管理网络成员或删除资源。
- 容器生命周期新增暂停与继续运行操作，并根据 Docker Engine 的实时状态只展示当前可执行动作。

### Changed

- 登录成功后的控制台过渡优化继续随主线发布，并纳入本版本登录回归，避免重复提交历史实现。

### Security

- Docker 写操作继续经过管理员登录、同源与 CSRF 校验、固定动作白名单、Agent Token、资源版本冲突检测和审计；右键菜单不新增任意命令或绕过现有确认流程。

### Performance

- 右键菜单仅复用当前 Docker 资源快照，不新增轮询、常驻任务或后台请求；关闭菜单时同步释放页面事件监听。

### Upgrade Notes

- 无数据库、端口、Compose、Agent 权限、`kejilion.sh` 协议或应用市场安装契约迁移；现有网站、应用、Docker 资源、AI 数据和登录配置保持不变。需要回滚时可恢复 `v0.44.2` 镜像，数据目录保持不动。

## [0.44.2] - 2026-08-04

### Fixed

- AI 图片和文本附件改用标准 `multipart/form-data` 上传，避免 Base64 JSON 被 ModSecurity 的 `SecRequestBodyNoFilesLimit` 在进入 KPanel 前误拦截；保留原 JSON 协议用于无附件消息和旧客户端兼容。
- 修复实时工具事件使用零值 `createdAt`，导致工具卡片全部堆在文字前面的时间线错误；工具调用、可见进度说明和后续结果现在按真实发生顺序穿插，模型未输出工具前说明时补充简短的非推理进度提示。
- 反向代理或 Provider 返回 HTML 错误页时不再把原始 Nginx 页面直接显示给用户，统一转换为可读的网关或模型能力提示。

### Security

- 不放宽 WAF 全局限制；附件仍由服务端重新识别真实类型，并保持单图 4 MiB、单文本 512 KiB、单消息附件总计 8 MiB、消息请求 12 MiB 的边界。multipart 未知字段、重复内容字段和超限请求继续失败关闭。

### Upgrade Notes

- AI SQLite 迁移新增版本 9，将旧工具调用的无效零值创建时间修复为已持久化的更新时间；Provider、密钥、会话、消息、记忆和流程内容不变。回滚到 `v0.44.1` 时旧版本可忽略该迁移记录。
- 不变更 Compose、端口、Agent 权限、依赖或应用市场安装契约；无需修改 Nginx WAF 镜像中的全局 `SecRequestBodyNoFilesLimit`。

## [0.44.1] - 2026-08-04

### Fixed

- 新增和同步的模型默认启用图像输入能力；升级时一次性修正 0.44.0 已保存模型的旧能力标记，同步模型不再保留历史 `vision=false` 默认值。
- 修复 Provider 存在遗留待审批 Run 时无法同步模型或删除的问题。待审批任务不再阻止同步；删除 Provider 时会原子取消相关 Run、拒绝未执行工具调用并保留会话历史。
- 修复关闭、中断或遗留活跃 Run 后会话无法删除并提示 `AI run is already active` 的问题。删除会话会先同步取消所属活跃 Run，再清理会话数据。

### Security

- Provider 的 `queued` 和 `running` Run 继续阻止模型变更与删除，避免执行中的模型快照失效；仅未执行的审批可安全取消。会话取消保持用户归属校验，不允许跨用户终止任务。

### Upgrade Notes

- AI SQLite 迁移新增版本 8，将已有模型的 `vision` 标记一次性设为启用；Provider 密钥、会话、消息、记忆和流程保持不变。升级前仍应成对备份 `ai.db*` 与 `ai-secrets.key`。
- 默认能力标记不保证上游模型实际支持图片；不支持图像的模型接收图片时可能返回 Provider 能力错误，可在模型设置中手动关闭。
- 不变更 Compose、端口、Agent 权限、依赖或应用市场安装契约；可回滚至 `v0.44.0`，旧版本会忽略迁移记录 8。

## [0.44.0] - 2026-08-04

### Added

- AI 对话支持最多 4 个附件：PNG、JPEG、WebP、GIF 图片使用模型原生多模态输入，UTF-8 文本、日志、代码和配置文件作为不可信附件进入上下文；模型设置可声明并同步保留图像与原生推理能力。
- 会话新增低、中、高三档思考强度。Run 在启动时固定选择快照，并映射 OpenAI-compatible、Anthropic 和 Gemini 的原生推理参数；不支持原生参数的模型继续使用通用规划强度。

### Changed

- AI 输出采用轻量帧缓冲，减少流式文字突跳；用户上滚后不再被持续拉回底部，并可主动返回最新位置。最终回答依次显示复制、模型和时间，代码块提供独立复制按钮。
- 同一轮的助手可见说明、紧凑工具执行行和最终结论按真实发生时间自然穿插；工具参数与结果按需展开，不再把所有过程卡片统一堆在回答前。
- 输入框合并为单层工作区，模型、思考强度、权限模式与附件入口集中在底部操作栏；桌面与移动布局保持同一交互语义。

### Security

- 附件在服务端重新检测实际类型并限制文件名、数量、单项和总大小；仅接受指定图片或无 NUL 的 UTF-8 文本，图片必须由明确启用视觉能力的模型处理。附件按上下文预算计量，隐藏思维链继续不通过 REST、SSE、日志或审计暴露。

### Performance

- 单图限制 4 MiB、单文本限制 512 KiB、每条消息附件总计限制 8 MiB；流式输出仍使用单帧有界缓冲，不增加常驻服务、Sidecar、端口或前端依赖。

### Upgrade Notes

- AI SQLite 迁移新增模型视觉/推理能力、会话与 Run 思考强度以及消息附件字段；升级前应成对备份 `ai.db*` 与 `ai-secrets.key`。旧版可忽略新增列，但无法使用附件和思考强度；回滚镜像为 `v0.43.2`。
- 不变更 Compose、端口、Agent 权限、应用市场安装契约或 Provider 密钥格式。

## [0.43.2] - 2026-08-04

### Fixed

- 修复部分真实模型为工具调用自动附加 `reason` 后，严格业务参数契约返回 `json: unknown field "reason"`、中断运维工作链的问题。所有已注册工具现在统一声明最多 500 字的可选说明，并在工具入口规范化处理，不再针对单个业务工具做兼容。
- 可纠正的本地工具参数错误现在会在审批和宿主机执行前失败关闭，并作为结构化反馈交给模型在当前轮自行修正；不会再要求用户批准无效参数或直接中断整轮。过程卡片按调用顺序稳定显示在本轮最终结论之前，状态更新不再改变卡片位置，重新打开会话时也会恢复最近一轮过程。

### Security

- `reason` 只作为模型侧兼容元数据，在业务校验、审批分类、审计和宿主机操作前统一剥离；其他未知字段继续严格拒绝。只读工具同步补齐运行时空参数校验，现有审批、安全路径、固定命令和错误脱敏边界不变。

### Upgrade Notes

- 无数据库、依赖、端口、Compose 或应用市场契约变化；回滚镜像为 `v0.43.1`。

## [0.43.1] - 2026-08-04

### Added

- AI 新增 Docker 运行容器资源占用只读工具，可一次读取并比较 CPU、内存、网络、块 IO 与 PID；新增受 File Manager 保护规则约束的目录查询、64 KiB 文本读取和带 `resourceVersion` 的文本写入工具。
- AI 新增宿主机进程 CPU/内存双排行、固定根目录磁盘热点分析、64 KiB 日志尾读、可恢复文件清理，以及固定 `nginx -t` 和“校验后重载”工具；与现有系统清理、站点、容器和备份迁移能力组成通用运维闭环。
- 每个会话新增“手动审批 / 安全自动审批”权限模式；模式在 Run 启动时快照，执行中切换只影响下一轮。

### Fixed

- 修复 `host_docker_task` 向模型声明 `type`、但 Agent 实际严格要求 `action`，导致 Docker 查询误入维护任务并返回 `400 invalid_request` 的问题；Schema 现与 Agent 的全部维护动作及字段保持契约测试一致。
- Docker 资源类问题现在明确路由到只读工具，禁止使用维护任务；Agent problem 响应不再把原始 JSON 和内部详情直接显示在聊天错误卡片中。
- 运维提示词统一为“观察 → 定位 → 最小可回滚修改 → 执行 → 复查”；Nginx、CPU、磁盘和迁移问题会调用现有结构化能力，不增加通用 Shell 或按自然语言硬编码动作。

### Security

- 手动审批模式下所有非只读工具逐次确认；安全自动审批仅放行已分类的常规结构化操作，Docker 维护、删除、容器 exec、终端输入和系统核心操作仍不可自动批准。
- AI 文件工具继续复用现有路径规范化、符号链接防护、保护/只读目录、文本大小、UTF-8、资源版本冲突和审计约束；文件内容不写入审计摘要。
- AI 额外拒绝读取密钥、`.env`、SSH 凭据、私钥、进程环境和 KPanel 数据目录，并拒绝改写 KPanel、Docker、systemd、SSH、PAM、sudo、cron 和系统账号等核心路径；工具结果脱敏补充 Basic 凭据、密码、Token、Secret 与 PEM 私钥。

### Performance

- 进程信息只采样 `/proc` 且不读取命令行；磁盘分析仅允许固定根目录、同一文件系统、不跟随符号链接，最多四个工作线程、20 万条目录项和 8 秒，并限制同一时刻一个扫描任务。

### Upgrade Notes

- `ai.db` 事务迁移新增会话和 Run 的 `approval_mode` 字段，现有会话默认进入 `manual`；旧版本忽略新增列，回滚到 `v0.43.0` 前仍应成对备份 `ai.db*` 与 `ai-secrets.key`。
- 不新增端口、容器、Sidecar、常驻进程、通用 Shell、通用 HTTP 或依赖；回滚镜像为 `v0.43.0`。

## [0.43.0] - 2026-08-04

### Added

- AI 在用户明确表达稳定偏好、成功完成常规写操作或多工具任务后，会异步评估是否形成跨会话记忆或流程；高置信度且通过安全校验的产物在后台自动启用，并可在设置中停用、退休或回滚。

### Changed

- 新建会话直接使用默认模型，第一条用户消息自动生成会话名称；模型选择器移至输入框右下角，运行中切换从下一轮生效。
- 对话输入区改为单层 Codex 风格容器，移除重复边框和手动“沉淀流程”入口。
- 常规应用安装、启停、更新，网站创建/更新，容器启停/重启和诊断可按固定结构化策略自动执行。

### Fixed

- 修复内部 `<tool_result>` 数据被错误保存并显示为用户消息的问题；历史兼容数据也会在 REST、SSE 和前端三层过滤。
- 修复 Run 完成后清空消息并异步重载导致结果短暂闪回欢迎页面的问题。
- 修复同一毫秒写入的会话消息可能因随机 ID 排序而错序或分页不稳定的问题。

### Security

- 删除、卸载、系统核心设置、Docker 维护、容器 exec、交互输入、未知动作和参数解析失败继续逐次确认；没有新增通用 Shell 或通用 HTTP 工具。
- 后台学习要求至少 `0.82` 置信度，并拒绝瞬时指标、一次性状态、敏感值、重复/冲突内容和需要核心确认的流程；流程只能引用本次实际成功且再次通过 dry-run 的注册工具。

### Performance

- 单次普通读取不触发学习请求；符合条件的任务完成后最多增加一次短模型评估，不新增常驻进程、Sidecar 或轮询。
- 后续会话最多加载最近 12 条后台记忆和 8 条后台流程，避免自动学习导致模型上下文无限增长。

### Upgrade Notes

- 无数据库迁移、新依赖、网络、端口或部署配置变更；现有 Provider、会话、消息、记忆和流程保持不变。
- 回滚至 `v0.42.1` 时可继续使用同一 `ai.db` 与密钥文件；`v0.42.1` 会忽略新的自动学习策略，但已生成的兼容记忆和流程仍可读取。

## [0.42.1] - 2026-08-04

### Fixed

- 修复应用市场安装或从 0.40.x 升级到 0.42.0 后，宿主机 Nginx 通过 HTTPS 域名反向代理 KPanel 时因出口网络网关未被识别为可信代理而返回 `421 host_validation_failed` 的问题。
- 安装和更新现在同时迁移内部代理网段与出口网络的宿主机网关地址，现有 `KPANEL_PUBLIC_URL`、端口、数据和自定义环境项保持不变。

### Security

- 仅将 KPanel 专用出口网络的宿主机网关单地址加入可信代理（IPv4 `/32`、IPv6 `/128`），不信任整个出口网段；Host、Origin、CSRF 和 HTTPS 转发校验继续生效。

### Upgrade Notes

- 受影响的 0.42.0 实例可再次执行应用市场更新升级至 0.42.1；更新会原子刷新 `KPANEL_TRUSTED_PROXY_CIDRS` 并重建 Panel 容器，失败时恢复原 `.env`、Compose、Agent 与镜像。

## [0.42.0] - 2026-08-04

### Added

- 新增 AI 三栏工作台、多会话、会话内 Provider/模型切换、流式回复、工具进度与逐次审批交互。
- 新增 OpenAI-compatible（Responses 与 Chat Completions）、Anthropic Messages、Gemini `generateContent` 三类流式协议，以及 Provider 测试、模型同步和手工模型管理。
- 新增原生 Go `AgentRuntime`、受控 KPanel 工具注册表、记忆、流程和进化提案审核；不引入 Hermes、Sidecar、向量数据库或通用宿主机 Shell。

### Security

- API Key 使用 XChaCha20-Poly1305 加密并由独立 `0600` 主密钥保护；响应、日志、审计、模型上下文和工具结果统一限长、脱敏。
- 公网 Provider 强制 HTTPS，并在解析和重定向阶段拒绝回环、私网、链路本地及保留地址；内网 Provider 必须显式开启，跨源重定向不携带认证头。
- 只读工具可自动执行，所有写入、删除、容器 exec 和任务输入继续要求普通逐次确认、真实状态回读、版本冲突检查及关联审计。

### Performance

- AI 数据独立使用 CGO-free SQLite WAL；单会话严格串行、全局最多两个 Run，模型步骤、工具调用、消息与结果大小均有界。
- 对话按 50 条分页并在上下文达到模型窗口 70% 时持久化摘要；前端 AI 页面保持懒加载，Panel 容器内存限制仍为 256 MiB。

### Upgrade Notes

- 新增 `/var/lib/kejilion-panel/ai.db` 与 `ai-secrets.key`，不迁移或扩展现有 `panel-state.json`；未配置 Provider 时不产生模型请求。
- Panel Compose 从单一内部网络迁移为固定内部网络加受控出口网络；应用市场更新会自动刷新可信代理网段，并在失败时恢复旧镜像、Agent、Compose 与 `.env`。
- 回滚至 `v0.40.3` 时保留数据目录；旧版本忽略 AI 数据。再次升级前必须成对保留 `ai.db*` 与 `ai-secrets.key`。

## [0.40.3] - 2026-08-03

### Changed

- 设置页将“修改密码”紧邻“修改用户名”展示，账户管理顺序更符合操作习惯。
- 手机端文件管理改为紧凑工具栏与底部选择操作区，优化路径导航、搜索、批量选择及文件列表在窄屏下的连续操作体验；桌面端布局和交互保持不变。

### Security

- 本次仅调整已登录管理界面的前端布局与交互，不新增 API、权限、网络入口、后台任务或持久化状态；文件操作继续受现有 Agent 路径、符号链接、登录态、同源、CSRF、限额和审计约束。

### Performance

- 不新增轮询、常驻进程或网络请求；移动端样式和选择状态继续复用现有页面数据，桌面端不增加渲染负担。

### Upgrade Notes

- 无数据、端口、部署配置、应用市场配置或 `kejilion.sh` 协议迁移；现有网站、应用、Docker、文件、集群、终端和监控数据不受影响。更新失败可回滚至 `v0.40.2` 镜像，数据目录保持不动。

## [0.40.2] - 2026-08-03

### Fixed

- 修复文件管理虽然已开放 `/root`、`/var`、`/opt`、`/srv`、`/mnt` 等宿主机目录，但通过应用市场安装或更新后的 Agent 仍被 systemd 写入范围限制为只读，导致创建、上传、编辑、复制、移动、回收站和解压操作返回 `read-only file system` 的问题。

### Security

- systemd 写入视图与文件 API 的 `/` 管理根保持一致；真实写操作仍必须经过固定类型接口、登录态、同源与 CSRF 校验、路径规范化、符号链接防护、内核虚拟文件系统拒绝、KPanel 数据与凭据目录隔离、请求上限和审计，不新增任意 Shell 或匿名文件入口。
- KPanel 的 Panel 数据目录继续通过 `ReadOnlyPaths` 独立保护，Agent Token、集群密钥和运行时凭据仍不向文件管理开放。

### Performance

- 本次仅修正 Agent systemd 单元的挂载写入范围，不增加常驻进程、轮询、缓存或请求；文件操作仍按现有有界 API 执行。

### Upgrade Notes

- 应用市场更新会重建 `kejilion-agent.service` 以应用新的 systemd 写入范围；无数据、端口、文件格式或 `kejilion.sh` 协议迁移，现有网站、应用、Docker、集群关系、终端会话策略和监控历史不受影响。更新失败可回滚至 `v0.40.1` 镜像，数据目录保持不动。

## [0.40.1] - 2026-08-03

### Added

- 多主机终端支持占满浏览器内容区的全屏工作区；全屏时保留多标签、连接状态圆点、标签切换、预输入框和退出入口，并隐藏连接列表以扩大终端可用空间。

### Changed

- 终端标签直接承载主机名称和连接状态，移除重复的长条主机标题区；普通与全屏模式共用同一会话，不会因切换布局重连或丢失回滚内容。
- 设置页将“两步验证”紧邻“安全入口”展示，安全相关入口保持连续；修改用户名所需的当前密码默认保持空白，只有用户点击或聚焦后才允许浏览器密码管理器主动填充。

### Security

- 当前密码不会由 KPanel 从会话、接口或持久化状态预加载；仍可由用户明确触发浏览器密码管理器的一键填充，并继续受登录态、同源、CSRF、限速和审计约束。
- 终端全屏只改变浏览器布局，不新增命令入口、权限范围、网络监听或会话存储；本机与远程终端继续复用现有 Agent 和集群加密通道。

### Performance

- 本次改动不增加后台轮询、终端连接、常驻任务或状态写入；全屏与标签状态复用现有终端会话及尺寸观察器，仅在可视区域变化时更新终端尺寸。

### Upgrade Notes

- 无数据迁移、端口变更、部署配置、应用市场配置或 `kejilion.sh` 协议变更；现有网站、应用、Docker、文件、集群关系、终端会话策略和监控历史不受影响。更新失败可回滚至 `v0.40.0` 镜像，数据目录保持不动。

## [0.40.0] - 2026-08-03

### Added

- 设置页新增管理员用户名修改入口；修改前必须验证当前密码，成功后立即注销全部现有会话并要求使用新用户名重新登录。
- 网站列表中的 WordPress、PHP 和静态站点本地目录可直接跳转到文件管理对应路径，减少跨模块定位网站文件的操作步骤。

### Changed

- 文件管理支持从受控路由参数打开绝对目录，并以 Agent 回读的真实路径为准；刷新、前进后退和手动导航保持一致。
- 站点目录只在存在明确、受支持的本地根路径时显示跳转；反向代理、负载均衡、重定向和未知产物维持原有展示。
- 左侧 KPanel 更新提醒与应用市场镜像检测共用 Docker Hub 失败回退：官方查询失败且主机位于 CN/HK（或地区服务不可用）时，短超时尝试 `docker.1ms.run`，再回退 `gh.kejilion.pro`；已知其他地区仍只使用官方 Registry。

### Security

- 用户名修改继续强制登录态、同源、CSRF、当前密码、持久化限速与审计；用户名采用大小写不敏感唯一约束，审计记录不保存新旧用户名明文。
- 文件管理路由参数拒绝相对路径、NUL、路径穿越、过长输入与异常分隔符，实际读取和写入仍由 Agent 的路径边界、符号链接和权限规则复核。
- 镜像加速回退只接受固定 HTTPS 主机和严格 Docker Hub 镜像引用，拒绝私有 Registry、URL、路径穿越与 digest 覆盖；代理返回值必须通过 `sha256` 摘要校验，不能改变实际更新与回滚目标。

### Performance

- 设置页仅在管理员提交修改时增加一次受限写请求；网站目录跳转复用现有文件列表接口，不增加后台轮询、常驻任务或额外状态存储。
- 镜像检测在官方 Registry 正常时零额外请求；只在失败时执行有界地区识别与固定源短超时回退，不增加常驻进程、定时任务或状态写入。

### Upgrade Notes

- 无数据迁移、端口变更、部署配置、应用市场配置或 `kejilion.sh` 协议变更；现有网站、文件、应用、Docker、集群关系和监控历史不受影响。更新失败可回滚至 `v0.39.2` 镜像，数据目录保持不动。

## [0.39.2] - 2026-08-03

### Fixed

- 修复已配对主机较多时连接列表按内容高度撑开页面、导致终端底部和预输入框被裁切的问题；连接列表与终端输出现在独立滚动。
- 修复交互终端在连续输入或短暂网络抖动时可能丢失尚未确认发送内容的问题；输入按小批次顺序发送，失败后保留队列并自动重试。
- 修复应用、建站、环境管理和体检终端在部分布局下输入区被输出区域挤出可视范围的问题，并统一智能跟随与“回到底部”能力。

### Changed

- 所有交互终端统一保留最多 5000 行回滚内容、显示可滚动输出区域，并在用户主动向上查看时暂停自动跟随。
- 多主机终端始终提供整行预输入框；脚本任务终端仅在后端明确等待输入时显示预输入框，避免改变非交互任务体验。
- 本机和已授权远程 KPanel 的终端继续通过现有登录态、Agent Unix Socket 与集群加密通道访问；本机终端允许管理员执行受支持的系统维护命令。

### Performance

- 原生按键输入使用 24 毫秒有界批处理，减少高延迟链路上的请求数量，同时保持整行预输入即时发送；连接列表和终端区域不再触发页面级重复布局。

### Security

- 本机维护终端仍只接受固定 `/bin/bash -l`（回退 `/bin/sh -l`），不接受浏览器传入的可执行文件、参数、环境变量或工作目录。
- 特权终端继续受登录态、权限范围、会话时长、输入输出上限和审计约束；临时 systemd 单元随 Agent 生命周期回收，不放宽 Panel 主服务的只读系统保护。

### Upgrade Notes

- 无数据、端口、部署配置、应用市场配置或脚本协议迁移；现有网站、应用、Docker、文件、集群关系和监控历史不受影响。更新失败可回滚至 `v0.39.1` 镜像，数据目录保持不动。

## [0.39.1] - 2026-08-03

### Fixed

- 修复三网延迟在部分主机上九条线路全部显示超时、但图表仍展示旧成功值的问题：固定端点现在由 Agent 在总超时预算内并行尝试非特权 ICMP、TCP/53 与 UDP DNS，最新一轮失败会明确显示“超时”，不会冒充当前延迟。
- 修正联通北京探测端点；九线路仍是内置固定目录，不接受用户输入的目标地址。
- 修复宿主机历史图表在宽屏下没有铺满、悬停提示遮挡左右边缘刻度的问题；图表按实际容器宽度计算坐标并与 Docker 历史图表保持一致。
- 修复终端输出响应被通用 API 包装器误拆为字符串，导致第二次轮询缺少 `offset` 并长期显示“正在重连”的问题。
- 修复 Agent 重启后旧终端会话无法收敛，以及发送过程中短暂断线可能丢失预输入内容的问题；后端现在返回明确的会话结束状态，前端采用有上限的重连退避并在发送成功前保留输入。
- 限制终端窗口尺寸与布局计算，避免异常行列数触发连续 resize 请求。

### Performance

- 三网探测最多使用 3 个目标工作线程，每个目标共享 1.5 秒总超时；每 5 分钟才执行一轮，最坏同时占用 9 个短生命周期网络套接字，不增加常驻进程或高频轮询。
- 监控图表只在容器尺寸变化时更新 SVG 视口，终端重连退避上限为 5 秒，避免断线期间产生请求风暴。

### Security

- 三网延迟继续只允许固定九个公网端点，未开放任意目标、任意端口或扫描 API；ICMP 使用 Linux 非特权 ping socket，不增加 `CAP_NET_RAW`。
- 终端继续复用登录态、Agent Unix Socket 与 Noise 集群加密通道；会话、输入、输出、尺寸、速率和生命周期限制保持不变。

### Upgrade Notes

- 无数据、端口、部署配置或脚本协议迁移；现有网站、应用、Docker、文件、集群关系和监控历史不受影响。更新失败可回滚至 `v0.39.0` 镜像，数据目录保持不动。

## [0.39.0] - 2026-08-03

### Added

- 新增轻量多主机终端：在统一连接列表中打开本机或已重新授权的 v2 KPanel 主机，复用现有登录态、Agent Unix Socket 与 Noise 加密集群通道，不新增公网监听端口。
- 终端支持多主机页签、预输入整行发送、原生交互输入、窗口缩放、断线续读和 `http://` / `https://` 安全链接跳转；轻量监控节点保持只读。
- 历史监控新增中国电信、联通、移动在北京、上海、广州的九路 DNS 延迟趋势；默认显示全部线路，并支持逐路隐藏、全部显示和全部隐藏。

### Performance

- 三网延迟每 5 分钟由固定三工作线程并发采样，复用现有监控归档与压缩响应；七天最大样本响应和每日落盘量均受现有历史监控预算约束。

### Security

- 终端能力使用独立 `cluster.terminal.open` scope，旧 v1/v2 配对不会自动扩权；会话、输入、输出、缓冲、速率和生命周期全部有界，OSC 8 任意链接与 OSC 52 剪贴板指令被阻止，审计不记录命令和终端内容。
- 三网延迟只访问内置、固定的 DNS 探测端点，不接受任意地址或命令输入，不增加 `CAP_NET_RAW`；采样失败记录为缺失，不伪装成零延迟。

### Compatibility

- 现有集群、网站、Docker、轻量节点和监控数据无需迁移；远端 KPanel 终端需要双方升级后撤销旧关系并重新配对，本机终端不受影响。

### Upgrade Notes

- 无数据、端口或部署配置迁移；升级后可直接使用本机终端和三网延迟历史。远端 KPanel 终端要求双方均升级到 `v0.39.0`，并撤销旧关系后重新配对授予终端权限；更新失败可回滚至 `v0.38.3`，现有业务数据保持不动。

## [0.38.3] - 2026-08-02

### Fixed

- 修复宿主机历史监控图表在宽屏下悬停提示与竖向刻度线坐标不一致，导致左右边缘提示遮挡刻度的问题；宿主机与 Docker 图表现在统一按 SVG 实际显示位置定位并自动避让。

### Security

- 发布门禁新增核心特权包竞态检测、固定摘要 Trivy 源码与最终镜像扫描，并验证候选镜像在非 root、只读根、`256 MiB`、`1 CPU`、`128 PID`、`cap-drop ALL` 和 `no-new-privileges` 条件下可正常启动。

### Changed

- 统一版本一致性、隔离部署安全、变更感知核验和完整质量审计入口，减少快速迭代中遗漏测试或发布步骤的风险。

### Upgrade Notes

- 无数据、配置、端口或脚本协议迁移；兼容现有网站、应用、Docker、文件、集群和监控历史，更新失败可回滚至 `v0.38.2`。

## [0.38.2] - 2026-08-02

### Changed

- Docker 容器、镜像、网络和存储卷的操作按钮在桌面端统一靠右排列，窄屏时自动换行，减少标题区错位和空白浪费。

### Performance

- 网站环境管理首屏只等待环境摘要；版本目录和备份列表改为进入对应分区后按需加载，后台任务记录独立刷新，避免辅助接口拖慢页面显示。

### Upgrade Notes

- 无数据、配置、端口或脚本协议迁移；兼容现有网站、LDNMP 环境、Docker 资源和多语言偏好，更新失败可回滚至 `v0.38.1`。

## [0.38.1] - 2026-08-02

### Fixed

- 修复轻量节点安装脚本调用内部 `systemctl` 包装器时参数被截断，导致授权成功后服务无法启动的问题。
- 修复部分 locale 下主机名清洗触发 `tr: range-endpoints ... reverse` 的问题。
- 兼容滚动升级期间旧版中心签发的 30 分钟接入授权；新授权仍保持 5 分钟有效期。
- 接入成功但服务启用失败时保留节点凭据；重新执行接入命令可继续安装，不再重复消耗一次性授权。

### Security

- 接入授权仍为一次性、绑定目标中心并要求可信 HTTPS；仅将解析兼容上限限制为 1 小时，不扩大节点权限或网络暴露面。

### Upgrade Notes

- 无数据迁移；兼容现有 KPanel、站点、应用、集群与已接入节点，更新失败可回滚至 `v0.38.0`。

## [0.38.0] - 2026-08-02

### Added

- 完成当前公开业务页面的 `zh-CN` / `en-US` 覆盖；概览、网站、应用市场、Docker、文件、体检、集群、活动记录、监控、环境管理和设置按路由懒加载英文资源。

### Fixed

- 语言切换现可同步更新动态弹窗、提示、占位文本与运行中任务状态；命令、路径、文件内容、日志、代码编辑器、交互终端和第三方原始错误继续保持原文。
- 非面板 Linux 主机接入命令改用 `https://kejilion.sh` 官方短入口；根目录与 `cn/kejilion.sh` 使用同一轻量节点协议，并兼容缺少 `useradd` 的精简 systemd 主机。

### Performance

- 新增业务文案覆盖检查，英文词典拆分为路由级资源块，中文用户不下载英文业务词典，语言切换不重复请求业务 API。

### Security

- KPanel 镜像固定并校验 `kejilion.sh` 提交与 SHA-256；轻量节点二进制继续通过 Release `SHA256SUMS` 校验，未放宽节点权限和网络暴露范围。

### Upgrade Notes

- 兼容现有数据、端口、反向代理、语言偏好和已接入节点，无需迁移配置；更新失败可回滚至 `v0.37.1` 镜像且不删除现有数据。

## [0.37.1] - 2026-08-02

### Changed

- GitHub Release 页面改为从对应版本的 Changelog 生成明确更新内容，并固定展示升级说明、兼容性提示、产物校验、测试结论和回滚方式；缺少版本章节或更新条目时禁止发布。

### Fixed

- 修复 KPanel 通过可信 `k fd` HTTPS 域名访问、但安装时保存的公共地址仍为 IP + 端口时，轻量节点接入命令无法生成的问题；中心端现在直接使用当前已验证的 HTTPS 根地址。

### Security

- 轻量节点授权仍只绑定直接 TLS 或可信反向代理确认的 HTTPS 根地址；非可信来源伪造 `Host` 或 `X-Forwarded-Proto` 不会被接受。

### Upgrade Notes

- 兼容现有 KPanel 数据、端口、`k fd` 反向代理和已接入节点，无需迁移配置；更新失败可回滚至 `v0.37.0` 镜像且不删除现有数据。

## [0.37.0] - 2026-08-02

### Added

- 集群支持未安装 KPanel 的 Linux 主机通过一次性命令接入只读轻量节点，统一展示 CPU、内存、磁盘、网络、系统和地区概要。
- Release 新增 `kejilion-node` 的 `linux/amd64`、`linux/arm64` 静态二进制并纳入 `SHA256SUMS`。

### Security

- 轻量节点仅进行出站 HTTPS 上报，不开放管理端口、Shell、文件、Docker 或远程命令；上报使用独立 HMAC-SHA256 凭据、时间窗和重放保护。
- systemd 服务使用非登录用户、只读系统、空 capability 与 `NoNewPrivileges`；安装和自动更新验证 Release 摘要，健康检查失败自动恢复旧二进制。

### Performance

- 节点默认每 30 秒采集一次，只保留最新快照；中心端有界 checkpoint，不增加高频历史写盘或轮询并发。

## [0.36.0] - 2026-08-02

### Added

- 建立前端多语言运行时、类型安全词典、语言选择器和路由标题契约，首批覆盖登录、初始化、全局导航、通用状态与设置页语言选项。
- 支持根据浏览器首选语言首次选择中文或英文；用户手动切换后保存偏好并固定使用。

### Changed

- 中文资源保持同步加载，英文资源按需加载；页面语言、标题和稳定错误提示随语言即时更新。
- 未知后端错误继续保留原始详情，业务页面按阶段迁移，避免翻译缺失掩盖真实故障。

### Performance

- 多语言运行时首屏压缩体积增量控制在 8 KiB 预算内，非当前语言不进入首屏预加载。

## [0.35.0] - 2026-08-01

### Added

- 设置页新增默认关闭的 TOTP 两步验证，兼容主流身份验证器，并提供 10 个只展示一次、使用后立即失效的恢复码。
- 登录页在密码验证通过后进入第二因素步骤，并支持在动态验证码与恢复码之间切换。

### Security

- TOTP 密钥使用 Panel 本地 `0600` AES-256-GCM 密钥加密，恢复码只保存摘要；启用、关闭和重置恢复码后撤销全部旧 Session。
- 动态验证码记录已使用时间步以阻止重放，恢复码原子消费；登录和两步验证管理均有持久化限速，写接口继续要求 Session、Origin 与 CSRF。
- 因素启用、首次验证码计数器和恢复码摘要一次原子落盘；密钥缺失或损坏时拒绝静默重建，避免升级或中断形成可绕过的半完成状态。

## [0.34.9] - 2026-08-01

### Added

- 文件管理新增列表与大图标两种排版；大图标模式为 JPEG、PNG 和 GIF 按需生成轻量缩略图，并持久化用户排版偏好。
- 批量文件操作新增“反选”，仅反转当前可见条目的选择状态。

### Changed

- 文件、文件夹、图片、音视频、归档、表格、数据库、演示文稿、安装包、敏感配置、代码和普通文档使用更明确的图标与主题色。

### Performance

- 缩略图仅在进入大图标视口时懒加载，限制源文件为 12 MiB、800 万像素，输出不超过 320×210，并将并发生成数限制为 2；不增加常驻缓存和后台任务。

### Security

- 缩略图继续经过登录鉴权、路径边界、符号链接保护和资源版本复核；解码任务有超时、并发与尺寸上限，不写入宿主机持久缓存。

## [0.34.8] - 2026-08-01

### Changed

- 交互式终端统一空闲与运行状态的深色画布、输入区和滚动条，降低 ANSI 彩色输出偏色，品牌绿色仅用于光标、选区和交互状态。
- 代码编辑器、Docker 日志与控制台、应用日志和体检输出统一使用 KPanel 深色工作区色板，并统一 12px 圆角、细边框和轻量内描边。

### Fixed

- 修复部分终端空闲态仍使用旧色板、不同深色弹窗边缘与圆角不一致的问题。

## [0.34.7] - 2026-08-01

### Added

- 文件管理支持批量压缩为 TAR.GZ、ZIP 或 TAR，并支持将 TAR.GZ、TGZ、ZIP 或 TAR 解压到不覆盖现有内容的新文件夹；执行过程可主动停止并自动清理临时产物。

### Security

- 归档操作使用 Agent 固定动作和 Go 标准库流式处理，拒绝路径穿越、链接、设备文件、重复条目及超限归档，并通过资源版本复核、固定预算、`fsync` 和不覆盖原子改名保护宿主机文件。

### Documentation

- 固化“先按 KPanel 规范独立设计、再复核 `kejilion.sh` 与竞品差距、最后开发验收发布”的产品方案流程。

## [0.34.6] - 2026-08-01

### Changed

- 交互式终端保持固定深色画布，并将光标、选区、边框、状态和输入操作统一为 KPanel 品牌与语义色；保留 ANSI 原生彩色输出。
- 代码编辑器保持固定深色画布，统一光标、选区、活动行、查找替换和滚动条样式，同时保留易辨识的多色语法高亮。

## [0.34.5] - 2026-08-01

### Changed

- 普通文件列表只提供“移入回收站”，彻底删除与清空仅在回收站管理界面中出现；Panel 与 Agent 同步拒绝普通目录直删动作。

### Fixed

- 修复文件页批量操作样式泄漏到主内容容器的问题；桌面端侧栏收起仅切换布局偏移，避免整页连续重排、卡顿和内容异常偏移。

## [0.34.4] - 2026-08-01

### Added

- 文件管理开放可视化回收站、原路径恢复、选择性彻底删除和清空回收站；普通文件列表同时提供明确的彻底删除入口。

### Fixed

- 回收站跟随 Agent 实际状态目录，修复应用市场部署下删除文件写入 `/var/lib/kejilion-panel` 时命中只读文件系统的问题。
- 桌面端侧栏收起时为主内容设置明确的剩余视口宽度并裁切横向溢出，避免内容区域异常偏移到屏幕右侧。

### Security

- 回收、恢复和彻底删除使用固定动作枚举、资源版本校验、受保护路径复核和审计；恢复不会覆盖同名文件，回收站元数据使用 Agent 私有 `0700/0600` 存储。

## [0.34.3] - 2026-08-01

### Fixed

- 侧栏更新提示不再把当前 Agent 版本误显示为目标版本；改为明确显示“更新可用”，悬停时说明当前版本及更新动作。
- 概览自动刷新改为在完整数据就绪后一次性更新页面，避免并发分段结果造成卡片反复消失、恢复和周期性布局跳动。
- 文件列表多选时阻止浏览器同时选中文字，`Ctrl`、`Shift` 与普通点击只改变文件条目选择状态。

## [0.34.2] - 2026-08-01

### Changed

- 代码编辑器的查找与自动换行入口移到右上角图标区，查找替换改为更接近 VS Code 的紧凑悬浮面板，避免占用整行编辑空间。

## [0.34.1] - 2026-08-01

### Added

- 轻量代码编辑器增加查找替换、跳转行、代码折叠、光标行列状态和自动换行开关；相关能力仍随编辑器按需加载。

### Changed

- 文件批量操作栏改为桌面端与移动端统一的底部悬浮操作区，保持路径导航、搜索和文件行位置稳定。
- 编辑内容改为由 CodeMirror 内部维护，仅在保存时读取完整文本，避免每次按键复制整份文件。
- 全局滚动条统一为 KPanel 深浅主题配色，并为代码编辑器、交互终端和日志视图提供深色专用样式。

### Fixed

- 修复深色代码编辑器输入光标不明显的问题。

## [0.34.0] - 2026-08-01

### Added

- 设置页增加可选登录安全入口；启用后只有携带正确入口路径的请求才能进入登录页，升级用户默认关闭，可生成、复制、修改和轮换入口。
- 文件管理范围扩展到宿主机根目录，并提供 `/`、`/home`、`/root`、`/etc`、`/var` 快捷入口；KPanel 数据、凭据目录以及内核虚拟文件系统写操作仍由固定边界隔离。
- 侧栏版本区可直接检查并启动现有 KPanel 应用更新流程，更新完成后自动等待服务恢复并刷新界面。

### Changed

- 文件写入在原子替换时保留 Linux 扩展属性（含 SELinux 标签），回收站迁移到 Agent 私有状态目录；Panel 继续保持非特权运行，所有宿主机文件操作仍由结构化 Agent API 执行。
- 容器监控列表高度与右侧四个趋势图区域对齐，列表较长时在自身区域滚动，避免底部出现大面积空白。

### Security

- 安全入口使用服务端生成随机值、HttpOnly/SameSite Cookie、资源版本冲突检测和审计记录；未知入口返回 404，不向未授权请求暴露登录页面。
- 全盘文件管理继续执行路径穿越、符号链接竞态、受保护目录、请求大小、并发、超时、CSRF/Origin 与审计限制，不增加任意 Shell 或新的公网监听入口。
- Go 最低工具链统一到 `1.26.5`，修复旧补丁版本中与 `os.Root`、TLS、HTTP/2、证书验证和网络输入处理相关的标准库漏洞。

## [0.33.2] - 2026-07-31

### Fixed

- 历史趋势图使用 SVG 原生坐标矩阵换算鼠标位置，并以全部曲线时间点作为悬停锚点，修复宽图表无法到达左右边缘的问题。
- 容器历史列表将不属于最新采样批次的条目灰化并标记为“历史”，避免同名旧容器被误认为仍在运行。
- 容器监控图表统一为 CPU、内存、磁盘 I/O、网络顺序，与宿主机指标布局保持一致。

## [0.33.1] - 2026-07-31

### Changed

- 历史监控容器列表改为最新数据优先，并在同一采样批次内按实际内存占用、CPU 使用率和名称排列，使高内存容器更容易被发现。

## [0.33.0] - 2026-07-31

### Added

- 历史监控增加宿主机磁盘读写 I/O，并统一宿主机与容器的 CPU、内存、网络和磁盘 I/O 指标单位；旧版历史记录可继续读取。

### Changed

- 历史监控保持四视图布局，磁盘可切换容量与读写 I/O，网络可切换流量与连接数；所有趋势图增加纵轴刻度和悬停精确值，同时继续保留概览快捷入口而不新增左侧分类。
- Docker 管理默认仍进入容器页，将 Engine 状态、摘要、资源分区、搜索与排序收拢为统一工作区，并按运行状态、使用关系和自然名称提供轻量智能排序。

### Performance

- 趋势图改为单次时间标准化、单遍边界计算和二分查找悬停点，不增加图表依赖、历史 API 请求或 Docker Stats 调用；同机 7 天查询基准中位数变化约 `+1.1%`。

## [0.32.0] - 2026-07-31

### Added

- 概览实时监控增加轻量历史趋势入口；CPU、负载、内存、系统盘和网络卡片可直接定位相应趋势图，并可按 1 小时、6 小时、24 小时和 7 天查看主机与运行中 Docker 容器的资源历史。
- `kejilion-agent` 在本机私有状态目录按日沉淀有界 JSONL 采样：主机每分钟、容器每 5 分钟，默认保留 7 天，不依赖数据库、Prometheus、Grafana 或新的外部服务。

### Fixed

- 文件管理恢复更符合桌面习惯的单击取消选择、复制或剪切后清除选择，并支持在目录空白区域右键粘贴或新建文件夹。

### Security

- 历史监控仅增加已认证只读 API；采样不执行 Shell、不查询公网身份，不记录容器环境变量、标签、命令、日志、挂载、凭据或进程参数，并限制采样并发、查询并发、文件/目录权限、单日容量、总容量及响应点数。

## [0.31.3] - 2026-07-31

### Fixed

- 左侧栏收起时改为文字渐隐收缩并保持导航图标横向锚点稳定，统一品牌区、导航、在线状态、用户区和内容区的过渡节奏，消除图标先跳位再移动的割裂感。

## [0.31.2] - 2026-07-31

### Fixed

- 文件或文件夹通过右键打开菜单时会同步勾选该项；右键已选中的项目仍保留现有多选结果，恢复符合桌面文件管理器习惯的操作上下文。

## [0.31.1] - 2026-07-31

### Fixed

- 文件选中后的批量操作栏改为覆盖现有路径工具栏，不再插入新行推动文件列表，避免首次点击造成目录条目位移并打断双击打开。
## [0.31.0] - 2026-07-31

### Added

- 文件管理采用更接近 Windows 的双击打开、`Ctrl/Cmd` 单项选择、`Shift` 连续选择和
  复制/剪切/粘贴工作流；右键只打开菜单，不再隐式改变勾选状态。
- 代码文件按需加载 CodeMirror 6 与对应语言解析器，支持语法高亮、行号、搜索、
  括号匹配和快捷保存，大文件自动退回轻量纯文本模式。
- 桌面端左侧栏支持展开与收起，收起后保留分类图标、在线状态与用户入口，并在浏览器中
  记忆显示偏好；移动端继续使用完整抽屉导航。

### Changed

- 文件上传、下载和目录读取增加并发上限、空闲超时、硬超时、服务端搜索与稳定分页；
  复制预算按整批累计，避免大目录和慢连接无界占用内存、连接与磁盘。
- 左侧分类资源在空闲、悬停或键盘聚焦时按需预取；首次切换立即显示加载进度，
  瞬时资源下载失败自动重试一次，页面仍保持独立拆包。

### Fixed

- 修复部分 systemd 版本中 `ProtectHome=read-only` 覆盖 `/home` 写入白名单，导致文件上传、
  新建目录及其他写操作统一返回 `read-only file system` 的问题。
- 修复大目录一次性返回全部条目、慢速文件流长期占用连接，以及批量复制可绕过单次容量预算的问题。

### Security

- 文件根继续固定在 `/home` 并隔离 KPanel 数据目录；新增传输并发、请求时长、目录扫描、
  单页条目和整批复制上限，不增加任意路径、任意 Shell 或新的公网监听入口。

## [0.30.0] - 2026-07-30

### Added

- 左侧导航在 Docker 与“体检”之间增加轻量“文件”工作台，支持 `/home` 目录浏览、搜索、
  排序、上传、下载、新建目录、重命名、复制、移动、回收站、权限修改、批量选择和右键菜单。
- 增加轻量代码编辑器及图片、PDF、音频、视频查看器；代码编辑支持行号、Tab 缩进、
  `Ctrl/Cmd+S`、全屏和资源版本冲突检测。

### Changed

- 文件上传与下载使用浏览器、Panel、Agent 之间的固定缓冲流式传输；批量操作逐项返回结果，
  部分成功后会重新读取真实目录状态，避免把已完成操作误报为整体失败。

### Security

- 文件能力使用根目录句柄固定在 `/home`，隔离 KPanel 数据、保护目录祖先与内部回收站，
  拒绝路径穿越、符号链接竞态逃逸、任意 Shell 和活动 HTML/SVG；写操作继续要求
  Session、Origin、CSRF、固定动作与审计记录。
- 文本读取限制为 2 MiB 的 UTF-8 内容并拒绝 NUL 字节；下载禁用私有缓存，上传限制为
  512 MiB，复制限制为 10,000 个条目或 10 GiB。

## [0.29.0] - 2026-07-30

### Added

- 概览“性能与网络工具”增加 BBRv3 管理，与普通 BBR 并排展示；读取 XanMod 安装状态、当前内核、拥塞算法、队列算法和待重启状态，并支持安装、更新、卸载。
- `kejilion.sh` v4.5.4 增加 BBRv3 固定非交互协议；KPanel 使用可信脚本和独立 systemd 后台任务执行，不接受自定义命令且不自动重启服务器。

### Changed

- 建站任务启动后自动切换到紧凑终端视图并隐藏模板表单；成功或失败后保留原始输出，等待用户确认配置信息并手动关闭窗口。

## [0.28.5] - 2026-07-30

### Fixed

- 更新内置 `kejilion.sh`，统一规范 WordPress、PHP、静态站及成品站的目录和文件权限，修复 KPanel 后台任务使用严格 `umask` 时 Nginx 无法读取 CSS、JavaScript 与站点入口的问题。

## [0.28.4] - 2026-07-30

### Changed

- 集群接入授权将主机 URL 与一次性授权码合并为单份版本化凭据，目标端可一键复制，中心端整段粘贴后自动拆分并沿用原有安全校验。

## [0.28.3] - 2026-07-30

### Changed

- 新建网站首屏固定为 WordPress、IP/端口反代与静态网站三种常用入口；全部成品站和其他建站方式默认折叠。
- 收紧域名解析辅助中的服务商入口，并将重复的服务摘要和多条执行说明合并为一条后台任务提示。

## [0.28.2] - 2026-07-30

### Added

- 集群主机支持拖拽或键盘调整显示顺序，并在当前浏览器持久化；CPU 同时显示核心数，内存与磁盘同时显示已用量和总量。
- 本机节点开放与远程主机一致的“管理”入口，可修改集群显示名称，但仍禁止移除本机节点。

### Changed

- 新建网站弹窗默认仅展示 WordPress、IP/端口反代与三个热门成品站，其余模板按需展开，并压缩说明与卡片高度以减少滚动。
- 应用安装或网站创建成功后自动返回对应列表，将新资源置顶并显示短时高亮标记。

### Fixed

- 集群授权码和主机 URL 在非安全上下文、WebView 或剪贴板权限被拒绝时，自动使用兼容复制方式，避免 HTTP IP+端口主机无法复制。

## [0.28.1] - 2026-07-30

### Added

- 集群主机支持行列表与卡片两种排列方式，默认使用更紧凑的行列表，并在当前浏览器保存用户选择。
- “本机接入授权”窗口增加当前主机 URL 展示和一键复制；域名反代与 IP+端口访问均按浏览器实际入口呈现。

### Changed

- 左侧导航将“集群”移动到“体检”下方，降低非日常功能的视觉优先级。
- 添加集群主机时将授权码声明为一次性凭据，并补充密码管理器忽略提示，避免浏览器错误预填用户名和密码。

## [0.28.0] - 2026-07-29

### Added

- 网站列表通过 KPanel 同源接口加载站点实际 favicon，并使用 Agent 磁盘缓存、浏览器 7 天缓存和低优先级懒加载，避免阻塞列表与重复访问外站。
- 补齐 Rocky Linux、AlmaLinux、RHEL、Oracle Linux、Manjaro、openSUSE 和 SUSE Linux Enterprise 的发行版身份与品牌图标映射。

### Fixed

- 修复应用市场安装时把探测到的公网 IPv4 固定为 `PublicURL`，导致内网、NAT、端口映射或无公网环境无法通过实际 IP 打开 KPanel 的问题；IPv4/IPv6 字面地址保持同源校验，域名仍需显式配置或通过可信 `k fd` 代理访问。
- 修复 Rocky Linux 等衍生发行版被 `ID_LIKE` 错误显示为 CentOS 或其他兼容家族品牌的问题。

## [0.27.0] - 2026-07-29

### Added

- 增加“集群”主机看板；每台 KPanel 均可同时作为中心端和被监控端，统一展示本机及最多
  100 台远端主机的 CPU、内存、磁盘、网络、系统、地区、延迟和在线状态。
- 增加一次性接入授权、控制端撤销、主机重命名、手动刷新和独立面板跳转；本机直接读取
  Agent 实时摘要，不占远端主机配额。
- 集群支持公网 HTTPS，以及没有域名时的 `http://IP:非80端口`；后者使用
  `Noise_IKpsk0/Noise_IK`、X25519 和 ChaChaPoly 对配对与日常监控数据端到端加密。

### Changed

- 标准部署为 Panel 增加独立出站网络，集群轮询不改变内部反向代理网络、非 root、
  只读根文件系统、无 capability 和 Agent Unix Socket 边界。
- 新配对默认使用 v2 加密协议，同时保留既有 v1 HTTPS 主机、状态文件和撤销流程兼容。

### Security

- 集群出站严格限制为已登记根地址，拒绝重定向、代理继承、云元数据、loopback、
  link-local、保留地址、混合 DNS 结果和 DNS rebinding；私网仅能通过部署配置的精确
  CIDR 放行。
- 授权码 5 分钟过期且单次消费；节点密钥使用 `0700/0600` 权限和原子状态文件保存，
  请求同时校验目标身份、固定路径、时间窗、重放、请求体和并发上限。

## [0.26.1] - 2026-07-29

### Fixed

- 修复已安装应用在停止、重启、暂停或恢复后的状态刷新中，旧版 Agent 返回
  `ports: null` 时详情页触发异常、无法打开的问题；前端现在兼容空值，并正确显示暂停状态。
- 新版 Agent 在应用没有可用端口时固定返回 `ports: []`，消除停止态端口字段的
  `null`/数组契约漂移。

### Added

- 增加隔离候选 KPanel 与真实 Chrome 的应用生命周期回归流程，覆盖运行、停止、启动、
  重启、暂停、恢复以及旧 Agent `ports: null` 兼容路径。

## [0.26.0] - 2026-07-29

### Added

- KPanel 交互式终端自动识别 `http://` 和 `https://` URL；用户点击后在隔离的新浏览器标签页中打开，覆盖应用、建站、体检和环境管理终端。
- KPanel 交互式终端增加预输入栏，支持先在浏览器端完整输入或粘贴内容，再按 `Enter` 整行发送；原实时终端输入继续保留，兼容方向键、控制键和原生交互菜单。

### Fixed

- 修复应用停止或状态刷新后，详情弹窗可能只留下页面滚动锁但内容不可见的问题；弹窗改用共享堆栈管理页面锁定，关闭后立即清理遮罩，再次打开可正常显示，`Escape` 仅关闭最上层弹窗。
- 修复 Release 重跑时无法复用草稿版本的问题，并为 Docker Hub 镜像摘要读取与 `latest` 提升增加有限重试，避免瞬时网络中断留下半发布状态。

## [0.25.0] - 2026-07-29

### Added

- 静态站、PHP 站、域名反代、负载均衡、重定向、Bitwarden 和 Halo 建站模板统一进入
  `kejilion.sh` 原生交互终端；面板只预填首个域名，后续参数由脚本提示输入。
- 建站任务使用独立后台 PTY 运行，关闭弹窗或刷新页面不会中断；可从任务横幅重新进入终端。

### Changed

- 扩展 KPanel 固定建站协议编号，并根据模板类型区分网站目录产物与仅配置产物，避免反代、
  重定向和负载均衡成功后被误判为失败。
- KPanel 固定到 `kejilion/sh@f031d1206224de3743845d2fc81c4801ecda32f4`，
  并使用 GitHub Raw 字节摘要校验发布镜像内脚本。

### Licensing

- KPanel 源代码正式采用 `AGPL-3.0-only`，并在设置页提供源码和许可协议入口。
- Release 部署包与运行镜像携带 KPanel 许可、第三方许可及品牌说明；OCI 镜像增加
  标准源码地址和许可证标签。
- 保留随发行版分发的 `kejilion.sh` 及前后端运行时依赖的原始许可与版权声明。

## [0.24.4] - 2026-07-29

### Added

- 应用市场的运行中交互任务增加“结束任务”入口；二次确认后由 Agent 请求 systemd
  停止对应的固定任务单元，关闭输入通道、持久化取消状态并释放应用管理锁。普通非交互
  安装或更新任务不允许从该入口强制终止。
- 标准单端口应用的安装端口改由面板预先输入，并在展示、任务提交和容器创建三个阶段检查
  Docker 发布端口及宿主机 TCP/UDP 监听端口；端口冲突时不启动安装任务。无法安全识别单一
  端口的专属安装流程继续使用原生交互终端。

## [0.24.3] - 2026-07-28

### Changed

- 应用详情改为紧凑纵向布局；常见桌面视口可在一屏查看状态、操作、域名、直接访问和维护区，窄屏仍自动回落为单列。
- KPanel 应用使用本地 128×128 WebP 品牌图标，避免远程热链并降低资源体积。
- `kejilion.sh` 的 Docker Hub 更新检测改为镜像摘要优先，不再按中国区域跳过，并兼容 `docker.io/` 镜像前缀。

### Fixed

- 补齐 Panel 到 Agent 的 `manage` 固定路由与 Marker 资源版本校验，修复受支持应用打开“脚本管理”时返回 `Route not found`。
- 停止后的应用继续保留在已安装列表和详情中，并可执行启动、检查更新、更新与卸载。
- 绑定域名、解绑域名和 IP + 端口访问切换会刷新过期的站点或容器版本并重试一次；应用后台任务运行期间禁用冲突操作，避免连续操作产生误报。
- 应用安装、更新、卸载及访问策略任务改用 `systemctl show ActiveState` 同步后台状态，长任务处于 `activating` 时不再被提前判为中断；页面断线重连后继续读取同一任务。
- 应用直接访问链接只使用已绑定域名或服务器公网 IP，不再错误拼接为 `KPanel 反代域名 + 应用端口`；IPv6 地址会自动添加方括号。
- 将官方在线应用目录数量更新为 152，并为离线回退目录补充 KPanel 应用及本地图标。

## [0.24.2] - 2026-07-28

### Changed

- 应用详情中的域名访问与 IP + 端口访问统一改为上下排布，减少不同应用状态下的空白和布局跳变。
- 概览发行版标志改为本地品牌色底板与单色矢量图标，在浅色、深色主题中保持一致辨识度；未知发行版统一使用 Linux 标志，不再加载发行版 PNG。

### Fixed

- 停止应用后若页面正处于“运行中”筛选，自动切换到“已安装”，应用详情仍可继续查看和重新进入。
- 对已由可信脚本配置和实际容器共同确认的应用开放更新、卸载和直接访问策略，不再因旧版 `appno.txt` 标记缺失而错误禁用；执行前仍校验精确容器身份，并由 `kejilion.sh` 原子补回兼容标记。
- 只有脚本安装标记但缺少容器的应用仅显示恢复终端，避免把没有实际操作目标的状态伪装成可更新或可卸载。
- 将应用资源版本冲突与后台任务冲突拆分为不同错误；检查更新、更新和卸载遇到页面快照过期时自动刷新并重试一次，不再误报已有应用任务运行。

## [0.24.1] - 2026-07-28

### Added

- 增加 KPanel 标准化品牌图标、PWA/搜索引擎元数据和本地轻量发行版图标；未知系统统一使用
  Linux 图标，不再使用字母占位。
- 增加可复现的运行时性能基线、网络入侵安全加固报告，以及性能、稳定性、资源占用和
  网络入侵安全开发规范。

### Changed

- 登录密码校验、Docker 批量探测和宿主机信息采集增加并发与重复请求抑制；静态资源启用
  预压缩与长期不可变缓存，API 响应按安全类型和收益启用 gzip。
- 运行镜像升级到固定摘要的 Node.js 24.18.0 构建链，并为发布流水线增加 Go 可达漏洞扫描
  与前端高危依赖审计。
- 为 HTTPS 请求增加 HSTS、COOP 和 CORP 等响应头，同时保留可信反向代理下的 `k fd`
  域名访问、Secure Cookie 和动态域名行为。

### Security

- 收紧代理缓冲、请求体、终端读取和 Docker 检查等数值边界，限制无界并发与资源耗尽攻击面；
  保持现有管理员业务能力不变。
- 当前源码、依赖与最终运行镜像的发布前扫描未发现可达或可利用的 High/Critical 漏洞。

## [0.24.0] - 2026-07-28

### Added

- 已安装且由 `kejilion.sh` 接管的非统一架构应用，在详情页提供“脚本管理”入口；
  通过固定应用编号打开原生交互菜单，复用后台 PTY、ANSI 输出和安全输入通道，不开放任意 Shell。

### Fixed

- 应用详情读取站点列表短暂失败时保留上次成功结果并自动重试，不再把已绑定域名静默显示为空；
  同时兼容 `localhost`、IPv4/IPv6 loopback 及带尾斜杠的等价反代上游。
- “检查更新”作为只读操作会刷新一次容器资源版本并处理检查期间的重启竞态，不再因页面快照过期直接报
  `container resourceVersion changed`；更新、卸载等写操作仍执行严格版本校验。
- 应用原生交互任务的 systemd 单元允许 `AF_NETLINK`，修复 `iptables-nft` 在“允许/阻止 IP+端口访问”
  菜单中报 `Address family not supported by protocol`，任务仍通过固定应用编号进入宿主机脚本。
- LDNMP 环境在没有备份时返回空列表，前端同时兼容旧 Agent 的 `items: null`；
  备份或任务辅助列表读取失败不再阻断环境状态及其管理功能。
- 应用域名绑定成功后立即更新弹窗中的域名列表，并使用重新读取的应用资源版本调整
  直接访问策略；策略调整失败不再把已经成功的域名绑定误报为失败。
- 应用域名绑定区和新建网站弹窗增加公网 IPv4/IPv6 解析记录复制，以及 Cloudflare、
  阿里云、腾讯云和华为云 DNS 控制台快捷入口。
- KPanel 应用市场配置不再硬编码 Agent 版本或 `kejilion.sh` 摘要；改为交叉校验镜像
  OCI 标签、`/release/VERSION`、Agent 自报版本和镜像内脚本实际 SHA-256，后续普通
  版本发布无需同步修改 `kejilion/apps`。

## [0.23.0] - 2026-07-28

### Added

- 应用市场中未进入 `docker_app`/`docker_app_plus` 统一管理函数的脚本内置应用，也可通过
  固定应用编号打开 `kejilion.sh` 原生交互终端完成部署；后台任务、ANSI 输出和终端输入
  与现有应用任务复用同一安全链路。
- 概览基础系统设置在 SSH 端口旁增加“SSH 防御”开关，读取 Fail2Ban SSH jail、开机自启
  和当前封禁数；开启与关闭复用 `kejilion.sh` 的固定 `k f2b` 协议并由 systemd 后台执行。

### Changed

- 应用、建站、环境管理和体检的交互终端统一改为 Agent 长轮询：输出变化约 25ms 内返回，
  不再固定等待 350ms；键盘输入采用 12ms 固定窗口合并，回车、方向键和控制键立即发送。
- 大段粘贴按 UTF-8 字符边界拆成不超过 2 KiB 的 FIFO 写入，并在 FIFO 短暂繁忙时有限等待，
  避免中文、Emoji 被截断或非阻塞写入偶发失败。
- 关闭 SSH 防御只停用 Fail2Ban 服务和开机自启，保留软件、jail 配置及历史日志，便于重新开启。

## [0.22.0] - 2026-07-28

### Added

- 网站模块增加“站点管理 / 环境管理”二级导航，提供 LDNMP 总览、防护、优化、更新、
  冷备、还原和卸载页面。
- Agent 增加固定枚举的 `/v1/web-environment` API、独立 systemd 后台任务、持久化任务
  状态、可交互 ANSI PTY 终端、完成凭据和统一活动记录映射。
- 环境任务关闭弹窗或刷新页面后继续运行，可重新打开终端并全屏查看。
- `kejilion.sh` 增加独立 `k web env` 交互菜单与 KPanel JSON/JSON Lines 协议，
  保留原 `k web` 菜单和现有命令。
- 冷备生成原脚本兼容归档及 SHA-256 sidecar；还原增加归档安全扫描、同文件系统暂存、
  原子目录交换和失败回滚。

### Security

- 环境写入只接受固定动作和当前 `expectedResourceVersion`，并与一键建站共享主机锁。
- Cloudflare 凭据使用任务专属 `0600` 文件传递，不写入任务 JSON、终端、审计或面板数据库。
- 备份下载改为认证流式代理，避免大归档进入普通 JSON 响应缓冲区。

## [0.21.0] - 2026-07-28

### Added

- 检测到公网 IPv6 时，DNS 预设自动同时加入对应服务商的 IPv6 解析地址；无公网 IPv6
  时仍仅配置 IPv4，避免不可达 IPv6 DNS 造成部分解析失败。

### Changed

- kejilion.sh 应用市场安装、更新和 Compose 配置统一使用 KPanel `latest` 镜像标签。
- 左侧导航将“体检”移到 Docker 下方；体检页压缩说明区、扩大终端工作区，并保持桌面
  左侧命令、右侧终端及移动端上下布局。
- Docker 容器页的“清理停止容器”和“新建容器”保持同排显示。

### Fixed

- `k web del` 已删除站点但数据库删除失败时，面板识别为带残留警告的部分成功，
  刷新站点列表并提示管理员手动核对数据库，不再把已删除站点整体报作失败。

## [0.20.8] - 2026-07-28

### Fixed

- DNS 固定协议跳过 `kejilion.sh` 的交互启动初始化，不再尝试改写只读的
  `/root/kejilion.sh`、Shell 配置或全局命令别名。
- Agent 增加 DNS 原生事务中 `cp -p` 保留属主所需的最小 `CAP_CHOWN` 能力；
  未包含隔离协议的旧脚本会在执行前被拒绝。
- 应用市场安装的后台维护与交换空间单元仅放行写入自己的 Agent 状态目录，修复
  `/home/docker/kpanel/data/agent` 被 `ProtectHome=read-only` 阻止后缺少完成凭据的问题。

## [0.20.7] - 2026-07-28

### Fixed

- 系统维护对账在写入失败状态前重新读取 worker 的原子任务状态，修复快速任务完成后被
  旧的 `2%` 启动快照覆盖成 `LoadState=not-found` 失败的竞态。
- RHEL 9 / systemd 252 使用与生产一致的 transient unit 参数完成兼容性复核，确认
  RPM 系并非因 systemd 属性不兼容而失败。

## [0.20.6] - 2026-07-28

### Fixed

- 系统更新与系统清理只接受后台 worker 写入的持久化完成凭据，不再把已回收或不存在的
  systemd 单元默认状态误判为成功。
- 维护完成结果增加实际执行的固定步骤数量和耗时；即使页面未轮询到短暂的中间阶段，
  也能区分真实完成与未验证退出。

## [0.20.5] - 2026-07-28

### Fixed

- 建站任务改由独立 `systemd` worker 持久执行，Host Agent 短暂重启或更新时不再终止
  WordPress、IP+端口反代和成品站脚本。
- 建站进度轮询遇到 Agent 502/503/504 或网络闪断时自动重连，不再把通信故障伪装成
  脚本执行失败；任务终态只接受 Agent 持久状态。
- Agent 重启后会重新关联仍在运行的建站单元，任务列表从磁盘刷新 worker 实时状态，
  继续提供原生 ANSI 彩色终端和交互输入。
- CI 与发布验收改用内置 Bash 的固定摘要镜像，不再因运行时 Alpine APK 源波动失败。

## [0.20.4] - 2026-07-27

### Fixed

- 网站一键安装改为直接调用 `k wp`、`k fd` 与对应成品站命令，并提供可关闭、可恢复的后台交互终端。
- 体检任务升级为 PTY 交互终端，保留 ANSI 彩色输出和输入通道，修复第三方脚本依赖确认失败。
- 完整删除站点可在缺少旧版资源标识时安全调用 `k web del <域名>`，并校验实际产物清理结果。

## [0.20.3] - 2026-07-27

### Added

- KPanel 镜像携带固定提交与 SHA-256 校验的 `kejilion.sh` 运行副本；安装和更新时仅从
  已接受许可协议的可信宿主机脚本继承授权及区域、统计设置。
- 体检页面改为左侧命令选择、右侧终端实时输出，移动端自动切换为上下布局。
- 概览页使用发行版官方矢量标志，不再显示单字母占位图标。
- 概览页国旗改用随面板发布的本地轻量 SVG 资源，不再依赖系统 emoji 字体或远程图片源。
- WordPress、IP+端口反代和成品站统一提供实时建站终端；域名与固定参数由面板校验，
  证书失败后可继续在终端重试或导入已有证书。

### Fixed

- 从旧版 KPanel 更新后，DNS、应用安装/更新/卸载、原生建站和体检不再因宿主机脚本
  版本滞后而整体禁用；脚本与 Agent 同步更新并支持失败回滚。
- 补回静态 DNS 所需的 `CAP_LINUX_IMMUTABLE`，确保脚本能够按原生事务处理
  `resolv.conf` 的不可变属性。
- 已完成并被 systemd 回收的系统维护任务不再误报启动失败。
- 已结束但未回写结果的应用交互任务会自动释放任务锁，不再永久阻塞下一次安装。
- 站点删除在资源版本缺失时先刷新实际状态，并显示具体字段错误，不再只提示
  `Validation failed`。
- 建站任务保留脚本返回的安全失败阶段和退出码，不再用通用提示覆盖具体原因。
- Docker 管理默认进入容器页，并统一导航、表格列宽和操作按钮分组。

## [0.20.2] - 2026-07-27

### Fixed

- Docker Hub 登录凭证改为供整个发布任务使用，使多架构镜像推送后能够读取同一 digest
  并安全提升为 `latest`。

## [0.20.1] - 2026-07-27

### Fixed

- 体检任务在系统重启或异常超时后自动结束遗留运行状态，不再永久阻塞后续体检。
- 体检页面避免慢请求被定时器重复取消，并在短暂网络错误后自动重试。
- Release 工作流为官方 Docker Hub 镜像和用户名提供默认值，发布前只要求配置
  `DOCKERHUB_TOKEN` Repository secret。

## [0.20.0] - 2026-07-27

### Added

- 新增独立“体检”页面，按 IP 与解锁、网络线路、硬件性能、综合评测四类展示
  `kejilion.sh` 测试脚本合集中的 17 个第三方检测项目。
- 体检任务通过 systemd 持久后台运行，支持单任务并发、实时日志、结果历史、预计耗时、
  资源影响和第三方来源展示。
- Agent 新增 `diagnostics.run` 能力及只接受固定 `checkId` 的体检目录、任务列表、
  任务启动和结果查询 API。

### Changed

- 变更感知核验改用 Bash 执行包含 `pipefail` 的验证脚本，修复 GitHub CI 在 Ubuntu
  `/bin/sh` 下启动失败的问题。

### Security

- KPanel 不保存或拼接第三方测试命令；目录、名称、分类和下载源均由属主及权限可信的
  `kejilion.sh` 机器协议提供。
- 体检目录只接受 HTTPS 来源和固定 selector；任务限制为一次一个、最长 90 分钟、
  最多保留 8 MiB 输出及 50 条历史，并在运行前显示 root、依赖安装与资源消耗提示。

## [0.19.0] - 2026-07-27

### Added

- 应用市场新增原生交互终端，可在受控会话中运行 `kejilion.sh` 交互式安装流程，
  并支持输入续传、任务复用和完成状态回报。
- 概览页新增 DNS 设置，直接调用可信 `kejilion.sh dns` 非交互协议，支持最多两个
  IPv4 与两个 IPv6 地址。

### Changed

- WordPress 与 IP+端口反向代理不再使用 KPanel 独立安装器或自编 Nginx 模板；
  宿主机后台任务直接执行 `kejilion.sh web` 的固定非交互分支，沿用脚本的环境安装、证书、
  数据库、官方远程配置、端口访问控制与最终产物。
- 网站页为原生反代增加独立能力检测和异步进度，WordPress、反代及成品站统一使用
  可持久化的网站任务状态，并在脚本完成后重新发现 `/home/web` 实际产物。
- 全新机器尚未生成 `/home/web` 受管目录时，网站列表返回空列表；只要可信脚本入口
  就绪，用户仍可点击新建网站并由脚本自动初始化 LDNMP。
- 已绑定域名的第三方应用优先使用域名打开；没有域名时继续回退到直接端口。

### Security

- DNS 输入经过结构化 IP 校验，不拼接 Shell；脚本按 systemd-resolved 或静态
  `resolv.conf` 实际后端事务写入，写入或服务重载失败时恢复原配置。
- Agent 仅接受属主与权限可信、声明 KPanel DNS 协议的脚本，并为静态 DNS 生命周期
  补充受 systemd 边界限制的 `CAP_LINUX_IMMUTABLE`。

## [0.18.1] - 2026-07-27

### Fixed

- KPanel 可在没有 `kejilion.sh`、`/home/web` 或既有网站环境的干净 Linux 主机上独立安装；缺失网站根目录只会禁用网站管理能力，不再导致 Agent 健康检查和整体安装失败。
- 应用市场安装器会在自动清理失败安装前直接输出 Agent、systemd 与容器状态，不再提示用户查询已经被清理的 unit。

## [0.18.0] - 2026-07-27

### Added

- Docker 管理改为环境、容器、镜像、网络和存储卷五个常驻子标签，移除功能混杂的
  “工具箱”大弹窗；每类资源拥有独立状态、搜索、创建、维护与清理入口。
- 容器新增支持端口、卷、环境变量和启动参数的结构化创建、按需性能采样、
  受审计的单次控制台命令，以及与
  `kejilion.sh` `DOCKER-USER` 规则形态一致的允许/阻止外部访问。
- `kejilion.sh` 以单容器方式安装且保留 `<容器名>_port.conf` 标记的应用，
  经危险配置检查后可在 Docker 页继续执行生命周期、日志、性能和访问控制。
- Docker 备份增加持久化列表、冲突保护还原和仅使用既有 SSH 密钥及
  `known_hosts` 的迁移；备份保存在 `/home/docker/.kpanel-backups`，KPanel
  更新或卸载不会顺带删除。
- Docker 环境状态直接读取 Engine 和现有 `daemon.json`，可识别官方、脚本同源和
  自定义镜像源，并回显 IPv6、存储驱动与 Data Root。

### Changed

- 镜像、网络、存储卷和停止容器支持各自独立的后台清理，不再要求进入全局清理。
- Docker 环境更新复用跨发行版系统后台维护任务；Docker 自卸载因会同步终止
  KPanel，继续明确交回 `k docker` 或 SSH。
- 还原只写入不存在的 `/home/docker` 顶层项目；同名目录在任何写入前拒绝，
  `appno.txt` 使用去重合并并保留数值属主，KPanel 自身目录和端口标记永不进入备份或还原。

### Security

- 新建容器只接受类型化镜像、端口、已有 local 卷、环境变量、非 host 网络、重启策略和参数，
  禁止任意 `docker run` 文本；启动失败自动删除刚创建的容器。
- 控制台只对归属明确、无 privileged/host namespace/额外 capability/设备/
  Docker Socket/越界 bind mount 的运行中容器开放；无 TTY、单次 20 秒、
  输出 64 KiB/1000 行，并且命令不写入审计变化、任务状态或 API 响应元数据。
- 访问控制执行前复核容器版本和唯一 Docker IPv4；iptables 变更或持久化失败会
  反向恢复本次规则，不接收原始防火墙命令。
- 还原拒绝路径穿越、链接、设备文件、超限归档和同名覆盖；SSH 迁移不接收密码、
  不关闭主机密钥校验，目标固定为 `/tmp/<backup-id>`。

## [0.17.2] - 2026-07-27

### Fixed

- Agent systemd 沙箱补充受边界和 `ReadWritePaths` 双重限制的 `CAP_DAC_OVERRIDE`，
  使 `/home/web/html` 由脚本用户而非 root 拥有时，静态站、PHP/WordPress 和完整
  站点删除仍可处理标准 Kejilion 目录；不扩大到 Panel 登录/审计数据目录。

## [0.17.1] - 2026-07-27

### Added

- 网站管理新增删除入口：脚本与 Panel 创建、结构可确认且未漂移的站点均可移除
  Nginx 入口，或按 `k web` 布局完整清理站点目录、域名证书和同名数据库。

### Fixed

- 应用市场解除域名绑定改用带删除范围和域名确认的新接口，继续复用网站管理的
  Nginx 校验与回滚事务。

### Security

- 删除必须携带当前 `resourceVersion`、明确范围并逐字确认主域名；完整删除额外验证
  配置哈希、配置文件名、站点目录和 TLS 路径，使用同文件系统原子暂存、`nginx -t`
  与 reload 失败回滚。
- 同名数据库只通过固定 `mysql` 容器和固定 SQL 形态删除，数据库名严格派生自域名，
  root 凭据仅通过 Docker Exec 环境传入，不进入命令参数、API、日志或审计。

## [0.17.0] - 2026-07-27

### Added

- Docker 管理新增持久化后台任务：镜像拉取/删除、网络创建/删除/加入/退出、存储卷创建/删除、
  确认式资源清理和 `/home/docker` 私有备份；容器可在停止且归属验证后安全删除。
- Docker 镜像源支持 `kejilion.sh` 同源中国大陆镜像组与官方线路恢复；Docker IPv6 支持真实
  IPv6/ULA `/64`，配置变更保留未知 `daemon.json` 字段并在重启失败时自动回滚。
- 新增 Discuz、Kodbox、MacCMS、独角数卡、Flarum、Typecho、LinkStack 和 AI Prompt
  一键建站，通过 `kejilion.sh` 固定非交互协议复用原生业务分支与产物。
- 脚本创建且结构可确认的静态、PHP、反代、负载均衡和重定向站点支持 Web 安全更新；
  只修补已识别指令，保留证书、安全规则和未知 Nginx 配置。

### Changed

- 系统概览先显示本地主机核心数据，再独立加载 Agent、网站、Docker 与公网身份；公网 IP/ISP
  使用 30 分钟 stale-while-revalidate 缓存。
- Docker 先显示摘要和容器，再并行加载镜像、网络、存储卷；网站和应用市场先显示主列表，
  能力与域名信息随后补齐。
- 应用市场动态第三方目录改为后台刷新和 5 分钟缓存，冷启动或上游超时不再阻塞内置目录。
- Agent 能力探测由串行改为并行；变更任务与安全审计合并为导航中的“活动记录”。

### Security

- Docker 变更只接受固定动作和类型化字段，使用资源版本、归属校验和 KPanel 资源保护；
  不开放任意 Exec、Compose、命令、覆盖式恢复、关闭主机校验的 SSH 迁移或 Docker 自卸载。
- 一键建站仅接受固定菜单编号和域名，拒绝覆盖已有配置/目录；不持久化可能包含数据库凭据的
  脚本原始输出。

## [0.16.2] - 2026-07-27

### Fixed

- 系统更新、系统清理和 Swap 后台任务改为使用当前运行中的 Agent 绝对路径，兼容
  `/usr/local/libexec/kejilion-agent` 标准安装与
  `/home/docker/kpanel/bin/kejilion-agent` 应用市场安装，不再错误回退到不存在的
  FHS 路径。
- 软件源位于自定义目录或缺少 `journalctl` 时不再误判整个维护功能不可用；原生包
  管理器负责校验其仓库，标准清理会安全跳过不可用的可选日志步骤。

### Changed

- 更新与清理覆盖 APT/dpkg、DNF/DNF5/YUM、APK、Pacman 和 Zypper；无法识别的
  Linux 发行版可按本机唯一受支持的原生包管理器安全降级。
- Pacman 标准清理对 `pacman -Qdtq` 返回的孤立包名进行数量、格式和重复校验后，
  以固定参数直接调用 `pacman -Rns`，不经过 Shell。

### Security

- 保持 KPanel 安全清理边界：不删除软件包锁、完整日志目录、`/tmp`、网站数据或
  Docker 资源；journal 默认保留 7 天并限制为 500 MiB。

## [0.16.1] - 2026-07-27

### Added

- 安装、更新、卸载和访问策略变更统一为可恢复查看的后台任务，并在应用市场显示实时阶段、进度与日志。
- 识别 `docker_app_service` 主服务容器，并持久化脚本/Web 共用的访问策略状态。

### Changed

- `kejilion.sh` 应用市场安装 KPanel 成功后直接显示首次初始化 Token，并提示完成管理员账户初始化后 Token 自动失效，不再要求用户手动读取凭证文件。
- 对齐 `kejilion.sh` 原生应用管理：脚本安装的内置与第三方应用在安装标记、配置和主容器精确匹配后，可继续更新、卸载和切换 IP + 端口访问。

### Security

- 第三方应用仅只读解析 `/root/apps/<token>.conf` 的字面量容器字段，不执行配置；拒绝软链接、非 root 所有或可写配置。

## [0.16.0] - 2026-07-26

### Added

- 应用安装改为可持久化后台任务：提交后立即返回任务 ID，应用市场和任务中心持续显示
  阶段、百分比、结果与最近日志，离开页面或刷新浏览器不会中断安装。
- `kejilion.sh` 新增受限的 KPanel 非交互安装协议。当前审计快照中 99 个标准
  `docker_app`/`docker_app_plus` 内置应用及官方第三方标准应用可直接后台安装，
  产物继续由脚本原生函数创建。
- Agent 重启时会核对 systemd 临时安装服务；已中断任务转为“需要处理”，避免永久
  卡在队列并阻塞后续安装。

### Changed

- 应用市场默认进入“已安装”，并将该筛选放在首位；列表下方新增“浏览全部应用”入口。
- 未安装且具备安装能力的应用卡片直接显示“安装”，其余专属交互应用明确显示需要
  配置向导。
- 安装端口可留空沿用脚本默认值；标准应用复用 `kejilion.sh` 的镜像、Compose、
  数据目录、账号提示、`appno.txt` 与端口文件业务。

### Security

- 后台脚本任务只接受固定 `install` 动作、受限应用编号/token 和
  `1-65535` 端口并检查占用；由 root-only Agent 创建独立 systemd 服务，Web 容器仍不
  挂载 Docker Socket 或宿主机脚本。
- 任务状态与有界日志使用 `0600` 持久化，只向已认证面板会话提供；并发应用安装
  首版串行化，避免端口、包管理器和脚本状态文件争用。
- KPanel 不自动接受 `kejilion.sh` 首次许可；仅当用户已在终端明确接受且脚本支持
  非交互协议时开放直接安装。

## [0.15.1] - 2026-07-26

### 修复

- 应用市场安装、更新、卸载不再调用 `kejilion.sh` 覆盖的
  `systemctl()` 包装函数，改为校验并调用绝对路径的 systemd 客户端，
  修复创建 unit 链接后报 `Too many arguments.` 并中断的问题。
- 应用市场安装现在从 `/dev/urandom` 原子生成独立的 256-bit Agent
  Token，并以 `root:kejilion-panel 0640` 保存；更新保留既有 Token，
  解决真实 Agent 因令牌文件缺失而拒绝启动的问题。
- Agent systemd 写入白名单补齐 `/home/web/certs` 与
  `/home/web/letsencrypt`，确保 WordPress 一键搭建能够发布证书，并保持
  其余 `/home` 路径只读。
- 应用配置生命周期测试现在在与 `kejilion.sh` 相同的 `systemctl()`
  函数覆盖环境中运行，避免同类集成缺陷再次遗漏。

## [0.15.0] - 2026-07-26

### Added

- “新建网站”把 WordPress 与 IP + 端口反代置于最前，并增加“热门 / 一键成品”
  标签；其余基础站点类型保持直观卡片选择。
- WordPress 一键搭建对齐 `kejilion.sh` 的源码包、`<domain>/wordpress` 目录、
  同名数据库、现有 MySQL 账号、Redis 配置、证书文件名和 Nginx 行为。
- Agent 新增 `sites.wordpress.install` 能力探测；LDNMP、MySQL、目录或证书条件
  不完整时入口明确禁用并给出原因。
- WordPress 安装改为可持久化后台任务，Web 每 2 秒读取进度；避免证书、镜像或
  源码下载超过反代超时后被误报为失败。
- systemd 沙箱仅新增独立可写目录
  `/var/lib/kejilion-panel/wordpress-jobs`，面板登录与审计数据目录继续只读隔离。

### Changed

- WordPress 使用 ACME Webroot 在现有 Nginx 在线时签发证书，不再照搬脚本停止
  整个 Nginx 容器的实现；最终业务产物与脚本保持一致。
- 安装过程中先发布仅服务 ACME 的临时配置，最终配置通过 `nginx -t` 后原子切换。
- 已存在脚本标准 `/home/web/docker-compose.yml`、但 MySQL/PHP/Redis 当前停机
  的主机，只启动这四个固定服务且使用 `--no-recreate`，不重建 Nginx。
- Kejilion 应用市场安装配置、宿主 Agent 版本校验与 Docker 镜像引用同步更新
  到 `0.15.0`。

### Security

- 固定 WordPress 源码包 SHA-256 与 Certbot 多架构镜像 digest；远端内容变化时
  拒绝安装，避免未审计代码静默进入宿主机。
- 同名域名、目录、配置、证书或数据库一律冲突拒绝，不执行脚本的“同名先删除”逻辑。
- 数据库与 Certbot 只使用固定 Docker API 操作；失败时核对内容后回滚本次新建的
  Nginx 配置、目录、数据库和复制证书，不删除任何既有业务产物。

## [0.14.0] - 2026-07-26

### Added

- 官方容器镜像同时携带同架构 `kejilion-agent`、systemd unit、Compose 与环境变量模板，
  可由 `kejilion/apps` 配置完成一站式安装。
- 支持在 IP + 端口部署后直接使用 `k fd <domain> 127.0.0.1 <port>` 添加 HTTPS 域名。

### Changed

- 直连端口覆盖改为单一 bridge 网络，确保 `kejilion.sh` 的容器端口访问策略能取得
  唯一、有效的容器 IP。

### Security

- 仅可信代理 CIDR 可提供动态 HTTPS Host/Origin；代理 HTTPS 自动启用 Secure Cookie。
- 当 `k fd` 模板未保留 `X-Real-IP` 时，从右向左验证 `X-Forwarded-For`，避免客户端
  伪造地址影响登录限速和审计。

## [0.13.1] - 2026-07-26

### Fixed

- IP + 端口覆盖文件为 Panel 增加独立发布网络，修复 Docker `internal` 网络不会
  建立宿主端口转发的问题；原内部 Agent 网络与固定私网地址保持不变。

## [0.13.0] - 2026-07-26

### Added

- 应用市场由 Agent 从固定的 `https://app.kejilion.sh/` 动态同步第三方入驻目录，
  五分钟缓存并在上游不可用时回退最近一次安全目录或内置快照。
- 新增可选 `direct-port.yml`，支持测试服务器以 IP + 端口访问而不依赖 Nginx。

### Fixed

- Docker 镜像构建现在会复制 `web/public`，应用市场图标不再在生产镜像中缺失。

### Security

- 远程应用目录仅作为展示元数据：固定 HTTPS 来源、限制响应大小和超时、严格校验
  分类、数量、ID、URL 与重复项；远程数据不能获得安装或 Shell 执行权限。
- 新入驻但尚未随版本发布图标的应用使用本地通用图标，不加载第三方远程图标。

本项目遵循语义化版本。所有日期均使用 `YYYY-MM-DD`。

## [0.12.0] - 2026-07-26

### Added

- 系统更新源切换对齐 `kejilion.sh` 的四个业务入口：中国大陆默认、中国大陆教育网、
  海外地区和智能切换；页面改为直观的线路卡片，不再使用二选一下拉框。
- 智能模式沿用脚本判定：公网国家为 `CN` 时使用华为云，其他地区使用发行版官方源；
  地区查询失败时明确回退官方源。
- Agent 可识别 LinuxMirrors 写入的 one-line 与 DEB822 APT 地址，包括带镜像路径前缀的
  海外源，确保脚本换源后 Web 能继续呈现和安全切换。

### Changed

- 中国大陆默认、教育网和海外入口分别固化为 LinuxMirrors 对应列表的首选阿里云、
  北京大学和 xTom 香港线路；换源本身不升级软件、不清缓存。
- 当前写入范围仍严格限定为 Debian/Ubuntu 发行版仓库；RPM、Pacman、Zypper 与第三方
  仓库继续只读，避免未经充分适配就改写系统源。

### Security

- Web 只接受四个固定枚举，不接受 URL 或 Shell；Agent 不下载、执行 LinuxMirrors 在线脚本。
- 修改前创建版本化备份，使用隔离 APT lists/cache 执行短超时 `apt-get update`；验证失败
  自动恢复全部已修改文件，Docker、NodeSource 等第三方源保持不变。

## [0.11.0] - 2026-07-26

### Added

- 系统监控与管理新增服务器重启入口；要求输入大写 `REBOOT` 并勾选二次确认。
- Agent 使用固定参数的 systemd transient timer 延迟 15 秒执行重启，为 Panel 写入成功审计和
  浏览器接收结果预留时间。

### Security

- 重启接口只接受 `action=reboot` 和固定确认值，不接受命令、执行时间或 Shell 参数。
- 系统更新或清理任务运行期间拒绝重启；宿主机缺少 `systemctl` 或 `systemd-run` 时能力保持禁用。

## [0.10.0] - 2026-07-26

### Added

- 新增应用市场，完整收录 `app.kejilion.sh` 当前 146 个应用、7 个分类和本地图标，并与
  `kejilion.sh` 的 115 个内置应用编号、主容器、镜像和默认端口建立审计映射。
- 应用详情整合运行状态、启动/停止/重启、镜像更新检查、域名绑定与解绑、IP+端口访问策略、
  更新和卸载；首批为 LibreSpeed、IT-Tools 和 DOS 游戏开放声明式安装器。
- 新增 Panel 管理的反向代理安全删除事务；仅允许删除未漂移的 Web 来源反向代理，删除失败时
  自动恢复原配置并重新校验、加载 Nginx。

### Changed

- KPanel 安装的应用同步写入 `/home/docker/appno.txt` 和对应 `*_port.conf`，脚本端可继续识别；
  Docker 仍是运行状态真相，孤立脚本标记显示为待核对。
- “阻止 IP+端口”改为把经过适配的容器监听地址切换到 `127.0.0.1`，不写入全局 iptables，
  域名反向代理继续通过宿主机回环地址访问。

### Security

- Agent 不执行 `k app`、远程 Shell、第三方 Compose 或脚本菜单；未审计应用完整展示但写操作
  保持禁用，并明确给出原因。
- 应用更新先拉取镜像，再使用固定容器规格重建；创建或启动失败时恢复原镜像、端口和运行状态。
  卸载仅接受资源版本一致、无挂载、无特权、配置完全匹配的声明式容器。

## [0.9.0] - 2026-07-26

### Added

- 新建网站扩展为静态站、PHP 动态站、IP/端口反代、域名反代、负载均衡和
  域名重定向六类服务，并提供不使用下拉框的卡片式选择向导。
- PHP 支持脚本现有 `php` 与 `php74` Socket；重定向支持
  301/302/307/308；负载均衡支持 2–8 个 HTTP 后端。
- 发现器可区分域名反代、负载均衡和 WordPress，并忽略 ACME Webroot 对
  实际站点根目录识别的干扰。

### Changed

- Panel 管理模板升级到 v2，沿用 `/home/web/conf.d`、`/home/web/html` 与
  Nginx 容器路径；旧 v1 固定模板在安全更新时原地迁移，不修改业务目录。
- 域名反代固定上游 Host 与 SNI；所有写入继续使用资源版本、原子替换、
  `nginx -t`、reload 和失败回滚。

### Security

- IP/端口反代仅允许本机、私网地址或 Docker DNS 名；域名反代和跳转目标
  必须是 ASCII FQDN；负载均衡目标数量、协议、端口和重复项均受限制。
- 应用安装器、Nginx Stream 和 TLS 签发不混入普通网站事务，避免执行远程
  `main` 脚本、处理数据库凭据、修改全局 Nginx 或停止现有服务。

## [0.8.0] - 2026-07-26

### Added

- 系统首页补齐 `kejilion.sh` 的 `k info` 主机信息：CPU 型号与频率、
  1/5/15 分钟负载、TCP/UDP 连接数、宿主机时间、公网 IPv4/IPv6、
  ISP 和地理位置。
- 公网信息查询使用固定 IPv4/IPv6 HTTPS 端点、严格地址族校验和 30 分钟缓存；
  查询失败不影响本地主机监控，并可通过 Agent 环境变量关闭。

### Changed

- 系统首页将主机、网络与位置、资源一致性重新分组；内存、Swap、根磁盘、
  累计流量、DNS、时区、拥塞算法和 qdisc 继续以宿主机实时状态为准。
- 系统摘要 API 扩展为向后兼容的可选字段；旧 Agent 未提供公网信息时页面明确
  显示查询不可用，不伪造默认数据。

### Security

- Agent 不接受外部查询 URL 或任意命令；响应限制为 64 KiB，设置短超时、
  同主机重定向限制、控制字符过滤和字段长度上限。

## [0.7.0] - 2026-07-26

### Added

- 宿主机软件包维护新增 RHEL/Fedora 系 DNF/DNF5/YUM、Arch/Manjaro Pacman
  和 openSUSE/SLES Zypper 固定命令序列。
- 软件源状态发现新增 `/etc/pacman.d/mirrorlist` 与
  `/etc/zypp/repos.d/*.repo`，并继续识别 RPM `.repo` 文件。
- 增加宿主机系统兼容矩阵，区分已实机验证、已实现待准入和安全只读层级。

### Changed

- Agent 根据 `/etc/os-release` 的 `ID`、`ID_LIKE`、软件包工具和源文件动态
  开放系统更新与清理；启动任务前即拒绝不满足条件的主机。
- 系统维护页面改为显示宿主机实际软件包管理器，不再把说明固定为 APT。

### Security

- 多发行版维护仍只接受 `full`、`cache`、`standard` 枚举，不能由 Web 传入
  命令、包名、仓库、路径或 Shell。
- RPM、Pacman 和 Zypper 软件源首版只读取不改写；Alpine/OpenRC 与未知
  发行版不开放宿主机写入。

## [0.6.0] - 2026-07-26

### Added

- 内核优化补齐 `kejilion.sh` 的高性能、均衡、网站、直播和游戏服五种预设，
  包括 TCP 缓冲区、连接队列、端口、虚拟内存、调度、安全、文件描述符和
  连接跟踪参数。
- 与脚本一致按宿主机 `MemTotal` 自适应缓冲区、`min_free_kbytes`、
  `swappiness` 和小内存队列，并为直播、游戏场景增加对应的 UDP/低延迟参数。
- 调优时同步处理 BBR 能力、透明大页、nofile 限制和模块持久化；页面刷新可
  识别脚本或 Web 生成的同路径、同模式产物。

### Changed

- Web 可以接管结构和标识均合法的 `kejilion.sh` 内核预设，切换后仍写入
  `/etc/sysctl.d/99-kejilion-optimize.conf`，实现脚本与面板双向识别。
- “还原默认设置”同时清理已识别的手动/自动调优产物和对应 nofile/模块设置，
  但保留通过独立 BBR 功能管理的配置。

### Security

- API 只接受五个固定预设和 `off`，逐项调用 `sysctl -w`，不接受参数名、
  参数值、文件路径或 Shell；全项失败时恢复所有文件并重新加载原配置。
- Web 不下载或执行测速型 `network-optimize.sh`；脚本产生的自动调优结果可以
  被识别和安全还原，未知同名配置仍拒绝覆盖。

## [0.5.1] - 2026-07-26

### Changed

- 虚拟内存改为识别并管理 `kejilion.sh` 使用的 `/swapfile`，脚本端和
  Web 端不再生成两套互不相认的 Swap。
- 支持直接创建、扩容、缩容和停用 `/swapfile`；页面提供脚本一致的
  1/2/4 GiB 选项以及 256–65536 MiB 自定义大小。
- 调整时自动合并旧版 `/var/lib/kejilion-panel/system/swapfile`，但保留
  Swap 分区和第三方 swapfile。

### Security

- Swap 变更通过固定参数的 systemd 一次性事务执行；不向 Web 暴露命令或
  路径，并拒绝符号链接等异常产物。
- `swapoff` 前校验受管 Swap 已用空间与可用内存；创建、`fstab` 更新或
  激活失败时恢复原文件、原启动项和原活动状态。
- 不复用 `kejilion.sh` 中 `wipefs`、重建 Swap 分区的高风险行为。

## [0.5.0] - 2026-07-26

### Added

- 系统监控与管理页增加“系统更新”和“系统清理”，对应 154 当前
  `kejilion.sh` 的 `linux_update` 与 `linux_clean` 业务。
- 系统维护通过固定参数的 systemd 后台任务运行，浏览器断开或 Panel 重启
  不会中断 APT/dpkg，并持续返回阶段、进度、结果和重启提示。
- 清理提供“仅缓存”和“标准安全清理”；标准模式移除无用依赖、清理 APT
  缓存，并保留最近 7 天且限制最大 500 MiB 的 journal。

### Security

- 不复制脚本中的 `pkill -9 apt/dpkg`、删除 dpkg 锁、清空 `/var/log`、
  清空 `/tmp` 或 Docker prune；Web API 仍不接受命令、路径或包名。

## [0.4.0] - 2026-07-26

### Added

- DNS 管理增加 Cloudflare、Google、阿里云、腾讯 DNSPod 和 Quad9 便捷预设，
  同时保留自定义地址。
- 时区管理增加常用城市与 IANA 时区下拉选择，同时保留其他合法时区的
  自定义入口。
- 打开管理弹窗时自动识别当前 DNS 和时区，不会默认改写宿主机配置。

## [0.3.0] - 2026-07-26

### Added

- 系统管理页开放主机名、安全新增 SSH 端口、systemd-resolved DNS、时区、
  KPanel 专属 Swap、Debian/Ubuntu APT 镜像源、V4/V6 优先级、内核优化
  预设和 BBR 的类型化写入。
- 每次系统变更均记录意图与结果审计，并在宿主机保存变更前配置快照；执行失败
  自动回滚，成功后重新读取真实状态。
- SSH 新端口在 `sshd -t`、防火墙放行、reload 和监听探测全部成功前不会完成，
  且首版保留所有旧端口，避免远程失联。

### Security

- Web 与 Agent 均只接受固定 action 和字段，不接受命令名、Shell、脚本内容或
  任意目标路径；所有写请求继续强制登录、同源、CSRF 和审计。
- Swap 只管理 `/var/lib/kejilion-panel/system/swapfile`，不会停用现有 Swap；
  APT 切换不修改 Docker、NodeSource 等第三方仓库。
- 检测到外部 `kejilion.sh` 内核调优配置时拒绝覆盖；系统重装仍保持锁定。

## [0.2.0] - 2026-07-26

### Added

- “概览”升级为“系统监控与管理”，整合主机名、SSH 端口、DNS、时区、
  Swap、系统镜像源、V4/V6 优先级、内核优化、BBR 与重装系统入口。
- Agent 直接读取宿主机配置，识别 `kejilion.sh`、人工或其他工具产生的
  SSH、DNS、时区、Swap、软件源、`gai.conf`、sysctl 与 BBR 实际状态。
- 系统写能力以独立 capability 呈现，页面显示当前状态、安全要求和禁用原因。

### Security

- v0.2.0 不执行系统配置变更；SSH、DNS、Swap、内核和重装等操作在具备
  白名单输入、前置校验、回读验证与回滚前保持锁定。
- 不复用脚本中清理防火墙、清除全部 Swap、锁定 `resolv.conf` 或下载后
  直接执行远程脚本等高风险组合命令。

## [0.1.3] - 2026-07-25

### Added

- 设置页支持验证当前密码后修改管理员密码；密码状态原子持久化，并立即注销该管理员的全部现有会话。

### Security

- 密码修改接口强制登录、同源和 CSRF 校验，复用 Argon2id 及密码强度策略，审计记录不包含密码内容。

## [0.1.2] - 2026-07-25

### Fixed

- 保留 Docker `internal` 网络隔离，同时为 Panel 分配确定的私网地址并取消无效的宿主端口发布；宿主健康检查和 Kejilion Nginx 直接访问该私网端点。
- 发布构建通过 BuildKit secret 注入可选代理，避免把构建基础设施地址写入镜像 Provenance；镜像记录源提交 revision，并在验收版本 digest 后才更新 `latest`。

## [0.1.1] - 2026-07-25

### Fixed

- 修复 systemd `ProtectProc=invisible` 隐藏 dockerd 进程，导致 Agent 的 Docker socket 防激活检查误判 `docker_unavailable`；改为显式 `ProtectProc=default`，其余沙箱限制保持不变。
- 防止 systemd 接管非 root Panel 数据目录的属主，并在 Agent、Panel 启动前后增加目录属主与权限门禁。

## [0.1.0] - 2026-07-25

首个可部署版本。

### Added

- 用户可见产品名称确定为 KPanel；为保持兼容，内部路径、服务、容器、网络及环境变量继续使用既有 `kejilion-panel` 标识。
- 一次性初始化、安全登录、Argon2id 密码哈希、服务端 Session、CSRF/Origin/Host 校验和登录限速。
- 通过受限 Unix Socket Agent 读取 Linux 主机状态、`/home/web` 网站产物和 Docker 资源。
- 现有静态站、反向代理站、证书及未知 Nginx 配置的只读发现与资源版本展示。
- 固定模板静态站和内网反向代理站的安全创建、更新、`nginx -t`、原子替换、reload 与失败回滚。
- 经归属验证的 Kejilion 容器启动、停止、重启、有界日志与一次性资源统计。
- 管理变更记录、结构化审计、敏感字段脱敏和 Agent 离线只读降级。
- 非 root 多架构 Panel 镜像、双架构 Agent、校验和、SBOM/Provenance 发布流程及隔离安装器。

### Compatibility

- 兼容基线为 `kejilion.sh` v4.5.2 与 Nginx 模板提交 `05f5a2eac269967706f30dc3ff7985339e1f3ce4`。
- Panel 不修改、执行或 `source` `kejilion.sh`；宿主机真实产物始终是事实来源。
- 脚本侧或人工修改会在下一次发现时呈现；Web 侧仅写入脚本既有路径和命名约定。

### Security

- 安装器仅允许全新安装，并固定使用本机 Docker Socket；dry-run 不连接 Docker daemon，也不启动服务。
- 安装前校验 Agent 版本、镜像 digest/用户/健康检查/版本标签、专用权限组、systemd/Compose 归属、监听端口、`/home/web` 和 Docker 子网冲突。
- 正式安装要求 Agent、Panel、Panel→Agent 及宿主回环端口全部健康；失败时尝试停止本次新进程并复核状态，无法确认则发出 `CRITICAL`，同时保留日志供恢复。
- 单 IP 与账户采用分级登录限速；成功登录按发生时间重置此前失败，避免少量匿名请求锁死管理员。
- Dockerfile frontend、构建镜像、BuildKit 与发布 Actions 均固定版本或摘要；发布前执行镜像运行时安全合约检查。

### Known limitations

- 首版不提供网站、证书、数据库、目录或 Docker 资源删除。
- 首版不提供任意 Shell、任意 Docker Exec、Compose 在线编辑、系统重装或应用市场远程脚本。
- 未改造的 `kejilion.sh` 与 Web 不共享事务锁；外部并发变更通过资源版本和再次校验拒绝覆盖。
