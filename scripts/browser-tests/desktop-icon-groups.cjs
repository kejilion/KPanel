const assert = require('node:assert/strict')
const fs = require('node:fs')
const os = require('node:os')
const { execFileSync } = require('node:child_process')
const { chromium } = require(process.env.KPANEL_PLAYWRIGHT_MODULE || 'playwright')
const base = new URL(process.env.COMBINED_PREVIEW).origin
assert.equal(new URL(base).hostname, '127.0.0.1')
const out = process.env.EVIDENCE_DIR
const candidate = execFileSync('git', ['rev-parse', 'HEAD'], { encoding: 'utf8' }).trim()
const draft = process.env.PREVIEW_GRADE === 'draft'
if (!draft) {
  assert.equal(candidate, process.env.REPORT_CANDIDATE)
  assert.equal(execFileSync('git', ['status', '--porcelain'], { encoding: 'utf8' }).trim(), '')
}
fs.mkdirSync(out, { recursive: true })
const report = { candidate, grade: draft ? 'draft' : 'acceptance', mode: 'mock-ui', cases: [], errors: [], cleanup: false }
;(async () => {
  const browser = await chromium.launch({ headless: true, executablePath: process.env.KPANEL_BROWSER_EXECUTABLE || undefined })
  report.browser = browser.version()
  let resourceFailure = false
  const watchdog = setInterval(() => {
    const disk = fs.statfsSync(process.cwd())
    if (os.freemem() < 512 * 1024 ** 2 || disk.bavail * disk.bsize < 1024 ** 3) { resourceFailure = true; void browser.close() }
  }, 1000)
  try {
    const context = await browser.newContext({ viewport: { width: 1920, height: 1080 } })
    await context.addInitScript(() => {
      localStorage.setItem('kejilion-panel-desktop-mode', 'desktop')
      localStorage.setItem('kejilion-panel-desktop-windows', '[]')
      if (!localStorage.getItem('kejilion-panel-locale')) localStorage.setItem('kejilion-panel-locale', 'zh-CN')
      if (!localStorage.getItem('kejilion-panel-theme')) localStorage.setItem('kejilion-panel-theme', 'dark')
    })
    const page = await context.newPage()
    page.setDefaultTimeout(12000)
    page.on('pageerror', error => report.errors.push(error.message))
    await context.tracing.start({ screenshots: true, snapshots: true })
    // Isolated per-context mock workspace: never overwrite the user's preview state.
    let state = { schemaVersion: 5, available: true, resourceVersion: 'sha256:' + '1'.repeat(64), groups: [], positions: {}, widgetPositions: {}, hiddenEntryKeys: [], hiddenWidgetKeys: [], labels: {}, shortcuts: [] }
    let writes = 0, failNext = false
    await page.route('**/api/v1/desktop/workspace', async route => {
      if (route.request().method() === 'PUT') {
        if (failNext) { failNext = false; return route.fulfill({ status: 500, json: { title: 'Injected save failure', code: 'desktop_workspace_unavailable' } }) }
        const input = route.request().postDataJSON()
        assert.equal(input.expectedResourceVersion, state.resourceVersion)
        state = { ...state, ...input, resourceVersion: 'sha256:' + (++writes + 1).toString(16).padStart(64, '0') }
      }
      await route.fulfill({ json: state })
    })
    const slot = key => page.locator(`[data-icon-key="${key}"]`)
    const group = page.locator('.desktop-group').first()
    const settle = async () => page.evaluate(async () => {
      await new Promise(requestAnimationFrame); await new Promise(requestAnimationFrame)
      await Promise.all(document.getAnimations().map(animation => animation.finished.catch(() => {})))
      await new Promise(requestAnimationFrame)
    })
    const save = async action => {
      const response = page.waitForResponse(r => r.url().endsWith('/api/v1/desktop/workspace') && r.request().method() === 'PUT')
      await action(); assert.equal((await response).status(), 200); await settle()
    }
    const drag = async (locator, x, y) => {
      await locator.scrollIntoViewIfNeeded(); await settle()
      const box = await locator.boundingBox(); assert(box)
      await page.mouse.move(box.x + box.width / 2, box.y + Math.min(24, box.height / 2))
      await page.mouse.down(); await page.mouse.move(x, y, { steps: 16 }); await page.mouse.up()
    }
    await page.goto(base + '/overview')
    await slot('nav:/overview').waitFor()
    await settle()
    assert.equal(await group.count(), 0)
    assert.equal(writes, 0)
    // Exercise the real multi-select workflow and sample live CSS transitions, not just final states.
    const inspectMotion = async (button, phase) => {
      const samples = await button.evaluate(async element => {
        element.click()
        await new Promise(requestAnimationFrame); await new Promise(requestAnimationFrame)
        await new Promise(requestAnimationFrame)
        const animations = document.getAnimations().filter(animation => {
          const target = animation.effect?.target
          return target instanceof Element && (target.matches('.desktop__icon-slot') || target.matches('.desktop-group'))
        })
        return animations.map(animation => {
          const target = animation.effect.target
          animation.pause()
          const duration = Number(animation.effect.getTiming().duration)
          animation.currentTime = 0
          const start = { transform: getComputedStyle(target).transform, opacity: getComputedStyle(target).opacity }
          animation.currentTime = duration * .4
          const middle = { transform: getComputedStyle(target).transform, opacity: getComputedStyle(target).opacity }
          return { target: target.matches('.desktop-group') ? 'surface' : 'icon', duration, start, middle, inert: target.inert }
        })
      })
      report.cases.push({ phase, samples })
      assert(samples.some(sample => sample.target === 'icon' && sample.duration === 280 && sample.start.transform !== sample.middle.transform), `${phase}: missing icon travel`)
      assert(samples.some(sample => sample.target === 'surface' && sample.start.opacity !== sample.middle.opacity), `${phase}: missing surface fade`)
      if (phase === 'dissolve') assert(samples.filter(sample => sample.target === 'surface').every(sample => sample.inert))
      const feedbackOverlaps = await page.evaluate(() => {
        const selection = document.querySelector('.desktop__selection-actions')?.getBoundingClientRect()
        const undo = document.querySelector('.desktop-group-undo')?.getBoundingClientRect()
        return Boolean(selection && undo && selection.top < undo.bottom && undo.top < selection.bottom)
      })
      assert.equal(feedbackOverlaps, false)
      await page.screenshot({ path: `${out}/${phase}-mid-motion.png` })
      await page.evaluate(() => document.getAnimations().forEach(animation => animation.play()))
    }
    const selected = ['nav:/overview', 'nav:/terminal', 'nav:/files', 'nav:/docker', 'nav:/settings', 'nav:/system']
    await slot(selected[0]).locator('button').click()
    for (const key of selected.slice(1)) await slot(key).locator('button').click({ modifiers: ['Control'] })
    failNext = true
    const failedCreation = page.waitForResponse(r => r.url().endsWith('/api/v1/desktop/workspace') && r.status() === 500)
    await page.locator('.desktop__selection-actions').getByRole('button', { name: '编为一组', exact: true }).click()
    await failedCreation; await settle()
    assert.equal(state.groups.length, 0)
    assert.equal(await page.locator('.desktop__selection-actions').count(), 1)
    await save(() => inspectMotion(page.locator('.desktop__selection-actions').getByRole('button', { name: '编为一组', exact: true }), 'create'))
    assert.equal(await page.locator('.desktop-group-form').count(), 0)
    assert.equal(state.groups[0].name, '新分组')
    assert.equal(state.groups[0].rows, 0)
    report.cases.push('one-click grouping with default name, no modal; failed creation restores selection for retry')
    assert.equal(state.groups[0].columns, 4)
    assert.equal(state.groups[0].members.length, 6)
    assert.equal(await group.locator('[data-group-cell]').count(), 8)
    const row = await group.locator('[data-group-cell]').evaluateAll(cells => cells.slice(0, 5).map(cell => cell.getBoundingClientRect().top))
    assert(row.slice(0, 4).every(y => y === row[0])); assert(row[4] > row[0])
    await group.locator('.desktop-group__menu').click()
    await save(() => inspectMotion(page.getByRole('menuitem', { name: '解散分组', exact: true }), 'dissolve'))
    assert.equal(await group.count(), 0)
    await save(() => page.locator('.desktop-group-undo').getByText('撤销', { exact: true }).click())
    assert.equal(state.groups[0].members.length, 6)
    await group.locator('.desktop-group__menu').click()
    const fastRemoval = page.waitForResponse(r => r.url().endsWith('/api/v1/desktop/workspace') && r.request().method() === 'PUT')
    await page.getByRole('menuitem', { name: '解散分组', exact: true }).evaluate(element => element.click())
    assert.equal((await fastRemoval).status(), 200)
    assert.equal(await page.locator('.desktop-group-surface-leave-active').count(), 1)
    await save(() => page.locator('.desktop-group-undo').getByText('撤销', { exact: true }).evaluate(element => element.click()))
    assert.equal(await group.count(), 1)
    assert.equal(await group.evaluate(element => element.inert), false)
    report.cases.push('undo during surface exit restores one interactive group without a ghost')
    await group.locator('.desktop-group__menu').click()
    failNext = true
    const failedDissolution = page.waitForResponse(r => r.url().endsWith('/api/v1/desktop/workspace') && r.status() === 500)
    await page.getByRole('menuitem', { name: '解散分组', exact: true }).evaluate(element => element.click())
    await failedDissolution; await settle()
    assert.equal(await group.count(), 1)
    assert.equal(await group.evaluate(element => element.inert), false)
    await page.locator('.desktop-group-undo[role="alert"]').waitFor()
    await page.keyboard.press('Escape'); await settle()
    report.cases.push('failed dissolve restores the group and persistent error without an inert survivor')
    await page.emulateMedia({ reducedMotion: 'reduce' })
    await group.locator('.desktop-group__menu').click()
    await save(() => page.getByRole('menuitem', { name: '解散分组', exact: true }).click())
    assert.equal(await group.count(), 0)
    assert.equal(await slot(selected[0]).evaluate(element => getComputedStyle(element).transitionProperty), 'none')
    await page.emulateMedia({ reducedMotion: 'no-preference' })
    report.cases.push('reduced motion disables icon travel and removes the surface immediately')
    // Return only this isolated mock fixture to the initial state for the existing journey matrix.
    state = { ...state, groups: [], positions: {} }
    await page.reload(); await slot('nav:/overview').waitFor(); await settle()
    writes = 0
    const hoverIcon = async (from, to) => {
      const a = await slot(from).boundingBox(), b = await slot(to).boundingBox()
      await page.mouse.move(a.x + 45, a.y + 24); await page.mouse.down()
      await page.mouse.move(b.x + 45, b.y + 24, { steps: 12 })
    }
    await hoverIcon('nav:/terminal', 'nav:/overview')
    await page.locator('.desktop__icon-slot--group-ready').waitFor()
    await page.screenshot({ path: `${out}/dwell-ready.png` })
    await page.keyboard.press('Escape'); await page.mouse.up(); await settle()
    assert.equal(state.groups.length, 0); assert.equal(writes, 0)
    assert.equal(await page.locator('.desktop__icon-slot--group-candidate').count(), 0)
    const quickTarget = await slot('nav:/overview').boundingBox()
    await save(() => drag(slot('nav:/terminal'), quickTarget.x + 45, quickTarget.y + 24))
    assert.equal(state.groups.length, 0)
    const looseBefore = await slot('nav:/files').boundingBox()
    await hoverIcon('nav:/terminal', 'nav:/overview')
    await page.locator('.desktop__icon-slot--group-ready').waitFor()
    await save(() => page.mouse.up())
    assert.deepEqual(state.groups[0].members, ['nav:/overview', 'nav:/terminal'])
    assert.equal(await slot('nav:/files').getAttribute('data-group-member'), null)
    const looseAfter = await slot('nav:/files').boundingBox()
    assert.equal(looseAfter.x, looseBefore.x); assert.equal(looseAfter.y, looseBefore.y)
    report.cases.push('default ungrouped; dwell cancel; quick pass does not group; dwell release groups; loose positions preserved')
    const nameButton = group.locator('.desktop-group__name')
    const nameInput = group.locator('.desktop-group__name-input')
    const beforeRename = await group.boundingBox()
    const beforeRenameWrites = writes
    await nameButton.click(); await nameInput.fill('临时名称')
    await nameInput.evaluate(input => {
      input.dispatchEvent(new CompositionEvent('compositionstart', { bubbles: true }))
      input.dispatchEvent(new KeyboardEvent('keydown', { key: 'Enter', isComposing: true, bubbles: true }))
      input.dispatchEvent(new CompositionEvent('compositionend', { bubbles: true }))
    })
    assert.equal(writes, beforeRenameWrites)
    await page.keyboard.press('Escape'); await settle()
    assert.equal(writes, beforeRenameWrites)
    await nameButton.click(); await nameInput.fill('   '); await page.keyboard.press('Enter'); await settle()
    assert.equal(writes, beforeRenameWrites)
    await nameButton.click(); await nameInput.fill('常用运维')
    failNext = true
    const failedRename = page.waitForResponse(r => r.url().endsWith('/api/v1/desktop/workspace') && r.status() === 500)
    await nameInput.press('Enter'); await failedRename; await settle()
    assert.equal(await nameInput.inputValue(), '常用运维')
    assert.equal(await nameInput.getAttribute('aria-invalid'), 'true')
    assert.equal(state.groups[0].name, '新分组')
    await save(() => nameInput.press('Enter'))
    assert.equal(await nameButton.evaluate(el => document.activeElement === el), true)
    await nameButton.click(); await nameInput.fill('常用运维 · 测试')
    await save(() => page.locator('.desktop__icons').click({ position: { x: 1100, y: 600 } }))
    assert.equal(state.groups[0].name, '常用运维 · 测试')
    const afterRename = await group.boundingBox()
    assert.equal(afterRename.x, beforeRename.x); assert.equal(afterRename.y, beforeRename.y)
    await page.reload(); await group.waitFor(); await settle()
    assert.equal(await nameButton.innerText(), '常用运维 · 测试')
    report.cases.push('single-click inline rename: IME Enter, Escape, blank, failed save/retry, blur save, focus and refresh')
    // Legacy minimum height disappears, while a deliberate second-row gap survives.
    state.groups[0].rows = 8
    state.groups[0].slots = { 'nav:/overview': 0, 'nav:/terminal': 4 }
    await page.reload(); await group.waitFor(); await settle()
    assert.equal(await group.locator('[data-group-cell]').count(), 8)
    const emptyCell = await group.locator('[data-group-cell="7"]').boundingBox()
    await save(() => drag(slot('nav:/overview'), emptyCell.x + 45, emptyCell.y + 24))
    assert.equal(state.groups[0].slots['nav:/overview'], 7)
    assert.equal(await group.locator('[data-group-cell="0"]').getAttribute('data-cell-empty'), 'true')
    await page.reload(); await group.waitFor(); await settle()
    assert.equal(state.groups[0].slots['nav:/overview'], 7)
    assert.equal(await group.locator('[data-group-cell="0"]').getAttribute('data-cell-empty'), 'true')
    assert.equal(state.groups[0].rows, 0)
    report.cases.push('no reserved height; explicit row gaps persist after drag and refresh')
    let box = await group.boundingBox()
    const addCell = await group.locator('[data-group-cell="4"]').boundingBox()
    await save(() => drag(slot('nav:/files'), addCell.x + 45, addCell.y + 24))
    assert(state.groups[0].members.includes('nav:/files'))
    assert.equal(await slot('nav:/files').getAttribute('data-group-member'), state.groups[0].id)
    const first = await slot('nav:/overview').boundingBox()
    await save(() => drag(slot('nav:/files'), first.x + 15, first.y + 24))
    assert.equal(state.groups[0].slots['nav:/files'], 7)
    assert.equal(state.groups[0].slots['nav:/overview'], 4)
    box = await group.boundingBox()
    // The old reserved grid extends beyond the new painted edge; it must not capture drops.
    await save(() => drag(slot('nav:/files'), box.x + box.width + 24, box.y + 70))
    assert(!state.groups[0].members.includes('nav:/files'))
    report.cases.push('pointer drag in, reorder and out past compact painted edge')
    const memberBefore = await slot('nav:/overview').boundingBox()
    await save(() => drag(group.locator('.desktop-group__header'), 680, 140))
    const moved = await slot('nav:/overview').boundingBox()
    assert(Math.abs(moved.x - memberBefore.x) > 100)
    report.cases.push('header moves group with members')
    await slot('nav:/overview').locator('button').focus()
    await save(() => page.keyboard.press('Control+ArrowRight'))
    assert.equal(state.groups[0].members[1], 'nav:/overview')
    await group.locator('.desktop-group__menu').focus()
    await page.keyboard.press('Enter')
    await page.locator('.desktop-group__actions').waitFor()
    await page.keyboard.press('Escape')
    await settle()
    assert.equal(await group.locator('.desktop-group__menu').evaluate(el => document.activeElement === el), true)
    report.cases.push('keyboard reorder and menu focus return')
    const thirdCell = await group.locator('[data-group-cell="0"]').boundingBox()
    await save(() => drag(slot('nav:/settings'), thirdCell.x + 45, thirdCell.y + 24))
    await save(() => group.locator('.desktop-group__toggle').click())
    assert.equal(await group.locator('.desktop-group-preview-icon').count(), 3)
    const expectedPreviewKeys = [...state.groups[0].members].sort((a, b) => state.groups[0].slots[a] - state.groups[0].slots[b])
    const expectedIconURLs = await Promise.all(expectedPreviewKeys.map(key => slot(key).locator('img').first().getAttribute('src')))
    assert.deepEqual(await group.locator('.desktop-group-preview-icon img').evaluateAll(images => images.map(image => image.getAttribute('src'))), expectedIconURLs)
    assert.equal(await group.locator('.desktop-group__previews').evaluate(el => el.inert), true)
    await page.screenshot({ path: `${out}/collapsed-dark.png` })
    await page.evaluate(() => { document.documentElement.dataset.theme = 'light' })
    await settle(); await page.screenshot({ path: `${out}/collapsed-light.png` })
    report.cases.push('collapsed ordered three-icon summary; inert preview; theme-aware glass')
    assert.equal(await slot('nav:/overview').getAttribute('aria-hidden'), 'true')
    assert.equal(await slot('nav:/overview').evaluate(el => el.inert), true)
    await page.reload(); await group.waitFor(); assert.equal(await group.locator('.desktop-group__toggle').getAttribute('aria-expanded'), 'false')
    await save(() => group.locator('.desktop-group__toggle').click())
    failNext = true
    const failed = page.waitForResponse(r => r.url().endsWith('/api/v1/desktop/workspace') && r.status() === 500)
    await group.locator('.desktop-group__toggle').click(); await failed; await settle()
    assert.equal(await group.locator('.desktop-group__toggle').getAttribute('aria-expanded'), 'true')
    const closeToast = page.getByRole('button', { name: '关闭通知', exact: true }).first()
    if (await closeToast.count()) await closeToast.click()
    report.cases.push('collapse inert; refresh persists; failed save restores')
    const cdp = await context.newCDPSession(page)
    for (const [width, height, theme, reduced, scale, locale] of [[1920, 1080, 'dark', false, 1, 'zh-CN'], [1280, 800, 'light', true, 1.25, 'en-US'], [960, 540, 'dark', false, 2, 'en-US'], [761, 700, 'dark', false, 1, 'zh-CN'], [760, 700, 'light', false, 1, 'zh-CN'], [390, 844, 'dark', true, 1, 'zh-CN'], [320, 568, 'light', true, 1, 'zh-CN']]) {
      const previousWrites = writes
      await page.evaluate(locale => localStorage.setItem('kejilion-panel-locale', locale), locale)
      await page.reload(); await group.waitFor()
      await page.setViewportSize({ width, height })
      await cdp.send('Emulation.setDeviceMetricsOverride', { width, height, deviceScaleFactor: scale, mobile: false })
      await page.emulateMedia({ reducedMotion: reduced ? 'reduce' : 'no-preference' })
      await page.evaluate(theme => { document.documentElement.dataset.theme = theme }, theme)
      await settle()
      await group.scrollIntoViewIfNeeded(); await settle()
      box = await group.boundingBox()
      assert(box.x >= -1 && box.x + box.width <= width + 1)
      for (const key of state.groups[0].members) {
        const member = await slot(key).boundingBox()
        assert(member.x >= box.x && member.x + member.width <= box.x + box.width + 1)
        assert(member.y >= box.y + 47 && member.y + member.height <= box.y + box.height + 1)
      }
      if (reduced) assert(Number.parseFloat(await group.evaluate(el => getComputedStyle(el).transitionDuration)) < 0.001)
      assert.equal(writes, previousWrites)
      await page.screenshot({ path: `${out}/${width}-${theme}.png` })
      const surface = await group.evaluate(el => {
        const probe = document.createElement('span')
        probe.style.cssText = 'background:var(--desktop-glass-strong);color:var(--text)'
        el.append(probe)
        const actual = { background: getComputedStyle(el).backgroundColor, color: getComputedStyle(el).color }
        const expected = { background: getComputedStyle(probe).backgroundColor, color: getComputedStyle(probe).color }
        probe.remove()
        return { actual, expected }
      })
      assert.deepEqual(surface.actual, surface.expected)
      const edges = await group.locator('[data-group-cell]').evaluateAll(cells => {
        const parent = cells[0].closest('.desktop-group').getBoundingClientRect()
        const boxes = cells.map(cell => cell.getBoundingClientRect())
        return [Math.min(...boxes.map(box => box.left)) - parent.left, parent.right - Math.max(...boxes.map(box => box.right)), parent.bottom - Math.max(...boxes.map(box => box.bottom))]
      })
      assert(edges.every(edge => edge >= 6 && edge <= 10), JSON.stringify(edges))
      const titleSize = await group.locator('strong').evaluate(el => parseFloat(getComputedStyle(el).fontSize))
      const memberSize = await slot('nav:/overview').locator('.desktop__icon-label').evaluate(el => parseFloat(getComputedStyle(el).fontSize))
      assert(titleSize >= 14); assert(memberSize >= 14)
      await save(() => group.locator('.desktop-group__toggle').click())
      await group.scrollIntoViewIfNeeded(); await settle()
      const collapsedBox = await group.boundingBox()
      const menuBox = await group.locator('.desktop-group__menu').boundingBox()
      assert(menuBox.x + menuBox.width <= collapsedBox.x + collapsedBox.width)
      assert(menuBox.y + menuBox.height <= collapsedBox.y + collapsedBox.height)
      assert((await group.locator('strong').boundingBox()).width >= 28)
      const miniCount = await group.locator('.desktop-group-preview-icon').evaluateAll(icons => icons.filter(icon => getComputedStyle(icon).display !== 'none').length)
      assert(miniCount >= 1 && miniCount <= 3)
      await page.screenshot({ path: `${out}/${width}-${theme}-collapsed.png` })
      await group.locator('.desktop-group__name').click()
      const narrowInput = group.locator('.desktop-group__name-input')
      await narrowInput.fill('Long group name · 运维与应用服务 · 0123456789')
      const inputBox = await narrowInput.boundingBox()
      assert(inputBox.width >= 28 && inputBox.x >= collapsedBox.x && inputBox.x + inputBox.width <= collapsedBox.x + collapsedBox.width)
      assert.equal(await narrowInput.evaluate(el => document.activeElement === el), true)
      assert.equal(await page.locator('.desktop-group-undo[role="status"]').isVisible(), false)
      await page.screenshot({ path: `${out}/${width}-${theme}-inline-rename.png` })
      await page.keyboard.press('Escape'); await settle()
      await group.locator('.desktop-group__menu').click()
      const popup = await page.locator('.desktop-group__actions').boundingBox()
      assert(popup.x >= 0 && popup.y >= 0 && popup.x + popup.width <= width && popup.y + popup.height <= height)
      await page.keyboard.press('Escape'); await settle()
      assert.equal(await group.locator('.desktop-group__menu').evaluate(el => document.activeElement === el), true)
      await save(() => group.locator('.desktop-group__toggle').click())
      report.cases.push({ width, theme, locale, reduced, titleSize, memberSize, surface: surface.actual, edges, effectiveScale: scale, scaleMethod: 'CSS viewport plus device metrics emulation', contained: true, resizeWrites: 0 })
    }
    await cdp.send('Emulation.clearDeviceMetricsOverride')
    await page.setViewportSize({ width: 1920, height: 1080 }); await settle()
    await group.locator('.desktop-group__menu').click()
    await save(() => page.getByRole('menuitem', { name: '解散分组', exact: true }).click())
    assert.equal(state.groups.length, 0)
    assert.equal(await slot('nav:/overview').getAttribute('data-group-member'), null)
    await save(() => page.locator('.desktop-group-undo').getByText('撤销', { exact: true }).click())
    assert.equal(state.groups.length, 1)
    report.cases.push('dissolve preserves entries; undo restores group')
    // Last-member move removes only the empty container; failure restores, undo recovers.
    const retiringId = state.groups[0].id
    for (const key of [...state.groups[0].members].slice(0, -1)) {
      await save(() => drag(slot(key), 1120, 520))
      assert(!state.groups[0].members.includes(key))
    }
    const lastKey = state.groups[0].members[0]
    const lastSlots = structuredClone(state.groups[0].slots)
    failNext = true
    const removalFailure = page.waitForResponse(r => r.url().endsWith('/api/v1/desktop/workspace') && r.status() === 500)
    await drag(slot(lastKey), 1120, 680); await removalFailure; await settle()
    assert.equal(state.groups.length, 1)
    assert.equal(await slot(lastKey).getAttribute('data-group-member'), retiringId)
    await save(() => drag(slot(lastKey), 1120, 680))
    assert.equal(state.groups.length, 0)
    assert.equal(await group.count(), 0)
    assert.equal(state.positions[`group:${retiringId}`], undefined)
    assert.equal(await slot(lastKey).getAttribute('data-group-member'), null)
    await save(() => page.locator('.desktop-group-undo').getByText('撤销', { exact: true }).click())
    assert.equal(state.groups[0].id, retiringId)
    assert.deepEqual(state.groups[0].slots, lastSlots)
    await save(() => drag(slot(lastKey), 1120, 680))
    await page.reload(); await slot(lastKey).waitFor(); await settle()
    assert.equal(await group.count(), 0)
    report.cases.push('last member out removes empty group and anchor; failed save restores; undo and refresh')
    // Reference-style composition uses this context's fixture, never the user's mock workspace.
    const dynamicKeys = await page.locator('[data-icon-key]').evaluateAll(icons => icons.map(icon => icon.dataset.iconKey).filter(key => key.startsWith('app:') || key.startsWith('site:')))
    const compositions = [
      ['常用运维', ['nav:/overview', 'nav:/terminal', 'nav:/files', 'nav:/docker', 'nav:/system', 'nav:/cluster'], false],
      ['应用与服务', ['nav:/sites', 'nav:/apps', ...dynamicKeys.slice(0, 4)], false],
      ['监控诊断', ['nav:/diagnostics', 'nav:/activity'], false],
      ['工具与设置', ['nav:/settings', 'nav:/ai', ...dynamicKeys.slice(4)], true],
    ]
    state.groups = compositions.map(([name, members, collapsed], index) => ({ id: String(index + 1).repeat(32), name, members, columns: 3, rows: 0, collapsed, slots: Object.fromEntries(members.map((key, cell) => [key, cell])) }))
    state.positions = Object.fromEntries(state.groups.map((item, index) => [`group:${item.id}`, { x: index % 2 ? 0.22 : 0, y: index < 2 ? 0 : 0.33 }]))
    await page.reload(); await page.locator('.desktop-group').nth(3).waitFor(); await settle()
    await page.evaluate(() => { document.documentElement.dataset.theme = 'light'; document.querySelector('.desktop__icons').scrollTop = 0 })
    await page.mouse.move(1000, 850); await settle()
    await page.screenshot({ path: `${out}/polished-composition.png` })
    assert.equal(await page.locator('.desktop-group--collapsed .desktop-group-preview-icon').count(), Math.min(3, state.groups[3].members.length))
    const lightSurface = await group.evaluate(el => getComputedStyle(el).backgroundColor)
    await page.evaluate(() => { document.documentElement.dataset.theme = 'dark' })
    await settle(); await page.screenshot({ path: `${out}/compact-composition-dark.png` })
    const darkSurface = await group.evaluate(el => getComputedStyle(el).backgroundColor)
    assert.notEqual(lightSurface, darkSurface)
    report.cases.push({ composition: 'three compact expanded groups and collapsed preview strip in both themes', lightSurface, darkSurface })
    // Reproduce the 8 + 9 reference cards: actual edges, live snap and saved anchors agree.
    const allKeys = await page.locator('[data-icon-key]').evaluateAll(icons => icons.map(icon => icon.dataset.iconKey))
    state.groups = [allKeys.slice(0, 8), allKeys.slice(8, 17)].map((members, index) => ({
      id: String(index + 5).repeat(32), name: index ? '应用与服务' : '常用运维', members,
      columns: 4, rows: 0, collapsed: false,
      slots: Object.fromEntries(members.map((key, cell) => [key, index && cell === 8 ? 11 : cell])),
    }))
    state.positions = Object.fromEntries(state.groups.map((item, index) => [`group:${item.id}`, { x: index ? .36 : 0, y: 0 }]))
    await page.reload(); await page.locator('.desktop-group').nth(1).waitFor(); await settle()
    const firstCard = page.locator('.desktop-group').nth(0)
    const secondCard = page.locator('.desktop-group').nth(1)
    const moveCard = async (card, left, top, liveCheck) => {
      await settle()
      const before = await card.boundingBox()
      const header = await card.locator('.desktop-group__header').boundingBox()
      assert(before && header)
      const x = header.x + header.width / 2, y = header.y + header.height / 2
      await page.mouse.move(x, y); await page.mouse.down()
      await page.mouse.move(x + left - before.x, y + top - before.y, { steps: 16 })
      await settle()
      if (liveCheck) await liveCheck(await card.boundingBox())
      await page.mouse.up()
    }
    const near = (actual, expected) => assert(Math.abs(actual - expected) < 1, `${actual} != ${expected}`)
    const a = await firstCard.boundingBox()
    near(a.width, 391)
    await save(() => moveCard(secondCard, a.x + a.width + 3, a.y + 7, live => {
      near(live.x - a.x - a.width, 12); near(live.y, a.y)
    }))
    const horizontal = await secondCard.boundingBox()
    near(horizontal.x - a.x - a.width, 12); near(horizontal.y, a.y)
    const savedAnchors = structuredClone(state.positions)
    await page.reload(); await secondCard.waitFor(); await settle()
    near((await secondCard.boundingBox()).x, horizontal.x)
    assert.deepEqual(state.positions, savedAnchors)
    await page.screenshot({ path: `${out}/adjacent-groups-dark.png` })
    await save(() => moveCard(secondCard, a.x + 7, a.y + a.height + 4, live => {
      near(live.y - a.y - a.height, 12); near(live.x, a.x)
    }))
    near((await secondCard.boundingBox()).y - a.y - a.height, 12)
    await page.screenshot({ path: `${out}/stacked-groups-dark.png` })
    const beforeFailure = await secondCard.boundingBox()
    failNext = true
    const failedMove = page.waitForResponse(r => r.url().endsWith('/api/v1/desktop/workspace') && r.status() === 500)
    await moveCard(secondCard, a.x + 475, a.y + 70); await failedMove; await settle()
    near((await secondCard.boundingBox()).x, beforeFailure.x)
    near((await secondCard.boundingBox()).y, beforeFailure.y)
    await save(() => moveCard(secondCard, a.x + 475, a.y))
    await secondCard.locator('.desktop-group__header').focus()
    await save(() => page.keyboard.press('Control+ArrowLeft'))
    near((await secondCard.boundingBox()).x - a.x - a.width, 12)
    const beforeBlocked = await secondCard.boundingBox()
    await moveCard(secondCard, a.x + 100, a.y + 80); await settle()
    near((await secondCard.boundingBox()).x, beforeBlocked.x)
    near((await secondCard.boundingBox()).y, beforeBlocked.y)
    report.cases.push('8+9 sparse cards: live 12px horizontal/vertical snap, refresh, keyboard, blocked drop and failed save restore')
    const groupCountBefore = state.groups.length
    await page.locator('.desktop__icons').click({ button: 'right', position: { x: 1100, y: 600 } })
    await save(() => page.getByRole('menuitem', { name: '新建分组', exact: true }).click())
    assert.equal(state.groups.length, groupCountBefore + 1)
    assert.equal(state.groups.at(-1).name, '新分组')
    assert.equal(state.groups.at(-1).members.length, 0)
    assert.equal(await page.locator('.desktop-group-form').count(), 0)
    const emptyGroup = page.locator('.desktop-group').last()
    await emptyGroup.locator('.desktop-group__menu').click()
    await page.getByRole('menuitem', { name: '重命名分组', exact: true }).click()
    await emptyGroup.locator('input').fill('我的空分组')
    await save(() => emptyGroup.locator('input').press('Enter'))
    assert.equal(state.groups.at(-1).name, '我的空分组')
    report.cases.push('blank-desktop context menu creates an empty default group immediately; menu rename edits inline')
    assert.equal(report.errors.length, 0)
    assert.equal(resourceFailure, false)
    await context.tracing.stop({ path: `${out}/trace.zip` })
    await context.close()
  } catch (error) { report.failure = error.stack; throw error }
  finally { clearInterval(watchdog); await browser.close(); report.cleanup = true; fs.writeFileSync(`${out}/result.json`, JSON.stringify(report, null, 2)) }
  console.log(JSON.stringify(report))
})().catch(error => { console.error(error); process.exitCode = 1 })
