# /etc/conf.d/kejilion-agent
#
# KPanel intentionally keeps the service command fixed. Supported Agent
# settings may be exported here when a deployment needs non-default paths.
# Example:
# export KEJILION_PUBLIC_NETWORK_LOOKUP_ENABLED=false
