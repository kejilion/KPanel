# 发布后分支归档闭环

- 提案状态：待复核（本地候选；不表示主线已采纳）
- 提案日期：2026-09-19
- 负责人：本次治理候选负责人；采纳后由唯一集成/发布任务负责
- 适用业务域：分支生命周期、发布收尾与下一轮候选筛选
- 基线提交 / 标签：`23cbb9979ce0df84801b9fe614bd1b00d7ad8a99`；当前预览 `v1.20.0-rc.3`
- 提交、推送、发布和生产权限：用户授权梳理及本地修改/候选提交；未授权本候选推送、主线、版本发布或生产

## 观察证据

2026-09-19 在管理树完成 `git fetch origin --prune` 与 management 检查，main clean 且与上述基线一致。
`git for-each-ref` 显示远端已有 193 个 `archive/` 分支；非归档除 main 外只有
`release/v1.20.0-candidate`（`c74d7d6643b3ba1c9ee731e6d0ec579de8dacb08`）和
`fix/release-metrics-chronology-20260919`（`5e18853b30d5501ccba2f22fa732133f30a45b23`）。
前者对应仍开放的 RC 序列，后者有两个未进入 main 的提交，均保留。归档引用的存在不是已上线证明。

两个历史本地工作树含未提交修改：视觉分支 `c21068848538ea7422bb419eaa5209ae49afe16a` 有规范差异；
文件切换分支 `1cc5a8421c4150f55731b32e6a829e4495be4928` 有源文件、测试及未跟踪文件，后者的已提交 tip
已由 `origin/archive/fix/file-stream-stability-20260913` 保留。它们属于本地待处置，不能自动删除或视为新候选。
本次不声称这些工作树全部归档，也不改写历史发布验收。两个已提交 tip 另存完整历史 bundle，
SHA-256 `c592caf48a97ffcccfbff71d4e14fae51d6a7220de99b055d9f451a32ea4a9ee`；bundle verify、
独立 bare clone、两个提交可读及 connectivity 检查通过。未提交内容不在 bundle 内，原工作树保持不变。

基线 release.yml 查询 candidate 失败时直接输出 already absent；删除使用没有 expected-SHA 约束的 API。
规范分别描述“公开后删除”“确认可恢复后删除”“发布后可选清理”，缺少一致的归档记录与队列排除方法。
发布工作流还允许未授权推送时交付 dirty 差异，与当前候选提交硬规则冲突。以上均来自当前入口，非推测事故。

## 原因假设

- 假设：缺少归档结束契约及唯一安全入口，使历史分支与待发布候选混淆，并让查询失败产生错误收尾状态。
- 替代解释：历史人工归档已有效（193 个引用支持这一点），当前主要缺口是复用和异常处理，不能据此宣称过去丢失代码。
- 证伪条件：下一次稳定发布仍须重新判定全部历史分支，或归档失败仍返回成功。
- 产品原则：不改变业务真源、权限、用户旅程、脚本、镜像内容或产品版本；只处理 Git 引用与工程证据。

## 基线、目标与观察窗口

| 指标 | 基线 | 目标 / 守护阈值 | 数据源 / 窗口 |
| --- | --- | --- | --- |
| 下一轮需重查的历史归档 | 193 个已归档引用，耗时未测量 | 默认队列排除 archive/，只按精确差集处理活跃分支 | 后续 2 次稳定发布的交付包 |
| 错误归档成功 | 查询失败可走 already absent，删除无 expected-SHA | 网络、分叉、冲突、并发写或事务失败一律不报成功 | Git bare 仓库回归 + 后续 2 次稳定发布 |
| 候选可恢复性 | main/tag/bundle 允许，但记录未统一 | 原 tip 与归档 SHA 一致；失败不丢失当前 tip | 远端复核与正式 tag |
| 流程开销 | 已有单发布任务与治理门禁 | 不新增监工、后台清理器、平行台账或产品构建 | 差异审查 |

## 范围与规范验收合同

- 允许：现有规范/工具入口/发布适配/验收模板，候选归档脚本、契约测试、治理接入。
- 非目标：清空全部分支、删除脏 worktree、处理活跃 RC、合并旁支修复、发布版本、生产操作或重写历史。
- 风险：工程工具 L1 + 永久规范 5.3 复核；实际版本发布仍执行 L3，不把本地测试当线上 Release 成功。
- 权威入口：项目管理 10.2 定义生命周期；通道规范定义 stable/preview；唯一脚本处理稳定候选引用。
- 固定矩阵：正确性、一致性、完整性、可执行性、效率与比例性、可演进性。
- 回归集：精确 tip 归档、只读预检、重试、RC 拒绝、tag 身份、分叉、归档冲突、网络失败、原子拒绝、并发推进、浅克隆。
- 完成条件：回归及治理门禁完成，跨入口一致，独立复核无阻断；远端未授权时交付 clean 本地候选。
- 范围外：现有发布指标修复由其独立候选处理；不为让本任务全绿吸收旁支提交。

## 备选方案与最小改动

只补文案不能修复错误成功和并发删除。全面分支管理平台成本过高，因此复用现有 archive/ 命名、Git 原子
push/expected-SHA lease、既有验收模板与治理测试，替换 release.yml 内联删除。归档指向原 tip，拒绝覆盖
不同 SHA；历史 tag/bundle 兼容，不回填历史记录。授权生产成功与分支收尾分别记录。

## 验证与证据层级

- 定向 Git 回归：已通过 12 项（归档 9 项与发布通道 3 项），全部使用隔离临时仓库，无真实远端写入。
- 治理门禁：首轮 `verify-change` 通过，191 项测试全部通过；复核修复后重跑受影响回归与同一门禁。
- 独立复核发现并修复同 SHA 双引用的 Git no-op 边界，补充写后核验失败时的真实状态与恢复说明；不承诺可阻止其他写入者后续改写归档。
- 浏览器、业务真机、公开产物、生产：不适用，本次不改变用户功能且不执行发布。
- `scriptLinkageState=not-required`：无需发布脚本（不适用）；固定脚本 commit
  `6ebb945f6d5cb69fdb41e3761de23566acbaf762` / SHA-256
  `28cf3934c01fe79a19c51fac520f11a2bdd7656d762f37d6fc4153140a6df549`，Dockerfile 与安装契约无差异。

## 独立复核

- 复核状态：待复核；不提前标记通过。
- 复核提供商 / 实现提供商：待记录。
- 本机未发现 Claude CLI；若无其他提供商入口，按跨智能体手册使用独立干净复核任务并记录限制。

## 回滚与采纳

- 规范/源码回滚点：上述精确基线；本地候选可整体放弃，采纳后使用 revert，不重写共享历史。
- Git 恢复：核对归档 tip，从精确 SHA 在新工作树读取或重放；不覆盖已有分支，不续写 archive/。
- 回滚触发：归档丢失原 tip、误处理 RC、误纳历史分支或两轮观察显示收尾反复阻断且无法局部修复。
- 决策：等待独立复核与同 SHA 候选 Linux CI，尚未主线采纳；后续两次稳定发布核对收益与保护条件。
- 观察窗口结果：未开始；不把本地夹具成功外推为生产或远端已验证。
