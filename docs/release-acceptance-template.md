# KPanel vX.Y.Z 发布验收记录模板

> 本文件是新版本验收记录的结构模板。复制为 `docs/release-vX.Y.Z-acceptance.md` 后删除本说明，
> 不回填或批量改写历史记录。状态只使用“已验证”“已实现未实机验证”“未实现”“不适用”。

日期：YYYY-MM-DD

发布级别：L3

候选提交 / 标签：`<commit>` / `vX.Y.Z`

上一稳定版本 / 回滚点：`vX.Y.Z` / `<commit-or-image-digest>`

## 发布画像

- 业务域：
- 变更面：展示 / 只读 / 宿主机写入 / 协议或数据 / 部署
- 受影响用户旅程：
- 未变化契约：API / 数据 / 端口 / Compose / Agent 权限 / `kejilion.sh` / 应用市场
- 风险等级及理由：

## 发布范围与未纳入内容

- 用户可见更新：
- 精确提交清单：
- 明确未纳入的分支、文件或后续事项：

## 多维质量结论

| 维度 | 状态 | 证据 | 未验证风险或不适用依据 |
| --- | --- | --- | --- |
| 业务正确性与双端互通 |  |  |  |
| 网络入侵与供应链安全 |  |  |  |
| 稳定性、失败恢复与兼容 |  |  |  |
| 性能与资源预算 |  |  |  |
| 用户体验与可访问性 |  |  |  |
| 数据、配置与迁移 |  |  |  |

## 自动门禁

- 定向测试及结果：
- `make verify-release` 环境和结果：
- 候选 CI：
- 主线 CI：
- Release workflow：
- 安全扫描、镜像契约、SBOM/provenance：

## 依赖与技术栈变化

- `make dependency-report` 生成时间及检测源完整性：
- 最近每日安全通告审计、EOL 复核状态及证据：
- 本版采用的依赖、工具链、基础镜像、Action、扫描器或受管脚本候选：
- 版本/锁文件/Action SHA/镜像 digest/脚本提交与摘要：
- 暂缓或拒绝候选、证据、负责人、复核日期和退出条件：
- 升级后的兼容、安全、构建、性能资源和回滚结论：

## 隔离真机与浏览器验收

- 主机/发行版/架构/运行时版本：
- 环境策略 ID 与允许用途：
- 使用的精确候选或公开产物：
- 后台作业 ID、终态、退出码、超时、证据目录、命令规格路径及 SHA-256：
- 测试窗口/循环数及风险依据（无 soak 时写不适用依据）：
- 受影响用户旅程、视口、100%/125%/200% 缩放、最小计算字号、主题、键盘/焦点、语言和失败态：
- 宿主机写入、失败注入、重启恢复和回滚结果：
- 未执行场景及原因：

## 发布产物与公开仓库复核

- GitHub Release：
- Docker 版本与 `latest` OCI index：
- `linux/amd64`、`linux/arm64` digest：
- 附件及 `SHA256SUMS`：
- 公开镜像 `image_e2e=pass`：
- `kejilion/apps` / `kejilion.sh` 契约结论：

## 生产部署安全核对

- 生产目标和部署授权范围：
- 验证/灰度环境（必须来自 `environment-policy.json`，不得包含 `prod-108`）：
- 正式部署环境（默认 `arena-154`；不得包含 `prod-108`）：
- `prod-108`：禁用全部 KPanel 操作；确认本次未连接、未备份、未部署、未升级、未核对：
- 部署前版本、健康、备份位置及摘要：
- 部署命令/入口：
- 部署后版本、Panel/Agent 状态、重启、日志、数据完整性和公网入口：
- 生产已执行写操作：
- 仅在隔离真机执行、未在生产执行的场景：

## 回滚

- 源码/tag：
- 镜像 digest：
- 数据/配置备份：
- 回滚步骤和回滚后复核：
- 回滚后生产实际版本与健康状态：
- GitHub Latest、Docker `latest` 与标准更新入口实际指向：
- 公共默认更新通道决策：恢复上一稳定版本 / 短期保留（已知问题、影响范围、负责人、修复期限）/ 不适用

## 交付节奏数据

<!-- kpanel-release-metrics:start -->
- 首个纳入提交时间：YYYY-MM-DDTHH:mm:ss+08:00
- 候选冻结时间：YYYY-MM-DDTHH:mm:ss+08:00
- 生产完成时间：YYYY-MM-DDTHH:mm:ss+08:00
- 提交到生产用时：<十进制小时；应与上述时间戳计算结果一致>
- 是否回滚、紧急热修复或重复发布：否 / 是（原因）
- 若发生失败，发现时间、恢复时间和逃逸门禁：<从下方两种格式中选择一项完整填写；未失败时填“不适用”>
<!-- kpanel-release-metrics:end -->

上述 marker 必须各出现一次且保持顺序；两者之间只能按模板顺序保留六行固定纯文本字段，不得嵌入
Markdown code 或 HTML 注释控制符；值不得留空，未知事实必须明确写“未验证”“未记录”或“不适用”。
区块外的说明、示例、代码或注释不构成机器指标证据。

<!-- kpanel-release-process-metrics:start -->
- 已记录发布流程异常或无效证据拦截次数：<非负整数；无历史证据时填“未记录”>
- 其中生产写操作开始后异常次数：<非负整数；无历史证据时填“未记录”>
<!-- kpanel-release-process-metrics:end -->

“发布流程异常或无效证据”用于记录使必需发布步骤无效、失败或需要重试的执行器、基础设施、取证和
验证通道问题；它与产品变更失败、生产回滚分别统计。业务缺陷被正常门禁拦截时保留在对应质量证据中，
不自动计为流程异常。两项计数必须同时明确填报；若异常发生在首次生产写操作之后，同时计入第二项，
并在对应验收章节保留原因、拦截点和恢复依据。历史事实不足时两项都填“未记录”，不得推断为零。

判定规则：首次生产写操作前被拦截的流程异常只计流程指标；产品载荷单独造成生产退化、回滚、紧急热修复
或重复发布时只计变更失败；流程异常发生在生产写操作后并造成上述生产影响时，两类同时计入。

### 流程异常明细

计数为 0 或“未记录”时填写“不适用”。计数大于零时，按实际事件逐项或按同一根因批次记录：

```text
- 指纹：<阶段>/<权威入口>/<根因类别>；位置：生产写前 / 生产写后；次数：<n>；影响：<事实>；
  恢复依据：<日志或状态>；永久处置：<脚本/Runner/夹具/预检提交，或有期限上游例外>；历史复发：<版本或无>
```

指纹用于发现重复根因，不替代两行机器计数，也不得把不同事件合并以降低数量。同一指纹在滚动 5 个
正式版本内出现 2 次，必须在下一次 L3 生产写前修复唯一入口并补回归；确认是不可控上游瞬时故障时
记录负责人、复核日期和退出条件。一次现场绕行不能直接写成永久处置。机器不判断指纹、根因和处置
真实性；发布复核者必须读取原始日志并比较最近 5 个验收记录。

```text
发现时间：<ISO 时间>；恢复时间：<ISO 时间>；逃逸门禁：已逃逸：<遗漏门禁和原因>
发现时间：<ISO 时间>；恢复时间：<ISO 时间>；逃逸门禁：未逃逸：<实际拦截门禁>
```

失败记录的“逃逸门禁”必须从“已逃逸”或“未逃逸”二选一并写明具体依据，不得填写“待确认”“无”
“TBD”等占位值。
机器校验只保证字段结构、计数、时间有效性/顺序和明显占位不进入记录；发布复核者仍须独立核对原始失败证据，
不得把格式通过解释为原因真实或分析完整。

## 遗留风险与后续准入

- 未验证风险：
- 已实现待实机准入：
- 不阻断本版的理由：
- 后续应进入的自动门禁或专项工作流：
