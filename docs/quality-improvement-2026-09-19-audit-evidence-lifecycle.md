# 外部审计证据与发布追踪改进

- 提案状态：试行（候选实现与独立复核通过，等待正式集成与观察）
- 提案日期：2026-09-19
- 负责人：本次治理候选维护者；正式交付状态由对应版本唯一发布任务维护。
- 适用业务域：安全审计、行级评审辅助、发布验收。
- 基线提交：`23cbb9979ce0df84801b9fe614bd1b00d7ad8a99`。
- 权限：用户授权完成规范修正、增量审计与发布闭环；先形成并推送独立候选，稳定发布仍需满足 L3 与冻结范围。

## 观察证据与假设

run-1 metadata 记录 dirty 19 文件但未保存完整 patch，无法重建全部输入。安全工作流声明 baseline_ref 却未校验 HEAD，
scoped 文字只选旧账本单元。OCR 工作流 Step 2 提取规则、Step 3 要求盲跑，执行顺序与 5.5 冲突。
这些是可重读的流程缺口，不是一次环境故障；修复不代表工具发现能力提升。
截至基线，上一稳定版以来的五条 OCR trailer 中三条 skipped、一条 unreported、一条数字增量 0；样本不足。
原 HIGH 修复 `e99e6b3d225425aa0b9afd14c55c8a33d75722f9` 已进入 RC，不能据此写成稳定版已交付。

替代解释：执行者可能已自行固定干净基线、遵循先自由后约束；文字修正的实际收益仍需后续 run 观察。
若下一轮仍无法从提交重建输入、遗漏新边界或混淆发现归属，则改进未达目标。

## 原则与范围

不改变单管理员产品权限、宿主业务真源、Panel/Agent 契约、工具 pin 或安全严重度。
只修改治理规则、两项审计工作流、基线校验入口与回归、审计状态和发布模板；风险 L1 + 5.3。
不引入新后台服务、CI 安全审计或新的发布门禁；不把代理数、覆盖率、工具退出码当漏洞检出效果。
无用户界面变化，本地功能预览不适用。
`scriptLinkageState=not-required`，无需发布脚本（不适用）。内置脚本沿用
`6ebb945f6d5cb69fdb41e3761de23566acbaf762` /
`28cf3934c01fe79a19c51fac520f11a2bdd7656d762f37d6fc4153140a6df549`；本任务不改脚本及安装协议。

## 冻结的规范验收合同

精确规范基线为上述 commit；权威入口为 PROJECT_RULES 5.4/5.5，工作流提供执行适配。
正常路径：独立 clean source → 基线检查 → scoped 账本和独立验证 → 结构验证 → 披露审查 → 候选提交 → 发布追踪。
固定矩阵：正确性、一致性、完整性、可执行性、效率与比例性、可演进性；严重度与停止条件按 5.3。
回归集：干净基线通过、错 HEAD、tracked/staged/untracked 改动拒绝、工作流渲染、治理一致性和 verify-change。
阻断清零且六项完成后结束；无关低概率优化不扩大本轮。

## 备选与最小方案

选用干净提交作为源码快照，较“自制 dirty patch + untracked 快照器”更易恢复且不收集秘密。
历史 run 不改写；治理输出先在仓库外生成，确认披露后选择性入库。scoped 从当前源码播种新单元。
OCR 自由臂先落盘，再读取圈选与规则；已见规则保持 unreported。finding 的源码修复、RC、stable、部署分开追踪。
新基线检查只在审计工作流调用；其回归进入既有治理测试，不把安全审计变成提交门禁。

## 指标与观察窗口

| 指标 | 基线 | 目标 | 窗口 |
| --- | --- | --- | --- |
| 输入可复现 | run-1 dirty patch 缺失 | 新 run 精确 commit/tree/clean | 下一次 scoped/full |
| 规则顺序 | OCR 模板先圈选 | 自由臂先落盘 | 本候选实测及下一次代码候选 |
| 状态区分 | 无集中修复交付链 | 修复、复核、RC、stable、部署各有依据 | v1.20.0 正式验收 |
| 防回归 | 非门禁、固定 pin、历史原样 | 保持，未知不计零 | 原三稳定版本试行窗口 |

## 验证、复核与回滚

定向回归 1/1、两个工作流校验与参数渲染通过；`verify-change` 在代码候选 `fc1cd39f` 上
185/185 治理测试通过。OCR 1.12.6 先自由臂落盘再读取规则，覆盖 7/7，两个臂有效发现均为 0，
constrained-only=0；这是单次治理代码样本，不证明总体检出收益。后续 `c53ea778` 只补交付追踪文档。
安全审计只读源码；不把未运行的产品测试写成通过。工具 token 成本不可用时记为未报告。
复核提供商 / 实现提供商：codex / codex（本机未安装其他提供商可调用 CLI 或工具，使用独立干净上下文）
复核状态：通过
规范验收结论：PASS。独立复核读取原始缺口、精确差异与核验日志，六项均通过；
首轮一般项为缺失追踪链接，`c53ea778` 补建并经独立复验关闭，阻断、重要、一般事项均为 0。
回滚点为 `23cbb997`；如正常审计无法执行或证据归属失真，revert 本任务治理提交，保留真实审计历史。
发布归档依赖独立候选 `docs/branch-archive-lifecycle-20260919`，不复制其规则或合入无关候选。
决策：候选试行；冻结矩阵与回归集完成，停止本轮治理复核，不提前宣称工具有效性或产品可发布。
