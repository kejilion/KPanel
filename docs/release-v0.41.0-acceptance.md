# KPanel v0.41.0 验收记录

验收日期：2026-08-03

## 上线范围

- 概览页 Hosts、root Cron、网络接口与防火墙管理。
- Docker 容器、镜像、网络、存储卷右键菜单及日常操作。
- Panel/Agent 固定协议、参数校验、审计与失败回滚。
- 镜像内置 `kejilion.sh` 固定为提交 `193a29710d18421242b2d5e2358ff36543e360cf`，SHA256 为 `7eb40d270323e91d12ce989ff13baa5b16f0feb53ef2906741d87eefcc3bb6d6`。

旧“系统中心”已彻底弃用，不属于本次发布范围；候选版本中不存在对应页面、路由、API 或部署入口。

## 154 真机业务协议

目标：Debian 13，KPanel 生产实例保持运行。

- Hosts：新增、读取、删除唯一测试记录通过，并恢复原文件。
- root Cron：新增、读取、删除唯一测试任务通过，并恢复原任务。
- 网络接口：在特权隔离网络命名空间内完成 dummy 接口启停。
- 防火墙：端口开关、Ping 开关、DDoS 开关、全部关闭/开放、SSH 22 保留均通过。
- 防火墙注入失败后，`iptables-save` 与操作前快照完全一致，事务回滚通过。

测试记录均已清理；生产网络与防火墙未被修改。

## L3 发布验收

候选源码提交：`ad8a7c72d6c456496f3a05d93ddf7185f68e1248`（本记录提交前的完整产品代码）。

- 前端：35 个测试文件、235 项测试全部通过；1524 条本地化短语校验通过；生产构建通过。
- 依赖：`npm audit --audit-level=high` 为 0 漏洞。
- Go：`go test ./...`、核心特权包 `go test -race`、`go vet ./...` 全部通过。
- `govulncheck`：产品调用路径受影响漏洞为 0。
- 应用契约：生态规范、部署校验和 `kpanel.conf` 安装/更新/回滚/卸载生命周期通过。
- Trivy：源码依赖、Secret、Dockerfile 配置和最终镜像 HIGH/CRITICAL 扫描均为 0。
- 候选镜像：版本、源码 revision、脚本 revision/SHA256 元数据校验通过；`image_e2e=pass`。
- 汇总输出：`L3_154=pass`。
- 验收结束后生产容器状态：`running healthy`。

## 回滚点

- 源码与镜像：`v0.40.3`。
- 数据目录不迁移、不删除；更新失败使用既有应用市场回滚流程恢复旧镜像和 Agent。
